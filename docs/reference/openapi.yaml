components:
  schemas:
    AboutPersonBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AboutPersonBody.json
          format: uri
          readOnly: true
          type: string
        admin:
          description: Whether they administer this deployment
          type: boolean
        audits:
          description: Whether they may read this deployment's own records. It grants no product's findings or decisions
          type: boolean
        deactivated_at:
          description: The date they left. Absent means they are active
          type: string
        display_name:
          type: string
        held:
          items:
            $ref: "#/components/schemas/HeldChangeBody"
          type:
            - array
            - "null"
        held_total:
          description: The number of role changes, of which the list above is a page
          format: int64
          type: integer
        holds:
          items:
            $ref: "#/components/schemas/HeldBody"
          type:
            - array
            - "null"
        identity:
          type: string
        record:
          $ref: "#/components/schemas/PersonRecordBody"
        sees_nothing:
          description: Every role they hold is a capability, so they reach no product. A capability is bounded by what its holder may read, so on its own it grants nothing
          type: boolean
        told:
          items:
            $ref: "#/components/schemas/ToldBody"
          type:
            - array
            - "null"
        told_total:
          description: "The number of things they were told that you may read, of which the list above is a page. Narrowed like the list: administering decides who may ask, not what the answer contains"
          format: int64
          type: integer
      required:
        - identity
        - held_total
        - record
        - told_total
      type: object
    Acknowledge-all-notificationsResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Acknowledge-all-notificationsResponse.json
          format: uri
          readOnly: true
          type: string
        acknowledged:
          description: The number that were waiting
          format: int64
          type: integer
      required:
        - acknowledged
      type: object
    Acknowledgment:
      additionalProperties: false
      properties:
        names:
          items:
            type: string
          type:
            - array
            - "null"
        summary:
          type: string
      type: object
    Add-advisory-issueRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Add-advisory-issueRequest.json
          format: uri
          readOnly: true
          type: string
        product:
          minLength: 1
          type: string
        vulnerability:
          description: The identifier the issue is filed under
          minLength: 1
          type: string
      required:
        - product
        - vulnerability
      type: object
    Add-outboundRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Add-outboundRequest.json
          format: uri
          readOnly: true
          type: string
        kind:
          description: One notification kind, or * for all of them
          maxLength: 191
          minLength: 1
          type: string
        name:
          maxLength: 191
          minLength: 1
          type: string
        secret:
          description: The signing secret. Never returned by any endpoint
          maxLength: 400
          minLength: 16
          type: string
        url:
          description: The address to send to. https only
          maxLength: 1000
          minLength: 1
          type: string
      required:
        - name
        - kind
        - url
        - secret
      type: object
    Add-routing-ruleRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Add-routing-ruleRequest.json
          format: uri
          readOnly: true
          type: string
        beneath:
          description: A component name, matching it and everything under it
          type: string
        name:
          maxLength: 120
          minLength: 1
          type: string
        team:
          description: The team work lands on, by name
          minLength: 1
          type: string
        upstream:
          description: A source package name
          type: string
      required:
        - name
        - team
      type: object
    AdvisoryBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AdvisoryBody.json
          format: uri
          readOnly: true
          type: string
        advisory:
          description: The identifier this deployment minted, which is what the document is tracked by
          type: string
        agreed:
          description: How many people agree to what it says now. None means it cannot go out
          format: int64
          type: integer
        covers:
          items:
            $ref: "#/components/schemas/CoveredBody"
          type:
            - array
            - "null"
        minted_at:
          type: string
        status:
          description: Where the document is in its life. Final where somebody agrees to what it says now, interim where it has gone out and nobody does, draft before either
          enum:
            - draft
            - final
            - interim
          type: string
        title:
          type: string
      required:
        - advisory
        - minted_at
        - covers
      type: object
    AdvisoryListedBody:
      additionalProperties: false
      properties:
        advisory:
          type: string
        agreed:
          description: How many people agree to what it says now
          format: int64
          type: integer
        issuances:
          description: How many times it has gone out
          format: int64
          type: integer
        issues:
          description: How many issues it covers
          format: int64
          type: integer
        minted_at:
          type: string
        products:
          description: How many products those sit in
          format: int64
          type: integer
        status:
          description: Where the document is in its life
          enum:
            - draft
            - final
            - interim
          type: string
        title:
          type: string
      required:
        - advisory
        - issues
        - products
        - issuances
        - status
        - agreed
        - minted_at
      type: object
    AffectsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AffectsBody.json
          format: uri
          readOnly: true
          type: string
        added:
          description: Builds it is now filed against that it was not
          format: int64
          type: integer
        closed:
          description: Builds taken back out, closed as invalid because they were never affected
          format: int64
          type: integer
      required:
        - added
        - closed
      type: object
    AgreedBody:
      additionalProperties: false
      properties:
        at:
          description: The moment they agreed
          type: string
        by:
          description: Their sign-in identity
          type: string
        carried:
          description: Whether this agreement was carried forward from an earlier claim rather than given for this one
          type: boolean
        withdrawn_at:
          description: The moment the agreement was taken back, by the approver or by somebody editing the words it was given for
          type: string
      required:
        - by
        - at
      type: object
    AgreementBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AgreementBody.json
          format: uri
          readOnly: true
          type: string
        agreed_at:
          type: string
        edition:
          description: Which edition was agreed to, counting from one within this advisory. A later edition is a document nobody has agreed to yet
          format: int64
          type: integer
      required:
        - edition
        - agreed_at
      type: object
    AlsoBuild:
      additionalProperties: false
      properties:
        stream:
          type: string
        variant:
          type: string
        version:
          description: The version that build ships under this name, where it ships more than one
          type: string
      required:
        - stream
        - variant
      type: object
    Amend-productRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Amend-productRequest.json
          format: uri
          readOnly: true
          type: string
        display_name:
          description: What screens and documents should show instead. Omitted leaves it alone
          maxLength: 191
          type: string
        name:
          description: What scans and paths should call it instead. Omitted leaves it alone
          maxLength: 191
          type: string
      type: object
    Amend-streamRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Amend-streamRequest.json
          format: uri
          readOnly: true
          type: string
        name:
          description: What to call it instead
          maxLength: 191
          minLength: 1
          type: string
      required:
        - name
      type: object
    Amend-variantRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Amend-variantRequest.json
          format: uri
          readOnly: true
          type: string
        customer_facing:
          description: Whether it reaches customers. Omitted leaves it alone
          type: boolean
        name:
          description: What to call it instead. Omitted leaves the name alone
          maxLength: 191
          type: string
      type: object
    AnywhereOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AnywhereOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/FindingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    ApprovalBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        batch:
          description: The batch it was approved under, if it was a bulk approval
          type: string
        carried_from:
          description: The approval this was carried forward from, where a re-affirmation stood on an earlier agreement rather than a fresh one
          format: int64
          type: integer
        covered:
          description: Findings this covered when it was agreed to
          format: int64
          type: integer
        id:
          format: int64
          type: integer
        revision_id:
          description: The revision of the justification that was agreed to
          format: int64
          type: integer
        withdrawn_at:
          description: The moment this approval was taken back, if it was
          type: string
      required:
        - id
        - revision_id
        - approved_by
        - approved_at
      type: object
    AroundBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AroundBody.json
          format: uri
          readOnly: true
          type: string
        above:
          description: The consumers that pull this in — usually short, and the direction people use
          items:
            $ref: "#/components/schemas/NeighborBody"
          type:
            - array
            - "null"
        below:
          description: The components it pulls in, downward
          items:
            $ref: "#/components/schemas/NeighborBody"
          type:
            - array
            - "null"
      required:
        - above
        - below
      type: object
    AssessmentBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AssessmentBody.json
          format: uri
          readOnly: true
          type: string
        id:
          format: int64
          type: integer
        mine:
          description: You made this rating, so you may not be the one who agrees
          type: boolean
        needs_approval:
          description: Whether a second person has to agree before it takes effect
          type: boolean
        off_the_list:
          description: The number this rating would put below the product's triage line, where they stop being work and carry no deadline
          format: int64
          type: integer
        open:
          description: Open findings of this issue you can see in this product
          format: int64
          type: integer
        product:
          description: The product this rating belongs to, by the name an address takes
          type: string
        product_name:
          description: That product's spelling on screen
          type: string
        published:
          description: The published rating when this was made, kept so a reader can see what we disagreed with
          type: string
        reasoning:
          description: The reasoning. It outlives the version it was made about, so the next person needs the argument
          maxLength: 65536
          minLength: 1
          type: string
        severity:
          description: This product's own rating
          enum:
            - low
            - medium
            - high
            - critical
          type: string
        state:
          enum:
            - proposed
            - live
            - withdrawn
          type: string
        vulnerability:
          description: The issue this is about
          type: string
      required:
        - severity
        - reasoning
      type: object
    Assign-findingRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Assign-findingRequest.json
          format: uri
          readOnly: true
          type: string
        person:
          description: Their sign-in identity, or empty for nobody
          type: string
        team:
          description: "A team to route it to instead, by name. A queue rather than a holding: it stays unheld until somebody takes it"
          type: string
      type: object
    AtComponentBody:
      additionalProperties: false
      properties:
        due:
          description: The date it runs out. The earliest among its places here, which is the one that makes it late
          type: string
        exploited:
          description: Somebody is known to be exploiting this
          type: boolean
        fixed_in:
          description: The version the report says fixes it, where it names one
          type: string
        likelihood:
          description: Published estimate that this will be exploited, 0 to 1
          format: double
          type: number
        places:
          description: The number of places in this build it sits at
          format: int64
          type: integer
        severity:
          description: The severity the report gives it
          type: string
        summary:
          description: The first line of what the issue says about itself, cut to fit a row
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - places
      type: object
    AttachmentBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/AttachmentBody.json
          format: uri
          readOnly: true
          type: string
        content_type:
          description: The type it is served as, which is decided here and is not what the uploader called it
          type: string
        filename:
          description: Its name when it arrived
          type: string
        inline:
          description: Whether it is displayed in the page rather than downloaded. Only a small allowlist of raster image types is
          type: boolean
        redacted:
          description: The file was removed on purpose. The record of it remains
          type: boolean
        redacted_reason:
          description: The reason it was removed
          type: string
        reference:
          description: The reference to paste into a justification or a comment
          type: string
        size:
          description: Bytes
          format: int64
          type: integer
        token:
          description: Refer to this in text as attachment:<token>. It is the only identifier for a file, and never an address
          type: string
        uploaded_at:
          type: string
      required:
        - token
        - reference
        - filename
        - content_type
        - size
        - uploaded_at
      type: object
    BecameBody:
      additionalProperties: false
      properties:
        by:
          description: The person who did it, where a person did
          type: string
        claim:
          $ref: "#/components/schemas/ClaimBody"
        decision:
          $ref: "#/components/schemas/DecisionBody"
        decisions:
          description: The number of rows the claim wrote
          format: int64
          type: integer
        finding:
          $ref: "#/components/schemas/FindingRefBody"
          description: The representative decision's subject. Absent where no open finding sits at its place
        happened:
          description: The claim's outcome
          enum:
            - waiting
            - sent-back
            - approved
            - withdrawn
            - lapsed
            - undone
            - mixed
          type: string
        issues:
          description: The number of distinct issues it covers
          format: int64
          type: integer
        outliers:
          $ref: "#/components/schemas/OutliersBody"
          description: The rows in a bulk claim that do not look like the rest. Absent for a claim about one issue and for one nothing can still be held back from
        place:
          $ref: "#/components/schemas/PlaceBody"
        places:
          description: The number of distinct places it covers
          format: int64
          type: integer
        reasoning:
          type: string
        when:
          description: The moment it became that
          type: string
      required:
        - claim
        - decision
        - place
        - happened
        - reasoning
        - decisions
        - issues
        - places
      type: object
    BecameOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/BecameOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/BecameBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    BindingBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/BindingBody.json
          format: uri
          readOnly: true
          type: string
        group:
          description: The group exactly as the provider names it — a team slug, or a claim value. Matched with its capitals, because it is the provider's identity rather than a name typed here
          maxLength: 191
          minLength: 1
          type: string
        product:
          description: The product the role is held against, by the name that addresses it
          type: string
        product_display_name:
          description: That product's display name, where it was declared with one
          type: string
        role:
          description: The role membership of this group grants
          enum:
            - approver
            - assigner
            - public-read
            - private-read
            - public-triage
            - private-triage
            - admin
            - audit
          type: string
      required:
        - group
        - role
      type: object
    BlockingBody:
      additionalProperties: false
      properties:
        component:
          type: string
        due:
          type: string
        exploited:
          type: boolean
        places:
          description: The number of places of the build it sits at
          format: int64
          type: integer
        severity:
          type: string
        state:
          description: The decision state. Anything agreed is not in this list
          enum:
            - undecided
            - waiting
            - lapsed
          type: string
        version:
          description: The version this sits at, which is what tells two rows of one component apart
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - component
        - places
      type: object
    Branch:
      additionalProperties: false
      properties:
        branches:
          items:
            $ref: "#/components/schemas/Branch"
          type:
            - array
            - "null"
        category:
          type: string
        name:
          type: string
        product:
          $ref: "#/components/schemas/Named"
      required:
        - category
        - name
      type: object
    BucketBody:
      additionalProperties: false
      properties:
        by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: The same count by how the issues were rated. 'unrated' is what nobody scored
          type: object
        days:
          description: The start of the stretch, so these can be ordered without reading the label
          format: int64
          type: integer
        label:
          type: string
        open:
          format: int64
          type: integer
        undecided:
          description: The number nobody has said anything about. A claim waiting for a second person is not an answer
          format: int64
          type: integer
      required:
        - label
        - days
        - open
        - undecided
      type: object
    BuildCountsBody:
      additionalProperties: false
      properties:
        critical:
          format: int64
          type: integer
        high:
          format: int64
          type: integer
        kind:
          enum:
            - branch
            - tag
          type: string
        last_scanned_at:
          type: string
        low:
          format: int64
          type: integer
        medium:
          format: int64
          type: integer
        stream:
          type: string
        total:
          format: int64
          type: integer
        variant:
          type: string
      required:
        - stream
        - variant
        - critical
        - high
        - medium
        - low
        - total
      type: object
    BuildName:
      additionalProperties: false
      properties:
        stream:
          type: string
        variant:
          type: string
      required:
        - stream
        - variant
      type: object
    BuildStandingBody:
      additionalProperties: false
      properties:
        agreed:
          description: The number answered at every place by a standing decision
          format: int64
          type: integer
        exploited:
          description: The number somebody is known to be exploiting
          format: int64
          type: integer
        last_scan_at:
          description: The moment a scan last arrived here. Absent where none ever has
          type: string
        open:
          description: Issues at components, the unit every count here uses
          format: int64
          type: integer
        out_of_support:
          description: This build's release is out of support
          type: boolean
        overdue:
          description: The number past a deadline
          format: int64
          type: integer
        stream:
          type: string
        stream_kind:
          description: Whether the line moves or is fixed
          enum:
            - branch
            - tag
          type: string
        undecided:
          description: The number nobody has claimed anything about
          format: int64
          type: integer
        variant:
          type: string
      required:
        - stream
        - variant
        - open
      type: object
    BulkApprovalBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        batch:
          type: string
        claims:
          format: int64
          type: integer
        rows:
          format: int64
          type: integer
      required:
        - batch
        - approved_by
        - approved_at
        - claims
        - rows
      type: object
    BundleBody:
      additionalProperties: false
      properties:
        builds:
          description: The number of builds of the selection holding any of it. Absent where the selection is one build
          format: int64
          type: integer
        components:
          description: The packages this one upgrade moves. More than one where a source package builds several
          items:
            type: string
          type:
            - array
            - "null"
        exploited:
          description: Some of what it closes is being exploited
          type: boolean
        from:
          description: The version in hand
          type: string
        in:
          description: The builds that hold this upgrade
          items:
            $ref: "#/components/schemas/BuildName"
          type:
            - array
            - "null"
        issues:
          description: Distinct vulnerabilities the upgrade closes
          format: int64
          type: integer
        places:
          description: The number of findings those sit at
          format: int64
          type: integer
        severity:
          description: The worst of what it closes
          type: string
        to:
          description: The version that fixes it, as whoever packages the component wrote it. Never compared against what ships, only grouped
          type: string
        upstream:
          description: "The upgrade's subject: the source package where one is recorded, and the component's own name otherwise"
          type: string
      required:
        - upstream
        - from
        - to
        - components
        - issues
        - places
        - in
      type: object
    CVSSv3:
      additionalProperties: false
      properties:
        baseScore:
          format: double
          type: number
        baseSeverity:
          type: string
        vectorString:
          type: string
        version:
          type: string
      required:
        - version
        - vectorString
        - baseScore
        - baseSeverity
      type: object
    CanBody:
      additionalProperties: false
      properties:
        may_agree:
          description: Agree to somebody else's claim, or send it back. The approver capability or a triage role on the product — a triager may answer somebody else's claim, which is the ordinary shape of a small team; that the two are different people is checked separately and has no override
          type: boolean
        may_assign:
          description: Give work to somebody else, or take what they hold — triage as well as the assigner role. Taking work nobody owns, and handing back your own, need only may_triage
          type: boolean
        may_hide:
          description: Argue about a finding nobody has disclosed
          type: boolean
        may_see:
          description: Read findings that have been disclosed
          type: boolean
        may_triage:
          description: Argue about a finding
          type: boolean
        name:
          description: The label shown for it
          type: string
        product:
          type: string
        sees_all:
          description: Read findings nobody has disclosed yet
          type: boolean
      required:
        - product
        - name
        - may_see
        - sees_all
        - may_triage
        - may_assign
        - may_hide
        - may_agree
      type: object
    CarriedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CarriedBody.json
          format: uri
          readOnly: true
          type: string
        absent:
          description: Cover nothing in the new line
          format: int64
          type: integer
        applying:
          description: Reach it by matching. Nothing to choose
          format: int64
          type: integer
        moved:
          description: The version differs, so each needs a fresh answer
          items:
            $ref: "#/components/schemas/InheritedBody"
          type:
            - array
            - "null"
        postponed:
          description: Deferrals, offered separately and never carried by default
          items:
            $ref: "#/components/schemas/InheritedBody"
          type:
            - array
            - "null"
      required:
        - applying
        - moved
        - postponed
        - absent
      type: object
    CarriedClaimBody:
      additionalProperties: false
      properties:
        justification:
          type: string
        pedigree:
          description: The claim arrived attached to a component — a carried patch saying what it fixes — rather than in a document of its own
          type: boolean
        since:
          description: The date the build first said it, by the scan it arrived in
          type: string
        statement:
          description: The build's own reasoning, shown as written and never rendered
          type: string
        status:
          description: The claim, in the exchange format's own vocabulary
          type: string
        subject:
          description: The claim's subject — a package name, or its identifier where it named no name
          type: string
        suppresses:
          description: Whether it takes a finding off the list. 'affected' and 'under investigation' are information, not answers
          type: boolean
        until:
          description: The moment it stopped saying it. Absent while it is still being said
          type: string
        vulnerability:
          description: The identifier the build argued about, as it wrote it
          type: string
      required:
        - vulnerability
        - subject
        - status
        - pedigree
        - suppresses
        - since
      type: object
    Carry-decisionsRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Carry-decisionsRequest.json
          format: uri
          readOnly: true
          type: string
        decisions:
          description: The offered judgments to carry
          items:
            format: int64
            type: integer
          minItems: 1
          type:
            - array
            - "null"
      required:
        - decisions
      type: object
    Carry-decisionsResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Carry-decisionsResponse.json
          format: uri
          readOnly: true
          type: string
        carried:
          description: The number of claims written, each waiting for a second person
          format: int64
          type: integer
      required:
        - carried
      type: object
    CatchesOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CatchesOutputBody.json
          format: uri
          readOnly: true
          type: string
        components:
          description: The components it names, at most twenty
          items:
            type: string
          type:
            - array
            - "null"
        total:
          description: The number of distinct components it matches. More than the list where the list was cut
          format: int64
          type: integer
        unheld:
          description: The number of those nobody holds, which is what a rule may place
          format: int64
          type: integer
        work:
          description: Pieces of work at those components — one issue in one component, whatever it sits at
          format: int64
          type: integer
      required:
        - components
        - total
        - work
        - unheld
      type: object
    ChangeBody:
      additionalProperties: false
      properties:
        about:
          type: string
        at:
          type: string
        became:
          type: string
        by:
          description: The person who made the change, by sign-in identity
          type: string
        cleared:
          description: This change cleared it
          type: boolean
        kind:
          description: The kind of thing that changed
          enum:
            - setting
            - role
            - routing
            - support
            - release
            - credential
            - account
            - team
            - case
            - alias
            - catalog
          type: string
        unset:
          description: Whether nothing had been set before this, as distinct from a value stored empty
          type: boolean
        was:
          type: string
      required:
        - at
        - by
        - kind
        - about
      type: object
    ChangedBody:
      additionalProperties: false
      properties:
        arrived_from:
          description: The version this was upgraded from since the earlier build. Only on still-present entries, where it means the upgrade did not reach the fix
          type: string
        because:
          description: The reason it went. Only on fixed entries
          enum:
            - removed
            - upgraded
            - revised
            - superseded
            - unexplained
          type: string
        closed_by_run:
          description: The run that stopped reporting it. Only on an entry that left the affected list, and absent where a person closed it
          format: int64
          type: integer
        component:
          type: string
        due:
          description: The soonest deadline among the places still open, as a date
          type: string
        from_version:
          description: The version the place held before the fix. Only on a fixed entry the version moved for
          type: string
        justification:
          description: The recognized reason it does not apply, on a dismissal
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        moved_to:
          description: The version the place moved to. Only on a fixed entry the version moved for, so a removed component carries neither
          type: string
        outcome:
          description: The decision, where every standing one over its places says the same thing
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        severity:
          type: string
        state:
          description: The decision state in this build. Only on a still-present entry. Absent where some places are agreed and the rest were never decided, which is none of the four
          enum:
            - undecided
            - waiting
            - agreed
            - lapsed
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - component
      type: object
    ClaimApprovalBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ClaimApprovalBody.json
          format: uri
          readOnly: true
          type: string
        batch:
          description: Name a batch to agree to several claims under one name, so they can be undone together. At most 64 characters
          maxLength: 64
          type: string
        because:
          description: The reason the rows in except are set aside, in markdown. Required when any are
          type: string
        except:
          description: Decisions in this claim to set aside rather than approve. They return to the proposer as a claim of their own, carrying the reason given in because
          items:
            format: int64
            type: integer
          maxItems: 2000
          type:
            - array
            - "null"
      type: object
    ClaimApprovedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ClaimApprovedBody.json
          format: uri
          readOnly: true
          type: string
        approved:
          description: The number of decisions agreed to
          format: int64
          type: integer
        returned_claim:
          description: The claim the rows set aside went into, where any were
          format: int64
          type: integer
      required:
        - approved
      type: object
    ClaimBody:
      additionalProperties: false
      properties:
        derived_from:
          description: The claim this one came from, for an extension or a returned set
          format: int64
          type: integer
        elsewhere:
          description: A ticket, a thread or a change. Stored and never fetched
          type: string
        id:
          format: int64
          type: integer
        kind:
          description: "The sort of action: one judgment about a finding, one about many issues at a component, an approved claim carried to a new issue, or rows set aside from a larger claim — by an approver agreeing to the rest, or by the author holding them back"
          enum:
            - finding
            - together
            - extension
            - returned
          type: string
        proposed_at:
          description: The moment the action was taken
          type: string
        proposed_by:
          type: string
        selected_by:
          description: The narrowing behind a bulk set. Never part of the claim itself
          type: string
        selection:
          $ref: "#/components/schemas/SelectionBody"
          description: The narrowing behind a bulk claim, as something you can re-run. Absent on a claim that was not one
      required:
        - id
        - kind
        - proposed_by
        - proposed_at
      type: object
    ClaimDetail:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ClaimDetail.json
          format: uri
          readOnly: true
          type: string
        age_days:
          description: The age of the claim. An old judgment should look like one
          format: int64
          type: integer
        argument:
          $ref: "#/components/schemas/DecisionBody"
          description: "The claim's contents: the outcome, the reason, the dates, the version an upgrade moves to, and the justification as it currently stands"
        builds:
          description: Every build the claim currently covers, as stream and variant
          items:
            type: string
          type:
            - array
            - "null"
        by:
          description: The person who did it, where a person did
          type: string
        claim:
          $ref: "#/components/schemas/ClaimBody"
          description: The actor, the moment, the sort of action, the narrowing behind a bulk set, and where the work is happening
        consumers:
          description: The number of things pulling them in
          format: int64
          type: integer
        finding:
          $ref: "#/components/schemas/FindingRefBody"
          description: A representative row's subject — build, issue, component, where it sits. Absent where no open finding sits at its place
        findings:
          description: The number of findings underneath, which is what the disposition register expands to
          format: int64
          type: integer
        folds:
          description: The number of things to decide about
          format: int64
          type: integer
        happened:
          description: The claim's outcome. mixed is a claim whose rows did not all end the same way
          enum:
            - waiting
            - sent-back
            - approved
            - withdrawn
            - lapsed
            - undone
            - mixed
          type: string
        issues:
          description: The number of distinct issues it covers
          format: int64
          type: integer
        outliers:
          $ref: "#/components/schemas/OutliersBody"
          description: "For a claim over many issues: the rows that do not look like the rest, and how many there are"
        packages:
          description: The number of binaries those fold together
          format: int64
          type: integer
        place:
          $ref: "#/components/schemas/PlaceBody"
        places:
          description: The number of distinct places it wrote at. The bulk cap is measured against this
          format: int64
          type: integer
        previously_approved:
          description: This was agreed to before and came back
          type: boolean
        rows:
          description: The number of decisions the claim wrote
          format: int64
          type: integer
        undisclosed:
          description: At least one row is about a finding nobody has announced
          type: boolean
        when:
          description: "The moment it became that. Absent while it is waiting: nothing has happened to it"
          type: string
      required:
        - claim
        - argument
        - place
        - age_days
        - happened
        - rows
        - issues
        - places
        - folds
        - packages
        - consumers
        - findings
        - builds
      type: object
    CollaboratorBody:
      additionalProperties: false
      properties:
        added_at:
          type: string
        added_by:
          type: string
        identity:
          type: string
        name:
          description: Their display name, where they have one
          type: string
      required:
        - identity
        - added_by
        - added_at
      type: object
    Comment-on-claimRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Comment-on-claimRequest.json
          format: uri
          readOnly: true
          type: string
        body:
          minLength: 1
          type: string
      required:
        - body
      type: object
    Comment-on-claimResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Comment-on-claimResponse.json
          format: uri
          readOnly: true
          type: string
        id:
          format: int64
          type: integer
        not_notified:
          description: Names written after an @ that reached nobody. Either no such person is recorded, or they cannot read what the text is about — deliberately not said which
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - id
      type: object
    CommentBody:
      additionalProperties: false
      properties:
        body:
          description: The comment text, in markdown
          type: string
        edited_at:
          description: The moment the author last changed it, if they did
          type: string
        id:
          format: int64
          type: integer
        written_at:
          type: string
        written_by:
          type: string
      required:
        - id
        - body
        - written_by
        - written_at
      type: object
    ComparisonBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ComparisonBody.json
          format: uri
          readOnly: true
          type: string
        closed_not_fixed:
          description: Left the affected list without being fixed
          items:
            $ref: "#/components/schemas/ChangedBody"
          type:
            - array
            - "null"
        fixed:
          items:
            $ref: "#/components/schemas/ChangedBody"
          type:
            - array
            - "null"
        newly_present:
          items:
            $ref: "#/components/schemas/ChangedBody"
          type:
            - array
            - "null"
        still_present:
          items:
            $ref: "#/components/schemas/ChangedBody"
          type:
            - array
            - "null"
      required:
        - fixed
        - closed_not_fixed
        - newly_present
        - still_present
      type: object
    ComponentFindingBody:
      additionalProperties: false
      properties:
        by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: Those issues by how they were rated. 'unrated' is what nobody scored
          type: object
        component:
          type: string
        ecosystem:
          description: The kind of package, as its identifier spells it. With the component and version it tells one row from another, which those two alone do not
          type: string
        exploited:
          description: Whether any of them is known-exploited
          type: boolean
        issues:
          description: Distinct vulnerabilities open against it, which is how many rows it contributes to the findings list
          format: int64
          type: integer
        places:
          description: The number of times those sit somewhere in the build
          format: int64
          type: integer
        source_package:
          description: "The source package this was built from, where one is recorded. What a routing rule matches on: several binary packages of one source move together, so a rule names the source rather than each binary"
          type: string
        upgrades:
          description: Versions upstream released that would close some of what is open here, furthest along first where the ecosystem defines an ordering and unranked where it does not
          items:
            $ref: "#/components/schemas/UpgradeBody"
          type:
            - array
            - "null"
        upstream:
          description: The upstream a fork was cut from, where one is known
          type: string
        version:
          type: string
        worst:
          description: The highest band among them. Absent where nothing here was rated
          enum:
            - critical
            - high
            - medium
            - low
          type: string
      required:
        - component
        - version
        - issues
        - places
        - exploited
      type: object
    ComponentFindingsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ComponentFindingsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ComponentFindingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    CounterBody:
      additionalProperties: false
      properties:
        elsewhere:
          additionalProperties:
            format: int64
            type: integer
          description: Approved claims about this issue at other places, counted by outcome
          type: object
        undecided:
          description: Other issues at the same place nobody has answered
          format: int64
          type: integer
      type: object
    CoverageBody:
      additionalProperties: false
      properties:
        last_received_at:
          description: The moment a scan last arrived. Absent where none ever has
          type: string
        last_refused_at:
          description: The moment an upload against this build was last turned away. Absent where none has been
          type: string
        out_of_support:
          description: Whether this build's release is out of support, in which case silence is expected and it is never reported as quiet
          type: boolean
        product:
          type: string
        quiet:
          description: Whether that is longer than this deployment allows
          type: boolean
        quiet_days:
          description: The span since, in days, measured from the last arrival or from when the build was declared
          format: int64
          type: integer
        refused_because:
          description: The words the producer was given the last time one was turned away, in the same words they were given
          type: string
        retired:
          description: Whether the product, release or variant has been taken out of use. No scan may be filed against it, so it is never reported as quiet
          type: boolean
        stream:
          type: string
        stream_kind:
          description: Whether this line moves
          enum:
            - branch
            - tag
          type: string
        variant:
          type: string
      required:
        - product
        - stream
        - stream_kind
        - variant
        - quiet_days
      type: object
    CoverageOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CoverageOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/CoverageBody"
          type:
            - array
            - "null"
        never:
          description: The number in support never scanned, across every build and not only this page
          format: int64
          type: integer
        quiet:
          description: The number gone quiet, across every build and not only this page
          format: int64
          type: integer
        quiet_after_days:
          description: The span this deployment allows, in days
          format: int64
          type: integer
        total:
          description: The number of builds to report on
          format: int64
          type: integer
        unsupported:
          description: The number out of support, across every build and not only this page. Silence there is expected, so these are never counted as quiet
          format: int64
          type: integer
      required:
        - items
        - quiet
        - never
        - unsupported
        - total
        - quiet_after_days
      type: object
    CoveredBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/CoveredBody.json
          format: uri
          readOnly: true
          type: string
        added_at:
          type: string
        product:
          type: string
        product_name:
          description: The product's display name, where it has one
          type: string
        summary:
          type: string
        vulnerability:
          type: string
      required:
        - product
        - vulnerability
        - added_at
      type: object
    CoveredBuild:
      additionalProperties: false
      properties:
        covered:
          description: The number of findings those places hold
          format: int64
          type: integer
        recorded:
          description: The number of places it was written against there
          format: int64
          type: integer
        stream:
          type: string
        variant:
          type: string
        version:
          type: string
      required:
        - stream
        - variant
        - recorded
        - covered
      type: object
    Deactivate-personResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Deactivate-personResponse.json
          format: uri
          readOnly: true
          type: string
        already:
          description: They had already left, and the date did not move
          type: boolean
        released:
          description: Findings handed back because they are gone
          format: int64
          type: integer
        since:
          description: The date they left
          type: string
      required:
        - released
        - since
      type: object
    Decide-togetherRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Decide-togetherRequest.json
          format: uri
          readOnly: true
          type: string
        contains:
          description: "The text you narrowed the candidate list by, if any. Re-run here rather than believed: what is recorded beside your sentence is how many issues that narrowing reaches against how many you named, so an approver can check the two"
          maxLength: 200
          type: string
        deferred_until:
          description: Required when it is deferred. A date, as 2026-03-31
          type: string
        fixed_version:
          description: Required when the outcome is already-fixed. The package version whoever packages this states the fix arrived in — which must be one release carrying the fix for every issue named, since the claim has to hold for all of them
          type: string
        justification:
          description: Required when it does not apply
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
          type: string
        reasoning:
          description: The reasoning, holding for every issue named
          minLength: 1
          type: string
        selected_by:
          description: The narrowing, in your own words. Recorded, and never part of the claim
          maxLength: 500
          minLength: 1
          type: string
        vulnerabilities:
          description: The issues this claim covers, by name
          items:
            type: string
          maxItems: 2000
          minItems: 1
          type:
            - array
            - "null"
      required:
        - vulnerabilities
        - selected_by
        - outcome
        - reasoning
      type: object
    Decide-togetherResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Decide-togetherResponse.json
          format: uri
          readOnly: true
          type: string
        claim_id:
          description: The claim this action made, which is what the review queue lists and what is approved
          format: int64
          type: integer
        ids:
          items:
            format: int64
            type: integer
          type:
            - array
            - "null"
        recorded:
          format: int64
          type: integer
      required:
        - claim_id
        - recorded
        - ids
      type: object
    DecidedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DecidedBody.json
          format: uri
          readOnly: true
          type: string
        also:
          description: The rows this judgment wrote in each other build it reached
          items:
            $ref: "#/components/schemas/CoveredBuild"
          type:
            - array
            - "null"
        claim_id:
          description: The claim this action made, which is what the review queue lists and what is approved
          format: int64
          type: integer
        covered:
          description: The number of findings those places hold
          format: int64
          type: integer
        ids:
          items:
            format: int64
            type: integer
          type:
            - array
            - "null"
        left:
          description: "Places of this finding this judgment did not reach: ones it did not name, and ones a decision already standing there covers"
          format: int64
          type: integer
        needs_approval:
          description: Whether a second person has to agree
          type: boolean
        recorded:
          description: The number of places it was written against
          format: int64
          type: integer
      required:
        - claim_id
        - recorded
        - covered
        - left
        - needs_approval
        - ids
      type: object
    DecisionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DecisionBody.json
          format: uri
          readOnly: true
          type: string
        claim_id:
          description: "The claim this decision is one row of: the action that wrote it, which is what the review queue lists and what is approved"
          format: int64
          type: integer
        committed_to:
          description: The date the promised work lands. Required for patch-needed and upgrade-needed, and refused with any other
          type: string
        deferred_until:
          description: The date a deferral returns. Required for a deferral
          type: string
        fixed_version:
          description: The package version whoever packages this states the fix arrived in. Required when the outcome is already-fixed, and refused with any other. Recorded and never compared against the version shipping
          type: string
        from_statement:
          description: A VEX statement this was started from, by its identifier. Recorded as a citation so a later revision to it raises an alert. It is never what the claim rests on
          format: int64
          type: integer
        id:
          description: The name for this decision in a later request
          format: int64
          type: integer
        justification:
          description: The reason it does not apply. Required when it does not
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        mitigation:
          description: The mitigation that stops it — the rule, the setting, the service that is not exposed. Required when the reason is that mitigations already exist, optional when the outcome is that this will not be fixed, and refused otherwise
          maxLength: 65536
          type: string
        needs_approval:
          description: Whether a second person has to agree before it takes effect
          type: boolean
        outcome:
          description: The outcome
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        places:
          description: The number of findings this decision covers
          format: int64
          type: integer
        reasoning:
          description: The reasoning, in markdown. Somebody else has to agree with this
          minLength: 1
          type: string
        selected_by:
          description: The narrowing behind a claim recorded as one of many. Never part of the claim itself
          type: string
        sent_back_at:
          description: The last time an approver asked for more. Empty means nobody has
          type: string
        state:
          description: The state it has reached
          enum:
            - proposed
            - approved
            - withdrawn
            - lapsed
          type: string
        upgrade_to:
          description: The version an upgrade moves to. Carried on an upgrade-needed decision, which is recorded from a component rather than from one finding
          type: string
        versions:
          description: The number of versions of the component here. More than one needs care
          format: int64
          type: integer
      required:
        - outcome
        - reasoning
      type: object
    DecisionDetail:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DecisionDetail.json
          format: uri
          readOnly: true
          type: string
        age_days:
          description: The age of the claim. An old judgment should look like one
          format: int64
          type: integer
        decision:
          $ref: "#/components/schemas/DecisionBody"
        finding:
          $ref: "#/components/schemas/FindingRefBody"
          description: The decision's subject — build, issue, component, where it sits — read from the open finding at its place. Absent where none is open there
        place:
          $ref: "#/components/schemas/PlaceBody"
        proposed_at:
          description: The moment the claim was made
          type: string
        proposed_by:
          type: string
        reasoning:
          description: The justification as it currently stands, in markdown
          type: string
      required:
        - decision
        - place
        - reasoning
        - proposed_by
        - proposed_at
        - age_days
      type: object
    DecisionsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DecisionsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/DecisionDetail"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    DeclaredKeyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DeclaredKeyBody.json
          format: uri
          readOnly: true
          type: string
        created:
          type: boolean
        item:
          $ref: "#/components/schemas/KeyBody"
      required:
        - created
        - item
      type: object
    DeclaredPersonBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DeclaredPersonBody.json
          format: uri
          readOnly: true
          type: string
        created:
          type: boolean
        item:
          $ref: "#/components/schemas/PersonBody"
      required:
        - created
        - item
      type: object
    DeclaredProductBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DeclaredProductBody.json
          format: uri
          readOnly: true
          type: string
        created:
          type: boolean
        item:
          $ref: "#/components/schemas/ProductBody"
      required:
        - created
        - item
      type: object
    DeclaredStreamBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DeclaredStreamBody.json
          format: uri
          readOnly: true
          type: string
        created:
          type: boolean
        item:
          $ref: "#/components/schemas/StreamBody"
      required:
        - created
        - item
      type: object
    DeclaredTeamBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DeclaredTeamBody.json
          format: uri
          readOnly: true
          type: string
        created:
          type: boolean
        item:
          $ref: "#/components/schemas/TeamBody"
      required:
        - created
        - item
      type: object
    DeclaredVariantBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/DeclaredVariantBody.json
          format: uri
          readOnly: true
          type: string
        created:
          type: boolean
        item:
          $ref: "#/components/schemas/VariantBody"
      required:
        - created
        - item
      type: object
    DisposedBody:
      additionalProperties: false
      properties:
        agreement_carried:
          description: Whether the agreement was carried forward from an earlier claim rather than given for this one
          type: boolean
        approved_at:
          type: string
        approved_by:
          description: The people who agreed. Two different people is the whole of the control, so both names are carried rather than a count
          type: string
        closed:
          type: string
        closed_because:
          description: The reason it closed, in the tool's terms. Only on a closed row
          enum:
            - removed
            - upgraded
            - revised
            - superseded
            - unexplained
            - invalid
            - fixed
          type: string
        closed_note:
          description: The person's own reason for closing it. Only where a person did
          type: string
        component:
          type: string
        consumer:
          description: The consumer that pulls the component in. Absent where the build holds it directly
          type: string
        due:
          type: string
        justification:
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        met:
          description: Whether the deadline was met. Answerable only for something that closed — an open row has not missed its deadline, it has not reached the end of the question
          type: boolean
        opened:
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        place:
          description: The place in the build, derived from content. It correlates two rows and names no location — consumer is the readable half
          type: string
        proposed_at:
          type: string
        proposed_by:
          type: string
        severity:
          type: string
        state:
          description: The decision state. undecided is the row every other report leaves out and the one an auditor is looking for
          enum:
            - undecided
            - waiting
            - agreed
            - lapsed
          type: string
        version:
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - component
        - place
        - state
        - opened
      type: object
    Distribution:
      additionalProperties: false
      properties:
        tlp:
          $ref: "#/components/schemas/TLP"
      type: object
    Document:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Document.json
          format: uri
          readOnly: true
          type: string
        document:
          $ref: "#/components/schemas/Meta"
        product_tree:
          $ref: "#/components/schemas/ProductTree"
        vulnerabilities:
          items:
            $ref: "#/components/schemas/Vulnerability"
          type:
            - array
            - "null"
      required:
        - document
        - product_tree
        - vulnerabilities
      type: object
    EarlierBody:
      additionalProperties: false
      properties:
        about:
          description: The component upstream version it was a claim about
          type: string
        approved_by:
          description: The person who last agreed to it, where anybody did
          type: string
        claim_id:
          format: int64
          type: integer
        decision_id:
          format: int64
          type: integer
        deferred_until:
          type: string
        ended:
          description: The reason it stopped applying
          enum:
            - lapsed
            - withdrawn
          type: string
        ended_at:
          type: string
        fixed_version:
          description: The package version the claim says the fix arrived in, where it claims one has
          type: string
        justification:
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        proposed_at:
          type: string
        proposed_by:
          type: string
        reasoning:
          description: The reasoning as it last stood, in markdown, offered back rather than thrown away
          type: string
      required:
        - decision_id
        - claim_id
        - outcome
        - proposed_by
        - proposed_at
        - ended
        - reasoning
      type: object
    Edit-commentRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Edit-commentRequest.json
          format: uri
          readOnly: true
          type: string
        body:
          minLength: 1
          type: string
      required:
        - body
      type: object
    Edit-issue-noteRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Edit-issue-noteRequest.json
          format: uri
          readOnly: true
          type: string
        body:
          description: The replacement text, in markdown
          minLength: 1
          type: string
      required:
        - body
      type: object
    ElsewhereBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        claim_id:
          format: int64
          type: integer
        decision_id:
          format: int64
          type: integer
        justification:
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        product:
          description: The product it was decided in
          type: string
        reasoning:
          type: string
      required:
        - product
        - claim_id
        - decision_id
        - outcome
        - reasoning
      type: object
    EmbargoedBody:
      additionalProperties: false
      properties:
        component:
          type: string
        disclose_at:
          description: The date the embargo ends. Reaching it discloses nothing
          type: string
        passed:
          description: Whether the date has already arrived
          type: boolean
        places:
          description: The number of findings this covers
          format: int64
          type: integer
        product:
          type: string
        severity:
          type: string
        stream:
          type: string
        summary:
          type: string
        variant:
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - component
        - product
        - stream
        - variant
        - disclose_at
        - passed
        - places
      type: object
    EndOfLifeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/EndOfLifeBody.json
          format: uri
          readOnly: true
          type: string
        "on":
          description: The date support ends, as YYYY-MM-DD, or empty to clear it
          pattern: ^(\d{4}-\d{2}-\d{2})?$
          type: string
      required:
        - "on"
      type: object
    Engine:
      additionalProperties: false
      properties:
        name:
          type: string
        version:
          type: string
      required:
        - name
      type: object
    EnteredBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/EnteredBody.json
          format: uri
          readOnly: true
          type: string
        builds:
          description: The number of builds it was recorded against
          format: int64
          type: integer
        component:
          description: The component in the build that carries it
          type: string
        due_at:
          description: The date it has to be answered by
          type: string
        identifier:
          description: The identifier this deployment filed it under, such as SONIC-2026-0001
          type: string
        places:
          description: The number of findings that opened. One per place the component sits in, in each build
          format: int64
          type: integer
        visibility:
          description: Whether it has been disclosed
          enum:
            - public
            - private
          type: string
      required:
        - identifier
        - component
        - visibility
        - builds
        - places
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    EvidenceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/EvidenceBody.json
          format: uri
          readOnly: true
          type: string
        advisory:
          description: The issue's write-up
          type: string
        aliases:
          description: Other names the same issue is known by
          items:
            type: string
          type:
            - array
            - "null"
        arrived_from:
          description: The version this was upgraded from, where the upgrade did not resolve it
          type: string
        assessed:
          description: This deployment's own rating, where somebody has said something. This is what ranks; severity is the published word
          type: string
        assigned_to:
          description: The party dealing with this, by sign-in identity. Empty means nobody, or not everywhere the same person
          type: string
        component:
          type: string
        days_left:
          description: Negative once it is overdue
          format: int64
          type: integer
        description:
          type: string
        disclose_at:
          description: The date the embargo ends. Reaching it discloses nothing by itself
          type: string
        due:
          description: The date it runs out. The earliest among its places, which is the one that makes the whole finding late
          type: string
        elsewhere:
          description: Approved claims about this same issue at this same place in another product. Evidence to read and quote, and never a decision about this product. At most five
          items:
            $ref: "#/components/schemas/ElsewhereBody"
          type:
            - array
            - "null"
        exploited:
          description: Somebody is known to be exploiting this
          type: boolean
        fix_state:
          enum:
            - fixed
            - none
            - wont-fix
            - unknown
            - mixed
          type: string
        fixed_at:
          description: The date that version became available
          type: string
        fixed_in:
          description: The version that resolves it
          type: string
        found_by:
          $ref: "#/components/schemas/MeasuredBody"
          description: "The provenance: the scanner, its version, and the vulnerability database it read at the time. Absent on something a person recorded, which no run found"
        latest_released_at:
          description: The date that version shipped
          type: string
        latest_version:
          description: The newest version the ecosystem's own index knows of
          type: string
        likelihood:
          description: Published probability of exploitation, 0 to 1
          format: double
          type: number
        likelihood_on:
          description: The day the estimate was computed for, as a date
          type: string
        likelihood_percentile:
          description: The estimate's standing among all published ones, 0 to 1
          format: double
          type: number
        links:
          items:
            $ref: "#/components/schemas/LinkBody"
          type:
            - array
            - "null"
        matched:
          enum:
            - advisory
            - identifier
          type: string
        matched_from:
          description: The source of this match
          type: string
        matched_in:
          description: The body of vulnerability data that answered, as the scanner names it — an ecosystem's own advisories against the national database's identifiers
          type: string
        matched_range:
          description: The version range this match fired on. For a distribution's package reached by identifier it is an upstream range, which names no packaging revision and so cannot see a backported fix
          type: string
        no_deadline:
          description: "The reason there is no deadline: below-the-line when this product does not consider it worth triaging, nothing-to-take when upstream has released no fix or has declined to, out-of-support when its release is past end of life or was built once. Where more than one holds, the narrowest is the one reported"
          enum:
            - below-the-line
            - nothing-to-take
            - out-of-support
          type: string
        nothing_since:
          description: Upstream has released nothing since the year this issue was named, and there is no fix. Two dates compared — it says why there is no fix, not that the project is abandoned
          type: boolean
        opened:
          description: The earliest of these places first appearing here, as a date
          type: string
        places:
          items:
            $ref: "#/components/schemas/SittingBody"
          type:
            - array
            - "null"
        previous:
          description: Decisions made at these places that lapsed or were withdrawn, newest first, with their reasoning
          items:
            $ref: "#/components/schemas/EarlierBody"
          type:
            - array
            - "null"
        recorded:
          description: Somebody recorded this here rather than a scanner reporting it. Only such a finding can be closed as fixed by hand
          type: boolean
        references:
          items:
            $ref: "#/components/schemas/ReferenceBody"
          type:
            - array
            - "null"
        routed_by:
          description: The standing rule that placed this, where one did. Empty means a person did, or nobody has
          type: string
        score:
          description: The same judgment as a number, where one is published
          format: double
          type: number
        score_kind:
          description: "The rating's rank: primary or secondary"
          type: string
        score_source:
          description: The publisher, where the report names them
          type: string
        score_version:
          description: The scoring system the number is on, as the report states it
          type: string
        severity:
          description: As the data rates it. A word
          type: string
        similar:
          description: Approved not-applicable claims about other issues at the same component and consumer, which extends can carry to this one. At most five
          items:
            $ref: "#/components/schemas/SimilarBody"
          type:
            - array
            - "null"
        standing:
          description: Live claims covering any of this finding's places, newest first. A proposed one is waiting for a second person
          items:
            $ref: "#/components/schemas/StandingClaimBody"
          type:
            - array
            - "null"
        tags:
          description: Words somebody put on this. Free text, no fixed vocabulary
          items:
            type: string
          type:
            - array
            - "null"
        undisclosed:
          description: This has not been announced. Anything said about it outside this deployment discloses it
          type: boolean
        upstream:
          description: The upstream a fork was made from, where it is one
          type: string
        vector:
          description: The score's assumptions — reachability, privilege, interaction
          type: string
        version:
          type: string
        vex:
          description: Statements from VEX documents uploaded here. Evidence, never applied
          items:
            $ref: "#/components/schemas/VexSaidBody"
          type:
            - array
            - "null"
        vulnerability:
          type: string
        weaknesses:
          description: The kind of flaw, as CWE identifiers
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - vulnerability
        - component
        - version
        - places
        - standing
        - previous
        - similar
        - elsewhere
        - vex
      type: object
    Extend-disclosureRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Extend-disclosureRequest.json
          format: uri
          readOnly: true
          type: string
        reason:
          description: The reason the date is moving
          maxLength: 65536
          minLength: 1
          type: string
        until:
          description: The date the embargo should end
          type: string
      required:
        - until
        - reason
      type: object
    FindingBody:
      additionalProperties: false
      properties:
        answered:
          description: The number of those the build has already argued do not apply
          format: int64
          type: integer
        builds:
          description: The number of builds in the selection holding this. Absent where the selection is one build
          format: int64
          type: integer
        chains:
          description: The number of distinct ways down. More than one means the pair above is one of them
          format: int64
          type: integer
        component:
          description: The component that carries it
          type: string
        consumers:
          description: The number of things pulling those in here. The build itself counts as one where anything is pulled in directly
          format: int64
          type: integer
        days_left:
          description: Negative once it is overdue. Absent where there is no deadline
          format: int64
          type: integer
        disclose_at:
          description: The date the embargo ends. Reaching it discloses nothing by itself
          type: string
        due:
          description: The date it is due. Absent where there is none, and then no_deadline gives the reason
          type: string
        ecosystem:
          description: "The kind of package, as its identifier spells it — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven and whatever else a producer emits. Read out of the identifier rather than chosen from a list, so the set is open. With the component and version it tells one row from another, which those two alone do not: one build can hold one name at one version as two components, a source repository and the package built from it"
          type: string
        exploited:
          description: Somebody is known to be exploiting this
          type: boolean
        fix_state:
          description: Upstream's answer about it
          enum:
            - fixed
            - none
            - wont-fix
            - unknown
            - mixed
          type: string
        fixed_in:
          description: The version that resolves it, where one exists
          type: string
        fold:
          description: "The row's subject: the source package at the version it was built at, in the ecosystem and distribution it came from"
          type: string
        likelihood:
          description: Published estimate that this will be exploited, 0 to 1
          format: double
          type: number
        matched:
          description: The scanner's route to this
          enum:
            - advisory
            - identifier
          type: string
        middle:
          description: The number of steps between those two
          format: int64
          type: integer
        no_deadline:
          description: "The reason there is no deadline: below-the-line when this product does not consider it worth triaging, nothing-to-take when upstream has released no fix or has declined to, out-of-support when its release is past end of life or was built once. Where more than one holds, the narrowest is the one reported"
          enum:
            - below-the-line
            - nothing-to-take
            - out-of-support
          type: string
        opened:
          description: The earliest of these places opening here, as a date. The age a deadline relates to
          type: string
        owner:
          description: The part of the product this belongs to. Absent where the inventory placed the component nowhere
          type: string
        packages:
          description: The number of binaries of the source package here. More than one means deciding on this row decides about all of them
          format: int64
          type: integer
        parent:
          description: The component that directly pulls it in, which is what a decision is about
          type: string
        places:
          description: The number of findings under this row. The bulk cap counts these, and the disposition register expands to them, so it is not a number to reconcile with the two above
          format: int64
          type: integer
        product:
          description: The product this is open in. Present only on the list that spans products
          type: string
        product_name:
          description: That product's display name, where it has one
          type: string
        score:
          description: The severity as a number, which is what the order compares
          format: double
          type: number
        score_version:
          description: The scoring system the number is on
          type: string
        sent_back:
          description: A live claim at one of these places is currently with its author, sent back for more
          type: boolean
        severity:
          description: "The rating in force: what we rate it where we have said something, and what was published otherwise. A word, not a score"
          type: string
        source:
          description: "The package this binary was built from, where the two differ. The same issue at two binaries of one source is two rows here and one piece of work everywhere else: decided once, upgraded once, routed by one rule"
          type: string
        state:
          description: "The decision state: undecided when no place has a decision of any kind, waiting when a claim stands proposed and nobody has agreed, agreed when every place is answered by a standing decision, lapsed when a decision here stopped applying and nothing replaced it. Absent where some places are approved and the rest never decided"
          enum:
            - undecided
            - waiting
            - agreed
            - lapsed
          type: string
        stream:
          description: "A branch or tag holding this, for linking to. One of them, not the only one: builds says how many there are. Absent where the selection is one build"
          type: string
        summary:
          description: The first line of what the issue says about itself, cut to fit a row. The whole of it is on the finding
          type: string
        tags:
          description: Words somebody put on this. Free text, no fixed vocabulary
          items:
            type: string
          type:
            - array
            - "null"
        undisclosed:
          description: Nothing here has been announced. Anything said about it outside this deployment discloses it
          type: boolean
        upstream:
          description: The upstream a fork was made from, where it is one
          type: string
        variant:
          description: The variant of that build
          type: string
        version:
          description: The version that ships
          type: string
        vulnerability:
          description: The issue, under the name it is most widely known by
          type: string
      required:
        - vulnerability
        - component
        - version
        - fold
        - packages
        - consumers
        - places
      type: object
    FindingDecisionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/FindingDecisionBody.json
          format: uri
          readOnly: true
          type: string
        also:
          description: Other builds of this product the same judgment covers. All of it is written together or none of it is
          items:
            $ref: "#/components/schemas/AlsoBuild"
          maxItems: 2000
          type:
            - array
            - "null"
        committed_to:
          description: The date a promised backport lands. Required for patch-needed and refused with any other
          type: string
        deferred_until:
          description: Required when it is deferred. A date, as 2026-03-31
          type: string
        extends:
          description: An approved claim at the same component and consumer to carry to this issue. The outcome and justification must match it; the new claim is recorded as an extension of it and still needs a second person
          format: int64
          type: integer
        fixed_version:
          description: The package version whoever packages this states the fix arrived in. Required when the outcome is already-fixed, and refused with any other
          type: string
        from_statement:
          description: A VEX statement this was started from, by its identifier. Recorded as a citation so a later revision to it raises an alert. It is never what the claim rests on
          format: int64
          type: integer
        justification:
          description: The reason it does not apply. Required when it does not
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        mitigation:
          description: The mitigation that stops it — the rule, the setting, the service that is not exposed. Required when the reason is that mitigations already exist, optional when the outcome is that this will not be fixed, and refused otherwise
          maxLength: 65536
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - patch-needed
          type: string
        places:
          description: The places this covers, as the finding names them. Omit for all of them
          items:
            maxLength: 191
            type: string
          maxItems: 2000
          type:
            - array
            - "null"
        reasoning:
          description: The reasoning
          minLength: 1
          type: string
        remaining:
          description: Decide only the places nothing currently stands at, and leave the rest as they are. For applying a decision to another build, where some of its places are already reached by lookup
          type: boolean
      required:
        - outcome
        - reasoning
      type: object
    FindingRefBody:
      additionalProperties: false
      properties:
        component:
          type: string
        decided:
          description: The number of those this claim covers
          format: int64
          type: integer
        description:
          description: The first four hundred characters of what the report says, as plain text
          type: string
        exploited:
          type: boolean
        fix_state:
          enum:
            - fixed
            - none
            - wont-fix
            - unknown
            - mixed
          type: string
        fixed_in:
          type: string
        owner:
          description: The part of the product this belongs to
          type: string
        parent:
          description: The component that directly pulls it in, which is what the decision is about
          type: string
        places:
          description: The number of places the issue sits at in that component in that build
          format: int64
          type: integer
        product:
          description: The build to link to, by product, branch or tag, and variant
          type: string
        score:
          format: double
          type: number
        score_version:
          description: The scoring system the number is on
          type: string
        severity:
          description: Our rating where one stands, else as published
          type: string
        stream:
          type: string
        variant:
          type: string
        version:
          description: The version that ships
          type: string
        vulnerability:
          description: The issue, under the name it is most widely known by
          type: string
      required:
        - product
        - stream
        - variant
        - vulnerability
        - component
        - version
        - places
        - decided
      type: object
    FindingsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/FindingsOutputBody.json
          format: uri
          readOnly: true
          type: string
        floor:
          description: The line they are below
          type: string
        hidden:
          description: Findings this product does not consider worth triaging, kept out of the list. Still recorded and still counted
          format: int64
          type: integer
        items:
          items:
            $ref: "#/components/schemas/FindingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    Generator:
      additionalProperties: false
      properties:
        date:
          format: date-time
          type: string
        engine:
          $ref: "#/components/schemas/Engine"
      required:
        - engine
        - date
      type: object
    Get-disposition-registerResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Get-disposition-registerResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/DisposedBody"
          type:
            - array
            - "null"
        measured:
          $ref: "#/components/schemas/MeasuredBody"
          description: The tools the register was measured with. Absent where nothing has been uploaded to the build
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    Get-release-trendResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Get-release-trendResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ReleasePointBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    GrantBody:
      additionalProperties: false
      properties:
        everywhere:
          description: Hold it across every product, including products declared later. The product is then omitted
          type: boolean
        product:
          description: The product the role is held against. Omit it and set everywhere instead to hold it across the estate
          type: string
        role:
          description: The rights it carries
          enum:
            - approver
            - assigner
            - public-read
            - private-read
            - public-triage
            - private-triage
          type: string
      required:
        - role
      type: object
    GrownBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        claim_id:
          format: int64
          type: integer
        covered:
          description: The claim's reach when it was agreed to
          format: int64
          type: integer
        covers_now:
          description: Its reach now, arrived at by matching rather than by anybody acting
          format: int64
          type: integer
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
      required:
        - claim_id
        - approved_by
        - approved_at
        - outcome
        - covered
        - covers_now
      type: object
    Hand-back-assignmentsRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Hand-back-assignmentsRequest.json
          format: uri
          readOnly: true
          type: string
        to:
          description: The party taking it on. Omit to return it to nobody
          type: string
      type: object
    Hand-back-assignmentsResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Hand-back-assignmentsResponse.json
          format: uri
          readOnly: true
          type: string
        moved:
          format: int64
          type: integer
      required:
        - moved
      type: object
    HeldBody:
      additionalProperties: false
      properties:
        effective:
          description: Whether this grants anything right now
          type: boolean
        everywhere:
          description: Held across every product, including products declared later
          type: boolean
        product:
          description: The product the role is held against, by the name that addresses it. Absent where it is held across every product
          type: string
        product_display_name:
          description: That product's display name, where it was declared with one
          type: string
        role:
          description: The rights it carries
          enum:
            - approver
            - assigner
            - public-read
            - private-read
            - public-triage
            - private-triage
          type: string
        source:
          description: Whether an administrator assigned this or a group derived it
          enum:
            - assigned
            - derived
          type: string
      required:
        - role
        - effective
      type: object
    HeldChangeBody:
      additionalProperties: false
      properties:
        about:
          description: The subject, as the trail records it
          type: string
        at:
          type: string
        by:
          description: The person who made the change
          type: string
        now:
          description: The role held after. Absent means a withdrawal
          type: string
        was:
          description: The role held before. Absent means they held none
          type: string
      required:
        - at
        - by
        - about
      type: object
    HolderBody:
      additionalProperties: false
      properties:
        identity:
          description: The name used when handing work over
          type: string
        kind:
          description: Whether this is a person or a team. Work is held by a party, and both are one
          enum:
            - person
            - team
          type: string
        name:
          description: The label, which is the spelling somebody typed where there is one
          type: string
      required:
        - kind
        - identity
        - name
      type: object
    HoldingBody:
      additionalProperties: false
      properties:
        open:
          description: "Pieces of work assigned to them: an issue in a component in a product"
          format: int64
          type: integer
        overdue:
          description: The number of those pieces past their deadline
          format: int64
          type: integer
        person:
          description: The holder, by the name they are shown under. A person or a team
          type: string
        places:
          description: The number of findings those cover, across every build
          format: int64
          type: integer
        team:
          description: This is a team's queue rather than one person's work
          type: boolean
      required:
        - person
        - open
        - places
        - overdue
      type: object
    IdentificationHelper:
      additionalProperties: false
      properties:
        purl:
          type: string
      type: object
    Info:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Info.json
          format: uri
          readOnly: true
          type: string
        commit:
          description: Git commit the binary was built from
          type: string
        date:
          description: Build timestamp, RFC 3339
          type: string
        go:
          description: Go toolchain that produced the binary
          type: string
        version:
          description: Release version, or "dev" for an unreleased build
          type: string
      required:
        - version
        - commit
        - date
        - go
      type: object
    InheritedBody:
      additionalProperties: false
      properties:
        component:
          type: string
        decision:
          format: int64
          type: integer
        deferred_days:
          description: The total this has already been put off for, across every line it has been carried through
          format: int64
          type: integer
        now:
          description: The new line's contents
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        reasoning:
          description: The old words, to start from rather than start without
          type: string
        vulnerability:
          type: string
        was:
          description: The version the claim was made against
          type: string
      required:
        - decision
        - vulnerability
        - component
        - outcome
        - was
        - now
        - reasoning
      type: object
    Inside:
      additionalProperties: false
      properties:
        "@id":
          type: string
      required:
        - "@id"
      type: object
    IssuanceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/IssuanceBody.json
          format: uri
          readOnly: true
          type: string
        digest:
          description: A digest of what went out, so that what is published and what we would generate stay answerable against each other
          type: string
        issued_at:
          type: string
        summary:
          type: string
        version:
          description: The issuance number, counting from one. It is what the next document's version says
          format: int64
          type: integer
      required:
        - version
        - digest
        - issued_at
      type: object
    Issue:
      additionalProperties: false
      properties:
        aliases:
          items:
            type: string
          type:
            - array
            - "null"
        name:
          type: string
      required:
        - name
      type: object
    IssueOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/IssueOutputBody.json
          format: uri
          readOnly: true
          type: string
        aliases:
          description: Every other name it answers to
          items:
            type: string
          type:
            - array
            - "null"
        description:
          type: string
        exploited:
          type: boolean
        items:
          items:
            $ref: "#/components/schemas/SightingBody"
          type:
            - array
            - "null"
        products:
          description: The number of your products carrying it
          format: int64
          type: integer
        score:
          format: double
          type: number
        score_version:
          description: The scoring system the number is on
          type: string
        severity:
          type: string
        total:
          format: int64
          type: integer
        vulnerability:
          type: string
      required:
        - vulnerability
        - items
        - total
        - products
      type: object
    Issued:
      additionalProperties: false
      properties:
        system_name:
          type: string
        text:
          type: string
      required:
        - system_name
        - text
      type: object
    Issuer:
      additionalProperties: false
      properties:
        category:
          type: string
        name:
          type: string
        namespace:
          type: string
      required:
        - category
        - name
        - namespace
      type: object
    Item:
      additionalProperties: false
      properties:
        stream:
          description: A branch or a tag
          minLength: 1
          type: string
        variant:
          description: The way that line is built
          minLength: 1
          type: string
      required:
        - stream
        - variant
      type: object
    Item1:
      additionalProperties: false
      properties:
        stream:
          minLength: 1
          type: string
        variant:
          minLength: 1
          type: string
      required:
        - stream
        - variant
      type: object
    JudgedBody:
      additionalProperties: false
      properties:
        approvals:
          items:
            $ref: "#/components/schemas/AgreedBody"
          type:
            - array
            - "null"
        component:
          type: string
        consumer:
          description: The consumer that pulls the component in. Absent where the build holds it directly
          type: string
        deferred_until:
          type: string
        ended_at:
          description: The moment it stopped applying — withdrawn, or lapsed because the code moved
          type: string
        fixed_version:
          description: The package version the claim says the fix arrived in, where it claims one has. What somebody auditing an already-fixed claim checks against the packager's own record
          type: string
        id:
          format: int64
          type: integer
        issue:
          description: The vulnerability, under the name it is filed here
          type: string
        justification:
          description: The recognized reason it does not apply
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        mitigation:
          description: The mitigation a holder can apply. Recorded where the reason is that mitigations already exist, or the outcome is that this will not be fixed, and refused otherwise. Nothing here notices a control being removed, so this is the record somebody checks
          type: string
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        product:
          type: string
        proposed_at:
          type: string
        proposed_by:
          type: string
        reasoning:
          description: The words the standing agreement was given for. Editing them withdraws the agreement, so this and what was agreed to cannot drift apart
          type: string
        standing:
          description: Whether it applies now. A judgment can be approved and no longer standing — the code moved out from under it
          type: boolean
        state:
          enum:
            - proposed
            - approved
            - withdrawn
            - lapsed
          type: string
        two_people:
          description: Whether somebody other than the proposer has a standing agreement on it
          type: boolean
        version:
          type: string
      required:
        - id
        - issue
        - product
        - component
        - outcome
        - reasoning
        - state
        - standing
        - proposed_by
        - proposed_at
        - approvals
        - two_people
      type: object
    KeyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/KeyBody.json
          format: uri
          readOnly: true
          type: string
        created_at:
          description: The date it was issued. A pipeline key does not expire, so this is the only thing that dates it
          type: string
        last_used_at:
          description: The last time it sent something
          type: string
        name:
          description: The credential's purpose
          maxLength: 191
          minLength: 1
          type: string
        product:
          description: The product it may send scans for, by the name that addresses it. Always required
          minLength: 1
          type: string
        product_display_name:
          description: That product's display name, where it was declared with one
          type: string
        secret:
          description: Shown once, at creation. It is stored hashed and cannot be shown again
          type: string
        stream:
          description: Optionally, the one release it may send for
          type: string
        variant:
          description: Optionally, the one variant it may send for
          type: string
        withdrawn:
          description: Whether it has been withdrawn
          type: boolean
      required:
        - name
        - product
      type: object
    LapsedApprovalBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        claim_id:
          format: int64
          type: integer
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        product:
          type: string
        rows:
          format: int64
          type: integer
      required:
        - claim_id
        - approved_by
        - approved_at
        - product
        - outcome
        - rows
      type: object
    LateBody:
      additionalProperties: false
      properties:
        assigned_to:
          description: Empty means nobody, or not everywhere the same person
          type: string
        component:
          type: string
        days_left:
          description: Negative once it is overdue
          format: int64
          type: integer
        due:
          description: The date it is due
          type: string
        exploited:
          type: boolean
        places:
          description: The number of places in that build this sits at
          format: int64
          type: integer
        product:
          type: string
        severity:
          type: string
        stream:
          type: string
        variant:
          type: string
        version:
          description: The version, so a link to the finding can name it — a build ships a name at more than one version often enough that a link without it cannot be resolved
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - component
        - product
        - stream
        - variant
        - places
        - due
        - days_left
      type: object
    LinkBody:
      additionalProperties: false
      properties:
        name:
          description: The destination — the issue's record, a distribution's answer about it, or the package's own page
          type: string
        url:
          type: string
      required:
        - url
        - name
      type: object
    List-administrative-changesResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-administrative-changesResponse.json
          format: uri
          readOnly: true
          type: string
        from:
          type: string
        items:
          items:
            $ref: "#/components/schemas/ChangeBody"
          type:
            - array
            - "null"
        to:
          type: string
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    List-assignedResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-assignedResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/UnassignedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    List-attachmentsResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-attachmentsResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/AttachmentBody"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    List-auditResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-auditResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/JudgedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    List-carried-patchesResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-carried-patchesResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/CarriedClaimBody"
          type:
            - array
            - "null"
        total:
          description: The total, so a page says what it is a page of
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    List-fix-bundlesResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-fix-bundlesResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/BundleBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    List-issues-at-componentResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-issues-at-componentResponse.json
          format: uri
          readOnly: true
          type: string
        cap:
          format: int64
          type: integer
        findings:
          format: int64
          type: integer
        items:
          items:
            $ref: "#/components/schemas/AtComponentBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
        - findings
        - cap
      type: object
    List-set-aside-workResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-set-aside-workResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/SetAsideBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
        waiting:
          items:
            $ref: "#/components/schemas/QueuedBody"
          type:
            - array
            - "null"
      required:
        - items
        - total
        - waiting
      type: object
    List-team-assignedResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-team-assignedResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/UnassignedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    List-unassignedResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/List-unassignedResponse.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/UnassignedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    ListBodyAdvisoryListedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyAdvisoryListedBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/AdvisoryListedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyApprovalBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyApprovalBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ApprovalBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyAssessmentBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyAssessmentBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/AssessmentBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyBindingBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyBindingBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/BindingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyCollaboratorBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyCollaboratorBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/CollaboratorBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyCommentBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyCommentBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/CommentBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyEmbargoedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyEmbargoedBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/EmbargoedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyHolderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyHolderBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/HolderBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyHoldingBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyHoldingBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/HoldingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyIssuanceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyIssuanceBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/IssuanceBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyKeyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyKeyBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/KeyBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyLateBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyLateBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/LateBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyMentionableBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyMentionableBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/MentionableBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyMovementBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyMovementBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/MovementBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyNoteBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyNoteBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/NoteBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyOutboundBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyOutboundBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/OutboundBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyPendingMovementBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyPendingMovementBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/PendingMovementBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyPerBuildBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyPerBuildBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/PerBuildBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyPersonBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyPersonBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/PersonBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyPlannedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyPlannedBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/PlannedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyPointBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyPointBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/PointBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyProductBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyProductBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ProductBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyProviderBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyProviderBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ProviderBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyReleaseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyReleaseBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ReleaseBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyRepeatBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyRepeatBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/RepeatBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyRevisionBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyRevisionBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/RevisionBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyRuleBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyRuleBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/RuleBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodySavedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodySavedBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/SavedBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodySettingBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodySettingBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/SettingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyStreamBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyStreamBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/StreamBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyString:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyString.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            type: string
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyTeamBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyTeamBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/TeamBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyTokenBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyTokenBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/TokenBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyVEXIssuanceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyVEXIssuanceBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/VEXIssuanceBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyVariantBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyVariantBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/VariantBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    ListBodyWasSaidBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ListBodyWasSaidBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/WasSaidBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
      type: object
    MatchBody:
      additionalProperties: false
      properties:
        here:
          description: This is another version in the same build, not another build
          type: boolean
        places:
          description: The number of places it sits at there
          format: int64
          type: integer
        stream:
          type: string
        variant:
          type: string
        version:
          description: The version that build ships under this name — pass it as ?version= when applying a decision there
          type: string
      required:
        - stream
        - variant
        - places
      type: object
    MeasuredBody:
      additionalProperties: false
      properties:
        built_at:
          description: The build time it describes
          type: string
        database_version:
          description: The vulnerability database it read
          type: string
        document:
          description: The inventory that was read, as the receipt names it
          format: int64
          type: integer
        document_at:
          description: The address of the inventory that was read. Absent where its contents were let go
          type: string
        document_hash:
          description: The hash of the inventory as it arrived
          type: string
        document_held:
          description: Whether the inventory itself is still here
          type: boolean
        ran_at:
          description: The moment that run finished
          type: string
        ran_here:
          description: We ran the scanner, rather than the build sending what its own found
          type: boolean
        run:
          description: The scanner run these came from
          format: int64
          type: integer
        scan:
          description: The upload the build's contents came from, as the receipt names it
          format: int64
          type: integer
        scan_hash:
          description: The hash of what was uploaded
          type: string
        scanner:
          description: The scanner that produced the findings
          type: string
        scanner_version:
          type: string
      required:
        - scanner
      type: object
    MeasuresBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MeasuresBody.json
          format: uri
          readOnly: true
          type: string
        capped:
          description: The ceiling was reached, so the spans describe the most recent part of the window rather than all of it
          type: boolean
        sampled:
          description: The number of observations behind the two spans
          format: int64
          type: integer
        sent_back:
          description: "Claims an approver asked more of in the window. Counted for the deployment rather than per person: the record holds that a claim was sent back and not by whom"
          format: int64
          type: integer
        since:
          type: string
        throughput:
          description: Each person's throughput, most first
          items:
            $ref: "#/components/schemas/WorkedBody"
          type:
            - array
            - "null"
        time_to_agree:
          description: The wait for a second person, per severity
          items:
            $ref: "#/components/schemas/SpreadBody"
          type:
            - array
            - "null"
        time_to_decide:
          description: The wait before anybody proposed anything, per severity
          items:
            $ref: "#/components/schemas/SpreadBody"
          type:
            - array
            - "null"
        until:
          type: string
      required:
        - since
        - until
        - time_to_decide
        - time_to_agree
        - throughput
        - sent_back
        - sampled
      type: object
    MentionableBody:
      additionalProperties: false
      properties:
        identity:
          description: The name written after the @
          type: string
        name:
          description: The label shown while choosing
          type: string
      required:
        - identity
        - name
      type: object
    MentionsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MentionsBody.json
          format: uri
          readOnly: true
          type: string
        not_notified:
          description: Names written after an @ that reached nobody. Either no such person is recorded, or they cannot read what the text is about — deliberately not said which
          items:
            type: string
          type:
            - array
            - "null"
      type: object
    Meta:
      additionalProperties: false
      properties:
        category:
          type: string
        csaf_version:
          type: string
        distribution:
          $ref: "#/components/schemas/Distribution"
        lang:
          type: string
        notes:
          items:
            $ref: "#/components/schemas/Note"
          type:
            - array
            - "null"
        publisher:
          $ref: "#/components/schemas/Issuer"
        references:
          items:
            $ref: "#/components/schemas/Reference"
          type:
            - array
            - "null"
        title:
          type: string
        tracking:
          $ref: "#/components/schemas/Tracking"
      required:
        - category
        - csaf_version
        - title
        - publisher
        - tracking
      type: object
    ModeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ModeBody.json
          format: uri
          readOnly: true
          type: string
        mode:
          description: Whether an administrator assigns roles or provider groups derive them
          enum:
            - direct
            - group-bound
          type: string
      required:
        - mode
      type: object
    MovementBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/MovementBody.json
          format: uri
          readOnly: true
          type: string
        act:
          description: Which act this was. An extension ends the embargo later, a shortening ends it sooner
          enum:
            - extension
            - shortening
          type: string
        approved_at:
          type: string
        approved_by:
          type: string
        asked_at:
          type: string
        asked_by:
          type: string
        id:
          format: int64
          type: integer
        in_force:
          type: boolean
        needs_approval:
          description: Whether a second person had to agree
          type: boolean
        reason:
          type: string
        until:
          description: The end that was asked for
          type: string
        was:
          description: The embargo's previous end
          type: string
      required:
        - id
        - act
        - was
        - until
        - reason
        - asked_by
        - asked_at
        - needs_approval
        - in_force
      type: object
    Named:
      additionalProperties: false
      properties:
        name:
          type: string
        product_id:
          type: string
        product_identification_helper:
          $ref: "#/components/schemas/IdentificationHelper"
      required:
        - name
        - product_id
      type: object
    NeighborBody:
      additionalProperties: false
      properties:
        beneath:
          description: Open findings in everything under it, including on it. A container holds none of its own, so this is the number that says whether a branch is worth opening
          format: int64
          type: integer
        beneath_by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: The same number by how the issues were rated, which is what says whether a branch is worth opening. 'unrated' is what nobody scored, and the bands sum to beneath
          type: object
        children:
          description: The number of components it pulls in. Zero means nothing to open
          format: int64
          type: integer
        component:
          type: string
        ecosystem:
          description: The kind of package this is, as its identifier spells it. Send it back where a build holds one name at one version as two components
          type: string
        findings:
          description: Open findings against this component itself
          format: int64
          type: integer
        version:
          type: string
      required:
        - component
        - version
        - findings
        - beneath
        - children
      type: object
    Note:
      additionalProperties: false
      properties:
        category:
          type: string
        text:
          type: string
        title:
          type: string
      required:
        - category
        - text
      type: object
    Note-on-issueRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Note-on-issueRequest.json
          format: uri
          readOnly: true
          type: string
        body:
          description: The text, in markdown
          minLength: 1
          type: string
      required:
        - body
      type: object
    NoteBody:
      additionalProperties: false
      properties:
        body:
          description: The text, in markdown
          type: string
        edited_at:
          description: The moment the author last changed it, where they have
          type: string
        id:
          format: int64
          type: integer
        written_at:
          type: string
        written_by:
          description: The author
          type: string
      required:
        - id
        - body
        - written_by
        - written_at
      type: object
    NoteWritten:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/NoteWritten.json
          format: uri
          readOnly: true
          type: string
        id:
          format: int64
          type: integer
        not_notified:
          description: Names written after an @ that reached nobody. Either no such person is recorded, or they cannot read what the note is about — deliberately not said which
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - id
      type: object
    NotificationBody:
      additionalProperties: false
      properties:
        about:
          description: A condition's subject. Absent for an event
          type: string
        at:
          description: The moment it was recorded
          type: string
        body:
          description: The message. It describes the moment it was written rather than the world now
          type: string
        id:
          description: The name for this when acknowledging it
          format: int64
          type: integer
        kind:
          description: "The kind, as a word: assigned, sent-back, build-quiet"
          type: string
        lifetime:
          description: An event happened once and is acknowledged; a condition holds until what it is about changes
          enum:
            - event
            - condition
          type: string
        link:
          description: The destination, where there is somewhere to go
          type: string
      required:
        - id
        - kind
        - lifetime
        - body
        - at
      type: object
    NotificationsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/NotificationsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/NotificationBody"
          type:
            - array
            - "null"
        total:
          description: The number waiting on you
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    OursOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/OursOutputBody.json
          format: uri
          readOnly: true
          type: string
        complete:
          description: Whether this covers everything you hold in this build. False where you hold work on more components than this assembles, and then the counts under-report
          type: boolean
        items:
          items:
            $ref: "#/components/schemas/UpwardBody"
          type:
            - array
            - "null"
      required:
        - items
        - complete
      type: object
    OutboundBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/OutboundBody.json
          format: uri
          readOnly: true
          type: string
        because:
          description: The reason the last one failed, where one did
          type: string
        failing:
          description: The number being retried or given up on
          format: int64
          type: integer
        kind:
          description: The notifications that go here, or * for all of them
          type: string
        name:
          description: The name, so a log line and a screen can use it
          type: string
        sent:
          description: The number of things delivered there
          format: int64
          type: integer
        url:
          type: string
      required:
        - name
        - kind
        - url
        - sent
        - failing
      type: object
    OutlierBody:
      additionalProperties: false
      properties:
        decision_id:
          description: A row of the claim about this issue, to set aside when approving
          format: int64
          type: integer
        description:
          description: The first two hundred characters of what the report says
          type: string
        exploited:
          type: boolean
        fixed_in:
          type: string
        severity:
          type: string
        vulnerability:
          type: string
        why:
          description: The signals that made it stand out
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - decision_id
        - vulnerability
        - why
      type: object
    OutliersBody:
      additionalProperties: false
      properties:
        exploited:
          description: Issues in the set known to be exploited
          format: int64
          type: integer
        fixable:
          description: Issues a fix is available for
          format: int64
          type: integer
        rows:
          description: The issues that stood out, exploited first and then by severity, at most twenty
          items:
            $ref: "#/components/schemas/OutlierBody"
          type:
            - array
            - "null"
        severe:
          description: Issues rated critical or high
          format: int64
          type: integer
        unmatched:
          description: Issues whose description does not carry the term the set was narrowed by
          format: int64
          type: integer
      required:
        - exploited
        - severe
        - fixable
        - unmatched
        - rows
      type: object
    OverPeriodRateBodyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/OverPeriodRateBodyBody.json
          format: uri
          readOnly: true
          type: string
        from:
          description: The first day of the period. Absent where it runs from the beginning
          type: string
        items:
          items:
            $ref: "#/components/schemas/RateBody"
          type:
            - array
            - "null"
        to:
          description: The day it ends, which is not itself in it
          type: string
        total:
          format: int64
          type: integer
      required:
        - items
        - to
      type: object
    OverPeriodSpentBodyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/OverPeriodSpentBodyBody.json
          format: uri
          readOnly: true
          type: string
        from:
          description: The first day of the period. Absent where it runs from the beginning
          type: string
        items:
          items:
            $ref: "#/components/schemas/SpentBody"
          type:
            - array
            - "null"
        to:
          description: The day it ends, which is not itself in it
          type: string
        total:
          format: int64
          type: integer
      required:
        - items
        - to
      type: object
    OverPeriodWentBodyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/OverPeriodWentBodyBody.json
          format: uri
          readOnly: true
          type: string
        from:
          description: The first day of the period. Absent where it runs from the beginning
          type: string
        items:
          items:
            $ref: "#/components/schemas/WentBody"
          type:
            - array
            - "null"
        to:
          description: The day it ends, which is not itself in it
          type: string
        total:
          format: int64
          type: integer
      required:
        - items
        - to
      type: object
    OverviewOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/OverviewOutputBody.json
          format: uri
          readOnly: true
          type: string
        builds:
          items:
            $ref: "#/components/schemas/BuildStandingBody"
          type:
            - array
            - "null"
        display_name:
          type: string
        end_of_life:
          description: The date the product goes out of support, where one is set
          type: string
        name:
          type: string
        open:
          format: int64
          type: integer
        overdue:
          format: int64
          type: integer
        retired:
          description: Whether the product has been taken out of use. Nothing filed against it is affected and no new scan is accepted
          type: boolean
        triage_floor:
          description: The least severity this product triages, where the product states one of its own
          type: string
        undecided:
          format: int64
          type: integer
        waiting:
          description: Claims about this product waiting for a second person
          format: int64
          type: integer
      required:
        - name
        - builds
        - open
        - overdue
        - undecided
        - waiting
      type: object
    PairingBody:
      additionalProperties: false
      properties:
        approver:
          type: string
        claims:
          format: int64
          type: integer
        proposer:
          type: string
        rows:
          format: int64
          type: integer
        share:
          description: The share of everything agreed to here that ran between these two, as a percentage of rows
          format: int64
          type: integer
      required:
        - proposer
        - approver
        - claims
        - rows
        - share
      type: object
    PendingMovementBody:
      additionalProperties: false
      properties:
        act:
          description: Which act is being asked for
          enum:
            - extension
            - shortening
          type: string
        asked_at:
          type: string
        by:
          description: The person who asked
          type: string
        days:
          description: How far the date moves, in days, whichever way it moves
          format: int64
          type: integer
        id:
          format: int64
          type: integer
        mine:
          description: You asked for this, so you may not be the second person
          type: boolean
        product:
          type: string
        reason:
          type: string
        until:
          description: The end being asked for
          type: string
        vulnerability:
          type: string
        was:
          description: The embargo's end now
          type: string
      required:
        - id
        - product
        - vulnerability
        - act
        - was
        - until
        - days
        - by
        - asked_at
        - reason
      type: object
    PerBuildBody:
      additionalProperties: false
      properties:
        by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: Everything open here by how it was rated. 'unrated' is what nobody scored, and the bands sum to the issue count
          type: object
        committed_to:
          description: The date the work promised here is due
          format: date-time
          type: string
        consumers:
          description: The number of things pulling it in here
          format: int64
          type: integer
        due_at:
          description: The earliest deadline among what is open here. A commitment at or before it needs no approval; past it a second person agrees, because that defers the worst thing it covers
          format: date-time
          type: string
        ecosystem:
          description: The ecosystem the identifier names, read out of it rather than stored
          type: string
        exploited:
          description: Whether any of what is open here is known to be exploited, which outranks everything else about it
          type: boolean
        first_seen:
          description: The moment a scan of this deployment first reported the component
          format: date-time
          type: string
        fixable:
          description: The number of open findings any version fixes, counted once per issue. What is left needs a judgment rather than an upgrade, and a record naming several fixed versions is still one issue
          format: int64
          type: integer
        issues:
          description: Distinct vulnerabilities open against it here
          format: int64
          type: integer
        newest_released_at:
          description: The date that version shipped, where the index said
          format: date-time
          type: string
        newest_version:
          description: The newest version the ecosystem's index knows of. Absent where no index is asked, which is every distribution package
          type: string
        package_page_name:
          description: The name for that address on screen — which distribution's or which index's record it is, since a reader choosing between two needs to know which kind of source each is
          type: string
        package_page_url:
          description: The address this package is published at, worked out from its identifier. Absent for a kind of package this has no address for, which is what a private registry, a vendored fork and a distribution with no package browser all look like
          type: string
        places:
          description: The number of times those sit somewhere in this build. What the bulk cap is measured against
          format: int64
          type: integer
        project_url:
          description: The address the index gives for where the package is developed. Absent where it does not say, in which case an address can still be built from the identifier
          type: string
        purl:
          description: The package identifier this build ships it under
          type: string
        stream:
          type: string
        summary:
          description: One line saying what the package is, as its ecosystem's index states it. Absent where no index serves one — the Go module protocol has no such field — and where no index is asked, which is every distribution package
          type: string
        supplier:
          description: The supplier the scan named — a distribution, a vendor, a project. From the inventory rather than from an index, and absent for plenty of it
          type: string
        upgrade_to:
          description: The version somebody has committed to moving this build to
          type: string
        upgrades:
          description: "Versions upstream released that would close some of what is open here, most-closing first. Per build, because the answer differs by build: a stream on a maintained older line and a stream that has moved on have different targets"
          items:
            $ref: "#/components/schemas/UpgradeBody"
          type:
            - array
            - "null"
        variant:
          type: string
        version:
          description: The version this build ships
          type: string
      required:
        - stream
        - variant
        - version
        - exploited
        - fixable
        - first_seen
        - issues
        - consumers
        - places
      type: object
    PersonBody:
      additionalProperties: false
      properties:
        admin:
          description: Whether they administer this deployment
          type: boolean
        audits:
          description: Whether they may read this deployment's own records. It grants no product's findings or decisions
          type: boolean
        deactivated_at:
          description: The date they left. Absent means they may still sign in
          type: string
        display_name:
          description: The label shown instead of the identity
          type: string
        email:
          description: The address they are reached at outside the application
          type: string
        email_source:
          description: The source that last decided it. A provider's may be refreshed by a later sign-in; one recorded here is never overwritten
          enum:
            - provider
            - recorded
          type: string
        holds:
          items:
            $ref: "#/components/schemas/HeldBody"
          type:
            - array
            - "null"
        identity:
          description: The name for them here
          maxLength: 191
          minLength: 1
          type: string
        sees_nothing:
          description: Every role they hold is a capability, so they reach no product. A capability is bounded by what its holder may read, so on its own it grants nothing
          type: boolean
        signs_in_by:
          items:
            $ref: "#/components/schemas/SignInBody"
          type:
            - array
            - "null"
      required:
        - identity
      type: object
    PersonRecordBody:
      additionalProperties: false
      properties:
        approved:
          description: Claims they agreed to that still stand
          format: int64
          type: integer
        last_approved_at:
          description: Absent where they never have
          type: string
        last_proposed_at:
          description: Absent where they never have
          type: string
        proposed:
          description: Claims they argued
          format: int64
          type: integer
        withdrawn:
          description: Claims they agreed to and later took back
          format: int64
          type: integer
      required:
        - proposed
        - approved
        - withdrawn
      type: object
    PlaceBody:
      additionalProperties: false
      properties:
        place:
          description: The place in the build, as the findings list gives it
          minLength: 1
          type: string
        product:
          minLength: 1
          type: string
        vulnerability:
          description: The issue, by any name it is known under
          minLength: 1
          type: string
      required:
        - product
        - vulnerability
        - place
      type: object
    PlanUpgradeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/PlanUpgradeBody.json
          format: uri
          readOnly: true
          type: string
        builds:
          description: The releases this is promised for. A stream staying on a maintained older line takes its own promise with its own version
          items:
            $ref: "#/components/schemas/BuildName"
          minItems: 1
          type:
            - array
            - "null"
        by:
          description: The date the work lands, as 2026-03-31. A missed target is measured against it
          type: string
        person:
          description: The party carrying it, by sign-in identity. Leave both out and it is held by nobody
          type: string
        reasoning:
          description: "The reason this is the answer here. Required like every other judgment: what an auditor reads is the reasoning, not the outcome"
          minLength: 1
          type: string
        team:
          description: "A team carrying it, by name. A queue rather than a holding: it stays unheld until somebody takes it"
          type: string
        to:
          description: The version this moves to, as whoever packages it writes it
          maxLength: 191
          minLength: 1
          type: string
      required:
        - to
        - by
        - builds
        - reasoning
      type: object
    PlannedBody:
      additionalProperties: false
      properties:
        by:
          description: The date the promise named, as a date. Absent where the commitment is intent rather than a promise
          type: string
        claim_id:
          description: "The claim that argued for it, where one did: its reasoning, its approval and its comments"
          format: int64
          type: integer
        components:
          items:
            type: string
          type:
            - array
            - "null"
        declared_at:
          description: The moment the commitment was made, which is what this release has been waiting since
          type: string
        fold:
          description: "The upgrade's own key: the source package at the version it was built at, in the ecosystem and distribution it came from"
          type: string
        from:
          type: string
        held_by:
          description: The party carrying it, where one party holds all of what is still open under it. An upgrade split between two is nobody's
          type: string
        issues:
          description: "Distinct issues still open under this upgrade here, which is what it would close. Nothing is declared done by hand: a build is clear when it stops holding them"
          format: int64
          type: integer
        places:
          description: The number of findings those issues sit at
          format: int64
          type: integer
        state:
          description: The promise state. 'landed' is nothing left open under it here, which the scans say; 'lapsed' is the date past with work outstanding. Nobody sets this
          enum:
            - planned
            - landed
            - lapsed
          type: string
        to:
          description: The version it moves to, as the commitment recorded it
          type: string
        upstream:
          description: "The name: the source package"
          type: string
      required:
        - fold
        - upstream
        - from
        - to
        - components
        - issues
        - places
        - declared_at
        - state
      type: object
    PlannedUpgradeBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/PlannedUpgradeBody.json
          format: uri
          readOnly: true
          type: string
        claim_id:
          description: The one claim the whole upgrade was recorded under
          format: int64
          type: integer
        components:
          description: The number of packages of the source it reached. Naming any binary of a source package reaches all of them, because they move together
          format: int64
          type: integer
        decisions:
          description: Places answered
          format: int64
          type: integer
        held:
          description: Findings handed to whoever is carrying this. Product-wide, like every assignment, because who moves the package on one release is who moves it on the next
          format: int64
          type: integer
        issues:
          description: Distinct vulnerabilities it covers
          format: int64
          type: integer
        targets:
          description: Build-level intents recorded, which is what the pending upgrades screen reads
          format: int64
          type: integer
        waiting:
          description: Whether this needs a second person. True when the date is past the earliest deadline among what it covers, because that defers the worst thing in it
          type: boolean
      required:
        - claim_id
        - decisions
        - issues
        - targets
        - components
        - waiting
      type: object
    Point-claim-elsewhereRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Point-claim-elsewhereRequest.json
          format: uri
          readOnly: true
          type: string
        elsewhere:
          description: The place the work is happening. Empty clears it
          maxLength: 1000
          type: string
      required:
        - elsewhere
      type: object
    PointBody:
      additionalProperties: false
      properties:
        at:
          description: The end of this step, as a date
          type: string
        by_severity:
          additionalProperties:
            format: int64
            type: integer
          type: object
        open:
          format: int64
          type: integer
        opened:
          description: Findings that appeared during this step
          format: int64
          type: integer
        opened_by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: Everything that appeared, split by severity
          type: object
        resolved:
          description: Findings that went away during this step
          format: int64
          type: integer
        resolved_by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: Everything that went away, split by the severity it held while it was open
          type: object
      required:
        - at
        - open
        - opened
        - resolved
        - by_severity
        - opened_by_severity
        - resolved_by_severity
      type: object
    PreparedBody:
      additionalProperties: false
      properties:
        defer_days:
          description: The deferral it prepares, in days from whenever somebody submits it. A date would be wrong the week after it was saved. Required where the outcome is a deferral, and refused where it is anything else
          format: int64
          maximum: 3650
          minimum: 1
          type: integer
        justification:
          description: The recognized reason it does not apply, where the outcome takes one
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        outcome:
          description: The outcome it offers
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
          type: string
        reasoning:
          description: "The words it offers. Required: this is what whoever submits it is putting their name to"
          maxLength: 10000
          minLength: 1
          type: string
      required:
        - outcome
        - reasoning
      type: object
    ProductBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ProductBody.json
          format: uri
          readOnly: true
          type: string
        branches:
          description: The number of branches declared
          format: int64
          type: integer
        display_name:
          description: The display name. Defaults to the name
          maxLength: 191
          type: string
        end_of_life:
          description: The date support ends for releases that have not stated their own, as YYYY-MM-DD
          type: string
        last_scan_at:
          description: The moment a scan last arrived for any of its builds
          type: string
        name:
          description: The name scans use for this product
          maxLength: 191
          minLength: 1
          type: string
        open:
          description: Issues open against it, counted at components rather than at every place they sit. Absent unless counts were asked for
          format: int64
          type: integer
        tags:
          description: The number of tags declared
          format: int64
          type: integer
        triage_floor:
          description: The product's own triage line, where it says something other than the deployment. Absent means it follows the deployment
          enum:
            - everything
            - low
            - medium
            - high
            - critical
          type: string
        variants:
          description: The number of variants declared
          format: int64
          type: integer
      required:
        - name
      type: object
    ProductTree:
      additionalProperties: false
      properties:
        branches:
          items:
            $ref: "#/components/schemas/Branch"
          type:
            - array
            - "null"
      type: object
    ProviderBody:
      additionalProperties: false
      properties:
        name:
          description: The segment in the sign-in path
          type: string
        path:
          description: The address to send the browser to
          type: string
      required:
        - name
        - path
      type: object
    QueueOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/QueueOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/WaitingBody"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    QueuedBody:
      additionalProperties: false
      properties:
        kind:
          description: The worker the work is for
          type: string
        limit:
          description: The depth of this kind allowed before more is refused
          format: int64
          type: integer
        waiting:
          description: The depth waiting, including work held by a worker that has stopped reporting
          format: int64
          type: integer
      required:
        - kind
        - waiting
        - limit
      type: object
    RateBody:
      additionalProperties: false
      properties:
        closed:
          description: The number of issue-and-component groups wholly closed, with a deadline to be judged against
          format: int64
          type: integer
        deferred:
          description: "Still open with a standing deferral: somebody moved the date deliberately"
          format: int64
          type: integer
        late:
          description: The number that did not
          format: int64
          type: integer
        met:
          description: The number of those closed by it. Closed exactly at the deadline met it, because something still open at that instant is not yet overdue
          format: int64
          type: integer
        open:
          description: Still open at all, whatever their deadline. The denominator the deferred and overdue counts are read against
          format: int64
          type: integer
        overdue:
          description: Still open, past the deadline, with no deferral standing — plainly late
          format: int64
          type: integer
        severity:
          type: string
      required:
        - severity
        - closed
        - met
        - late
        - deferred
        - overdue
        - open
      type: object
    ReachBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ReachBody.json
          format: uri
          readOnly: true
          type: string
        automatic:
          description: Other builds it reaches by matching. Nothing to agree to
          items:
            $ref: "#/components/schemas/MatchBody"
          type:
            - array
            - "null"
        differing:
          description: The same issue at the same place held at another version — in this build or in another. Each is a separate judgment, because the code differs
          items:
            $ref: "#/components/schemas/MatchBody"
          type:
            - array
            - "null"
        here:
          description: Places in this build the judgment covers
          format: int64
          type: integer
      required:
        - here
        - automatic
        - differing
      type: object
    ReadinessBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ReadinessBody.json
          format: uri
          readOnly: true
          type: string
        blockers:
          description: The number of pieces of work nobody has agreed to ship with
          format: int64
          type: integer
        blocking:
          description: The work nobody has agreed to ship with, worst first. Bounded; total says how many there are
          items:
            $ref: "#/components/schemas/BlockingBody"
          type:
            - array
            - "null"
        floor:
          description: The least severity counted, or empty where everything is
          type: string
        now:
          $ref: "#/components/schemas/BuildCountsBody"
        shipped:
          $ref: "#/components/schemas/BuildCountsBody"
        why:
          description: The missing half, where there is nothing to compare against
          type: string
      required:
        - now
        - blocking
        - blockers
      type: object
    Reaffirm-claimRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Reaffirm-claimRequest.json
          format: uri
          readOnly: true
          type: string
        reasoning:
          description: The reason every one of them still holds, in markdown
          minLength: 1
          type: string
      required:
        - reasoning
      type: object
    Reaffirm-decisionRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Reaffirm-decisionRequest.json
          format: uri
          readOnly: true
          type: string
        previous:
          description: The decision being re-made
          format: int64
          type: integer
        reasoning:
          description: The reason it still holds, in markdown
          minLength: 1
          type: string
      required:
        - previous
        - reasoning
      type: object
    ReaffirmedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ReaffirmedBody.json
          format: uri
          readOnly: true
          type: string
        claim_id:
          description: The claim this action made, which is what a second person agrees to where one is needed
          format: int64
          type: integer
        decisions:
          items:
            format: int64
            type: integer
          type:
            - array
            - "null"
        places:
          description: The number of distinct places it covers. A place at two versions in two builds is two decisions, because the versions are what a decision expires on
          format: int64
          type: integer
        waiting:
          description: Whether a second person has to agree
          type: boolean
      required:
        - claim_id
        - decisions
        - places
        - waiting
      type: object
    ReceiptBody:
      additionalProperties: false
      properties:
        built_at:
          description: The build time the producer states
          type: string
        caution:
          description: "The scanner's own words while still succeeding — a qualification on what it found rather than a failure. Usually empty: the scan runs over an inventory written from what is held here, so most of what a scanner would warn about a producer's document it has no grounds to say about ours"
          type: string
        closed:
          description: Issues that were open and are not any more. Absent for the same reasons as the count beside it
          format: int64
          type: integer
        components:
          description: The number of components the inventory described
          format: int64
          type: integer
        failure:
          description: The reason it could not be used, where it could not
          type: string
        measured:
          $ref: "#/components/schemas/MeasuredBody"
          description: The tools the run answering this upload was measured with. Absent until a run has covered it
        opened:
          description: Issues this run found that were not open before. Absent where this upload's run is reported against a newer one, or where none has covered it yet
          format: int64
          type: integer
        placed:
          description: The number of them something placed in the graph
          format: int64
          type: integer
        received_at:
          description: The moment it arrived here
          type: string
        run_id:
          description: The run that answered this upload. Absent until one has
          format: int64
          type: integer
        scan_id:
          description: The scan this upload became
          format: int64
          type: integer
        sent:
          description: The documents this upload was made of
          items:
            $ref: "#/components/schemas/SentBody"
          type:
            - array
            - "null"
        serial:
          description: The identity the inventory carries for itself
          type: string
        state:
          description: The state it has reached
          enum:
            - reading
            - scanning
            - scanned
            - failed
          type: string
      required:
        - scan_id
        - received_at
        - state
      type: object
    ReceiptsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ReceiptsOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/ReceiptBody"
          type:
            - array
            - "null"
        measured_against:
          $ref: "#/components/schemas/MeasuredBody"
          description: The tools the last completed run was measured with
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    Record-advisory-issuedRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Record-advisory-issuedRequest.json
          format: uri
          readOnly: true
          type: string
        summary:
          description: The revision's summary, for the document's revision history. A history whose every entry reads the same is one nobody reads
          maxLength: 191
          type: string
      type: object
    Record-findingRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Record-findingRequest.json
          format: uri
          readOnly: true
          type: string
        builds:
          description: Every build that ships it. One issue, and one finding for each place the component sits at in each build — which is the shape a scanner's findings already take
          items:
            $ref: "#/components/schemas/Item"
          maxItems: 200
          minItems: 1
          type:
            - array
            - "null"
        component:
          description: The component that carries it. Omit for the build itself
          type: string
        contact:
          description: The address to reach them at. A researcher has no account here, which is the shape of the thing
          maxLength: 191
          type: string
        credit:
          description: The credit they asked for in an advisory, where that is not the name they reported under. "anonymous" is a real answer
          maxLength: 191
          type: string
        disclosed:
          description: Whether this is already public. Undisclosed by default
          type: boolean
        ecosystem:
          description: The ecosystem, where two share a name and a version
          type: string
        received:
          description: The day it arrived. The embargo is counted from this rather than from when it was typed in — the reporter is counting from the day they sent it, and they are the party who will publish regardless
          format: date
          type: string
        reported_by:
          description: The finder, as they gave their name
          maxLength: 191
          type: string
        severity:
          description: The severity. May be left out during early triage, before anybody has worked that out — an unrated finding is carried and listed, and what it does not get is a deadline. Worked out from the vector where one is given
          enum:
            - critical
            - high
            - medium
            - low
            - negligible
            - none
          type: string
        summary:
          description: The flaw, in your own words
          minLength: 1
          type: string
        vector:
          description: A CVSS 3.0 or 3.1 base vector. The score and the severity are worked out from it, so a score is never taken alongside it. Anything else is refused rather than scored with the wrong formula
          type: string
        version:
          description: The version, where the build holds that name at several
          type: string
        weaknesses:
          description: The kind of flaw, by the classification the world uses, such as CWE-125. Recorded as given, and the first is the root cause — a published advisory states one weakness, and this is what says which
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - builds
        - summary
      type: object
    RecordBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RecordBody.json
          format: uri
          readOnly: true
          type: string
        admin:
          description: Whether they administer this deployment. Omit it to leave it as it is
          type: boolean
        audits:
          description: "Whether they may read this deployment's own records: the settings, who holds what, and the administrative change log. It grants no product's findings or decisions. Omit it to leave it as it is"
          type: boolean
        display_name:
          description: The label shown instead of the identity
          type: string
        email:
          description: The address to reach them at outside the application. Optional. Send it empty to clear it; omit it to leave it alone. A sign-in provider that verifies an address fills it in where nobody here has recorded one, and never replaces one that was
          type: string
        holds:
          items:
            $ref: "#/components/schemas/GrantBody"
          type:
            - array
            - "null"
        identity:
          description: The name for them here
          maxLength: 191
          minLength: 1
          type: string
      required:
        - identity
      type: object
    Redact-attachmentRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Redact-attachmentRequest.json
          format: uri
          readOnly: true
          type: string
        reason:
          description: The reason the file is being removed. Recorded and shown wherever the text referred to it
          maxLength: 1000
          minLength: 1
          type: string
      required:
        - reason
      type: object
    Reference:
      additionalProperties: false
      properties:
        category:
          type: string
        summary:
          type: string
        url:
          type: string
      required:
        - summary
        - url
      type: object
    ReferenceBody:
      additionalProperties: false
      properties:
        kind:
          description: The kind of reference. A patch is the change itself
          enum:
            - patch
            - advisory
            - report
            - other
          type: string
        url:
          type: string
      required:
        - url
        - kind
      type: object
    ReleaseBody:
      additionalProperties: false
      properties:
        by_severity:
          additionalProperties:
            format: int64
            type: integer
          description: That total split by the rating in force
          type: object
        kind:
          description: "The kind of stream: a branch or a tag"
          type: string
        open:
          description: Every open finding at this build
          format: int64
          type: integer
        stream:
          description: The branch or tag
          type: string
        variant:
          type: string
      required:
        - stream
        - kind
        - variant
        - open
      type: object
    ReleasePointBody:
      additionalProperties: false
      properties:
        by_severity:
          additionalProperties:
            format: int64
            type: integer
          type: object
        cut:
          description: The date the release was declared. It orders and labels them; the axis is the sequence
          type: string
        open:
          description: Distinct issues open against it now, against today's vulnerability data rather than the day it was cut
          format: int64
          type: integer
        stream:
          type: string
      required:
        - stream
        - cut
        - open
      type: object
    Remediation:
      additionalProperties: false
      properties:
        category:
          type: string
        details:
          type: string
        product_ids:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - category
        - details
      type: object
    RemediationOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RemediationOutputBody.json
          format: uri
          readOnly: true
          type: string
        aging:
          description: Everything open now, by how long it has been. About now whatever period was asked for
          items:
            $ref: "#/components/schemas/BucketBody"
          type:
            - array
            - "null"
        fixed:
          description: Distinct issues that actually went away in the window
          format: int64
          type: integer
        from:
          description: The first day of the period. Absent where it runs from the beginning
          type: string
        opened:
          description: Distinct issues that appeared in it
          format: int64
          type: integer
        time_to_fix:
          additionalProperties:
            format: double
            type: number
          description: Average hours an issue closed in the window was open for, by severity. A severity nothing closed at is absent rather than zero
          type: object
        to:
          description: The day it ends, which is not itself in it
          type: string
      required:
        - fixed
        - opened
        - aging
        - to
      type: object
    RepeatBody:
      additionalProperties: false
      properties:
        last_until:
          description: The furthest any of them reached
          type: string
        place:
          description: "Names the place rather than describing it: what it is called depends on the build, and this is not about one build"
          type: string
        product:
          type: string
        severity:
          type: string
        standing:
          description: A deferral is in force now. Something put off three times and since decided is history; the same thing still being put off is the pattern
          type: boolean
        times:
          description: The number of times it has been put off
          format: int64
          type: integer
        total_days:
          description: The total it has been put off for
          format: int64
          type: integer
        vulnerability:
          type: string
      required:
        - product
        - vulnerability
        - place
        - times
        - total_days
      type: object
    ReportBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ReportBody.json
          format: uri
          readOnly: true
          type: string
        acknowledged:
          type: string
        acknowledged_by:
          type: string
        contact:
          type: string
        credit:
          description: The credit they asked for in an advisory
          type: string
        received:
          description: The day it arrived, which the embargo is counted from
          type: string
        recorded_by:
          type: string
        reported_by:
          type: string
      required:
        - recorded_by
      type: object
    Repromise-upgradeRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Repromise-upgradeRequest.json
          format: uri
          readOnly: true
          type: string
        by:
          description: The date the work will be done by
          format: date
          type: string
        reasoning:
          description: The reason the promise is changing, in markdown
          minLength: 1
          type: string
        to:
          description: The version it now moves to
          minLength: 1
          type: string
      required:
        - to
        - by
        - reasoning
      type: object
    Resolve-findingRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Resolve-findingRequest.json
          format: uri
          readOnly: true
          type: string
        because:
          description: The fix
          minLength: 1
          type: string
      required:
        - because
      type: object
    ResolvedBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ResolvedBody.json
          format: uri
          readOnly: true
          type: string
        at:
          description: The moment it closed
          type: string
        closed:
          description: The number of locations of the issue in this build that closed
          format: int64
          type: integer
      required:
        - closed
        - at
      type: object
    RetiredBody:
      additionalProperties: false
      properties:
        ended:
          description: The date has passed. False is a release that is about to go out of support
          type: boolean
        ended_days:
          description: The days since support ended. Negative where the date has not arrived, which is how many days are left
          format: int64
          type: integer
        ended_on:
          description: The date support ended, as YYYY-MM-DD
          type: string
        inherited:
          description: The date came from the product rather than from this release
          type: boolean
        kind:
          description: Whether this line moves
          enum:
            - branch
            - tag
          type: string
        open:
          description: Issues open against it, counted at components rather than at every place they sit
          format: int64
          type: integer
        product:
          type: string
        stream:
          type: string
      required:
        - product
        - stream
        - kind
        - ended_on
        - ended_days
        - open
        - ended
      type: object
    RetiredOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RetiredOutputBody.json
          format: uri
          readOnly: true
          type: string
        ending:
          description: Releases whose date has not arrived yet, soonest first. Empty unless within was asked for
          items:
            $ref: "#/components/schemas/RetiredBody"
          type:
            - array
            - "null"
        ending_open:
          description: Issues open across those, which is what leaves every overdue count on the day they cross
          format: int64
          type: integer
        items:
          items:
            $ref: "#/components/schemas/RetiredBody"
          type:
            - array
            - "null"
        open:
          description: Issues open across all of them
          format: int64
          type: integer
        total:
          description: The number of releases out of support
          format: int64
          type: integer
        within:
          description: The days ahead this looked
          format: int64
          type: integer
      required:
        - items
        - total
        - open
        - ending
        - ending_open
        - within
      type: object
    Retitle-advisoryRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Retitle-advisoryRequest.json
          format: uri
          readOnly: true
          type: string
        title:
          description: What to call it. Empty, the document names the issues it covers
          maxLength: 191
          type: string
      required:
        - title
      type: object
    Revise-claimRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Revise-claimRequest.json
          format: uri
          readOnly: true
          type: string
        reasoning:
          minLength: 1
          type: string
      required:
        - reasoning
      type: object
    Revision:
      additionalProperties: false
      properties:
        date:
          format: date-time
          type: string
        number:
          type: string
        summary:
          type: string
      required:
        - number
        - date
        - summary
      type: object
    RevisionBody:
      additionalProperties: false
      properties:
        body:
          description: The justification text, in markdown
          type: string
        id:
          description: The identifier an approval names when it says which words were agreed to
          format: int64
          type: integer
        ordinal:
          description: The revision number, counting from one
          format: int64
          type: integer
        written_at:
          type: string
        written_by:
          type: string
      required:
        - id
        - ordinal
        - body
        - written_by
        - written_at
      type: object
    RootsBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RootsBody.json
          format: uri
          readOnly: true
          type: string
        components:
          description: The number of components this build holds
          format: int64
          type: integer
        edges:
          description: The number of edges placing them
          format: int64
          type: integer
        items:
          items:
            $ref: "#/components/schemas/NeighborBody"
          type:
            - array
            - "null"
        root:
          $ref: "#/components/schemas/NeighborBody"
          description: The build itself, which everything below descends from. Absent where the inventory named no root of its own
        term:
          description: The search this answers, where one was asked
          type: string
      required:
        - items
        - components
        - edges
      type: object
    RowsStandingBody:
      additionalProperties: false
      properties:
        approved:
          description: Agreed to and in force
          format: int64
          type: integer
        proposed:
          description: Waiting for a second person
          format: int64
          type: integer
        sent_back:
          description: Returned to the author for more
          format: int64
          type: integer
      required:
        - proposed
        - sent_back
        - approved
      type: object
    RuleBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RuleBody.json
          format: uri
          readOnly: true
          type: string
        beneath:
          description: A component name. Catches it and everything sitting under it, in every build
          type: string
        id:
          format: int64
          type: integer
        name:
          description: The name, so a placement can be explained in words
          type: string
        order:
          description: Its position among the others. The first rule that matches places the work
          format: int64
          type: integer
        team:
          description: The team work lands on, by the name that addresses it
          type: string
        team_display_name:
          description: That team's display name, where it was declared with one
          type: string
        upstream:
          description: A source package name. Catches every binary package built from it
          type: string
      required:
        - id
        - name
        - team
        - order
      type: object
    RunBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/RunBody.json
          format: uri
          readOnly: true
          type: string
        caution:
          description: The scanner's own words while succeeding — a qualification on what it found rather than a failure
          type: string
        closed:
          $ref: "#/components/schemas/RunChangeBody"
        database_version:
          description: The vulnerability database it read
          type: string
        failure:
          description: The reason it produced nothing
          type: string
        finished_at:
          description: Absent while it is still going
          type: string
        opened:
          $ref: "#/components/schemas/RunChangeBody"
        opened_exploited:
          description: The number it opened that somebody is known to be exploiting
          format: int64
          type: integer
        ran_here:
          description: We ran the scanner, rather than the build sending what its own found
          type: boolean
        run_id:
          format: int64
          type: integer
        scanner:
          type: string
        scanner_version:
          type: string
        started_at:
          type: string
      required:
        - run_id
        - scanner
        - started_at
        - opened
        - closed
      type: object
    RunChangeBody:
      additionalProperties: false
      properties:
        critical:
          format: int64
          type: integer
        high:
          format: int64
          type: integer
        low:
          format: int64
          type: integer
        medium:
          description: Includes anything rated by a word nothing recognizes, which is treated as unknown rather than as harmless
          format: int64
          type: integer
        total:
          description: Issues at components, the unit every count here uses
          format: int64
          type: integer
      required:
        - total
      type: object
    Save-filterRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Save-filterRequest.json
          format: uri
          readOnly: true
          type: string
        prepares:
          $ref: "#/components/schemas/PreparedBody"
          description: The claim this filter should offer about what it catches. Left out, it prepares nothing — including on a name that used to
        query:
          description: The list's query string, without a leading ?
          maxLength: 2000
          type: string
      required:
        - query
      type: object
    SavedBody:
      additionalProperties: false
      properties:
        name:
          description: The name. Matched without regard to capitals, and one of a name replaces the one before
          maxLength: 120
          minLength: 1
          type: string
        prepares:
          $ref: "#/components/schemas/PreparedBody"
          description: The claim this filter offers about what it catches. Absent on an ordinary saved filter, which is most of them
        query:
          description: The findings list's query string, without a leading ?
          maxLength: 2000
          type: string
      required:
        - name
        - query
      type: object
    Score:
      additionalProperties: false
      properties:
        cvss_v3:
          $ref: "#/components/schemas/CVSSv3"
        products:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - products
      type: object
    Score-vectorResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Score-vectorResponse.json
          format: uri
          readOnly: true
          type: string
        score:
          format: double
          type: number
        severity:
          description: The band the score falls in
          enum:
            - none
            - low
            - medium
            - high
            - critical
          type: string
        vector:
          description: As it was read, upper-cased
          type: string
        version:
          description: The scheme the vector is on
          type: string
      required:
        - vector
        - version
        - score
        - severity
      type: object
    ScrutinyOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/ScrutinyOutputBody.json
          format: uri
          readOnly: true
          type: string
        agreed:
          description: Decisions a standing agreement covers in this period
          format: int64
          type: integer
        alone:
          items:
            $ref: "#/components/schemas/UnagreedBody"
          type:
            - array
            - "null"
        bulk:
          items:
            $ref: "#/components/schemas/BulkApprovalBody"
          type:
            - array
            - "null"
        capped:
          description: A section reached the limit, so this is the worst of it rather than all of it
          type: boolean
        from:
          description: The first day of the period, by when a claim was proposed. Absent where it runs from the beginning
          type: string
        grew:
          items:
            $ref: "#/components/schemas/GrownBody"
          type:
            - array
            - "null"
        lapsed:
          items:
            $ref: "#/components/schemas/LapsedApprovalBody"
          type:
            - array
            - "null"
        pairs:
          items:
            $ref: "#/components/schemas/PairingBody"
          type:
            - array
            - "null"
        to:
          description: The day it ends, which is not itself in it
          type: string
      required:
        - alone
        - bulk
        - pairs
        - lapsed
        - grew
        - agreed
        - to
      type: object
    SelectionBody:
      additionalProperties: false
      properties:
        contains:
          description: The text the candidate list was narrowed by. Absent where it was not narrowed, which means every issue at the component was on the page
          type: string
        matched:
          description: The number of issues that narrowing reached when the claim was written, read here rather than taken from the caller
          format: int64
          type: integer
        named:
          description: The number the claim was then made about
          format: int64
          type: integer
      required:
        - matched
        - named
      type: object
    Send-claim-backRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Send-claim-backRequest.json
          format: uri
          readOnly: true
          type: string
        because:
          description: The change being asked for, in markdown
          minLength: 1
          type: string
      required:
        - because
      type: object
    SentBody:
      additionalProperties: false
      properties:
        document_id:
          description: The name that reads this document back
          format: int64
          type: integer
        hash:
          description: SHA-256 of the bytes as they arrived
          type: string
        held:
          description: Whether the contents are still kept
          type: boolean
        kind:
          description: The kind of document
          enum:
            - inventory
            - suppressions
          type: string
        size_bytes:
          description: Its size
          format: int64
          type: integer
      required:
        - document_id
        - kind
        - size_bytes
        - hash
        - held
      type: object
    Set-affected-buildsRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Set-affected-buildsRequest.json
          format: uri
          readOnly: true
          type: string
        builds:
          description: Every build it affects, as the whole answer rather than a change to it. Bounded, because this is a complete list and every build absent from it is closed as never affected — so a caller that sent what it happened to have in hand would close the rest. Where an issue is open at more builds than this, the builds are answered one at a time from each build's own finding
          items:
            $ref: "#/components/schemas/Item1"
          maxItems: 200
          minItems: 1
          type:
            - array
            - "null"
        reason:
          description: The reason any build is being taken out. Required whenever one is
          type: string
      required:
        - builds
      type: object
    Set-digestRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Set-digestRequest.json
          format: uri
          readOnly: true
          type: string
        digest:
          description: Send a daily digest
          type: boolean
        unassigned:
          description: Include findings nobody owns
          type: boolean
      required:
        - digest
      type: object
    Set-release-detailsRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Set-release-detailsRequest.json
          format: uri
          readOnly: true
          type: string
        cut_from:
          description: The branch it was cut from, by name. Fills in where nothing has been said; naming a different one is refused, and empty leaves whatever stands alone
          type: string
        released_on:
          description: The day it went out, as 2026-03-31. Empty clears it, and the day it was declared here stands in
          type: string
      required:
        - released_on
      type: object
    Set-settingRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Set-settingRequest.json
          format: uri
          readOnly: true
          type: string
        value:
          minLength: 1
          type: string
      required:
        - value
      type: object
    SetAsideBody:
      additionalProperties: false
      properties:
        attempts:
          description: The number of attempts
          format: int64
          type: integer
        id:
          description: The job, for putting it back
          format: int64
          type: integer
        kind:
          description: The worker the job was for
          type: string
        last_error:
          description: The reason it stopped, where anything reported one. Worker output, which may quote what the job was about
          type: string
        reference:
          description: The subject of the work
          type: string
        stopped_at:
          description: The moment it was set aside
          format: date-time
          type: string
      required:
        - id
        - kind
        - reference
        - attempts
        - stopped_at
      type: object
    SettingBody:
      additionalProperties: false
      properties:
        default:
          description: Nobody has set this; the shipped value is in use
          type: boolean
        kind:
          description: "The kind of value: a length of time, a count of things, a count of bytes, one of a few words, or on and off"
          enum:
            - duration
            - count
            - size
            - word
            - switch
          type: string
        means:
          description: The thing it decides
          type: string
        name:
          type: string
        value:
          type: string
        words:
          description: For a word setting, the values it takes, in the order to offer them
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - name
        - value
        - means
        - kind
      type: object
    Shipped:
      additionalProperties: false
      properties:
        "@id":
          type: string
        subcomponents:
          items:
            $ref: "#/components/schemas/Inside"
          type:
            - array
            - "null"
      required:
        - "@id"
      type: object
    Shorten-disclosureRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Shorten-disclosureRequest.json
          format: uri
          readOnly: true
          type: string
        reason:
          description: The reason the date is moving
          maxLength: 65536
          minLength: 1
          type: string
        until:
          description: The date the embargo should end
          type: string
      required:
        - until
        - reason
      type: object
    SightingBody:
      additionalProperties: false
      properties:
        component:
          type: string
        due:
          description: The earliest deadline among its places
          type: string
        fixed_in:
          type: string
        places:
          description: The number of times that component sits in that build carrying this issue
          format: int64
          type: integer
        product:
          description: The product's name, as an address takes it
          type: string
        product_name:
          description: Its spelling on screen
          type: string
        state:
          description: The decision state here, by the definition the findings list uses
          enum:
            - undecided
            - waiting
            - agreed
            - lapsed
          type: string
        stream:
          type: string
        undisclosed:
          type: boolean
        variant:
          type: string
        version:
          type: string
      required:
        - product
        - product_name
        - stream
        - variant
        - component
        - version
        - places
      type: object
    SignInBody:
      additionalProperties: false
      properties:
        pinned:
          type: boolean
        username:
          type: string
      required:
        - username
        - pinned
      type: object
    SimilarBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        claim_id:
          description: Pass as extends when deciding to carry it to this issue
          format: int64
          type: integer
        decision_id:
          format: int64
          type: integer
        issues:
          description: The number of distinct issues the claim covers
          format: int64
          type: integer
        justification:
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        reasoning:
          type: string
      required:
        - claim_id
        - decision_id
        - reasoning
        - issues
      type: object
    SittingBody:
      additionalProperties: false
      properties:
        chain:
          description: The way down to here, the build first and this component last. Empty where the inventory left the component unplaced
          items:
            $ref: "#/components/schemas/StepBody"
          type:
            - array
            - "null"
        claim:
          description: The action that decision was one row of, so a claim shown on this finding can name the places it covers rather than only count them
          format: int64
          type: integer
        component:
          description: The package of the source package this place is
          type: string
        consumer:
          description: The consumer that pulls the component in here. Absent under the product itself
          type: string
        decision:
          description: The claim already standing here, where one does. Not the same as suppressed, which is the build's own argument
          format: int64
          type: integer
        declared_as:
          description: "The producer's own word for this dependency, where it said anything: a CycloneDX component scope, or an SPDX lifecycle scope. Evidence, and nothing acts on it"
          type: string
        place:
          description: Name this when recording a decision about it
          type: string
        suppressed:
          description: The build has already argued this place away
          type: boolean
      required:
        - place
        - component
      type: object
    SpentBody:
      additionalProperties: false
      properties:
        claims:
          description: Arguments made about it in the period
          format: int64
          type: integer
        component:
          description: The subject of the judgments, by name. Empty where nothing in any build carries the place any more
          type: string
        decisions:
          description: Places those reached
          format: int64
          type: integer
        deferred:
          description: Claims that put it off
          format: int64
          type: integer
        dismissed:
          description: Claims that argued it away
          format: int64
          type: integer
        people:
          description: The number of different people who argued about it
          format: int64
          type: integer
        product:
          type: string
        promised:
          description: "Claims that promised work: an upgrade or a backport"
          format: int64
          type: integer
      required:
        - product
        - component
        - claims
        - decisions
        - people
        - promised
        - dismissed
        - deferred
      type: object
    Split-claimRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Split-claimRequest.json
          format: uri
          readOnly: true
          type: string
        because:
          description: The reason they are being held back, in markdown
          minLength: 1
          type: string
        rows:
          description: The decisions to hold back, which must be this claim's
          items:
            format: int64
            type: integer
          minItems: 1
          type:
            - array
            - "null"
      required:
        - rows
        - because
      type: object
    Split-claimResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Split-claimResponse.json
          format: uri
          readOnly: true
          type: string
        claim_id:
          description: The claim the rows were moved into
          format: int64
          type: integer
      required:
        - claim_id
      type: object
    SpreadBody:
      additionalProperties: false
      properties:
        band:
          description: The severity these were rated at. 'unrated' is what nobody scored
          type: string
        count:
          description: The number of observations behind it
          format: int64
          type: integer
        median_days:
          description: The middle one, in days
          format: double
          type: number
        p90_days:
          description: "The span nine in ten came in under, in days. Nearest-rank rather than interpolated: these are waits something actually had"
          format: double
          type: number
        worst_days:
          description: The longest single one, in days
          format: double
          type: number
      required:
        - band
        - count
        - median_days
        - p90_days
        - worst_days
      type: object
    StandingBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/StandingBody.json
          format: uri
          readOnly: true
          type: string
        previously:
          items:
            $ref: "#/components/schemas/DecisionDetail"
          type:
            - array
            - "null"
        standing:
          $ref: "#/components/schemas/DecisionDetail"
      required:
        - previously
      type: object
    StandingClaimBody:
      additionalProperties: false
      properties:
        approved_at:
          type: string
        approved_by:
          type: string
        builds:
          description: Every build the claim currently covers, as stream and variant
          items:
            type: string
          type:
            - array
            - "null"
        claim_id:
          format: int64
          type: integer
        decision_id:
          description: A representative row of the claim at this finding
          format: int64
          type: integer
        elsewhere:
          description: A ticket, a thread or a change. Stored and never fetched
          type: string
        fixed_version:
          description: The package version the claim says the fix arrived in, where it claims one has
          type: string
        justification:
          enum:
            - component_not_present
            - vulnerable_code_not_present
            - vulnerable_code_not_in_execute_path
            - vulnerable_code_cannot_be_controlled_by_adversary
            - inline_mitigations_already_exist
          type: string
        kind:
          enum:
            - finding
            - together
            - extension
            - returned
          type: string
        needs_approval:
          type: boolean
        outcome:
          enum:
            - affected
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        places:
          description: The number of this finding's places the claim covers
          format: int64
          type: integer
        proposed_at:
          type: string
        proposed_by:
          type: string
        rows:
          $ref: "#/components/schemas/RowsStandingBody"
          description: The state of the claim's rows here
        sent_back_at:
          description: The last time rows were sent back to the author
          type: string
        sent_back_because:
          description: The reason given when they were, in markdown
          type: string
        state:
          description: "The claim's state as a whole: approved only when every live row here is approved, otherwise proposed"
          enum:
            - proposed
            - approved
          type: string
      required:
        - claim_id
        - kind
        - decision_id
        - state
        - rows
        - outcome
        - proposed_by
        - proposed_at
        - places
        - builds
      type: object
    Start-advisoryRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Start-advisoryRequest.json
          format: uri
          readOnly: true
          type: string
        title:
          description: What to call it. Left out, the document names the issues it covers
          maxLength: 191
          type: string
      type: object
    Statement:
      additionalProperties: false
      properties:
        action_statement:
          type: string
        impact_statement:
          type: string
        justification:
          type: string
        products:
          items:
            $ref: "#/components/schemas/Shipped"
          type:
            - array
            - "null"
        status:
          type: string
        timestamp:
          format: date-time
          type: string
        vulnerability:
          $ref: "#/components/schemas/Issue"
      required:
        - vulnerability
        - timestamp
        - products
        - status
      type: object
    Statements:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Statements.json
          format: uri
          readOnly: true
          type: string
        "@context":
          type: string
        "@id":
          type: string
        author:
          type: string
        statements:
          items:
            $ref: "#/components/schemas/Statement"
          type:
            - array
            - "null"
        timestamp:
          format: date-time
          type: string
        tooling:
          type: string
        version:
          format: int64
          type: integer
      required:
        - "@context"
        - "@id"
        - author
        - tooling
        - timestamp
        - version
        - statements
      type: object
    StatementsTakenBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/StatementsTakenBody.json
          format: uri
          readOnly: true
          type: string
        digest:
          description: The document's digest, which is how a revision is noticed later
          type: string
        publisher:
          description: The publisher the document names
          type: string
        recorded:
          description: Statements taken from it
          format: int64
          type: integer
        superseded:
          description: This publisher's previous statements, set aside rather than deleted
          format: int64
          type: integer
      required:
        - publisher
        - recorded
        - superseded
        - digest
      type: object
    Status:
      additionalProperties: false
      properties:
        fixed:
          items:
            type: string
          type:
            - array
            - "null"
        known_affected:
          items:
            type: string
          type:
            - array
            - "null"
      type: object
    StepBody:
      additionalProperties: false
      properties:
        component:
          type: string
        version:
          type: string
      required:
        - component
      type: object
    StreamBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/StreamBody.json
          format: uri
          readOnly: true
          type: string
        display_name:
          description: The same name as it was spelled. Absent where it is the name itself
          type: string
        end_of_life:
          description: The date support ends, as YYYY-MM-DD
          type: string
        end_of_life_inherited:
          description: The date shown is the product's, not this release's own
          type: boolean
        kind:
          description: Whether this line moves. A branch is rebuilt; a tag never changes
          enum:
            - branch
            - tag
          type: string
        last_scan_at:
          description: The moment a scan last arrived for any build of it
          type: string
        name:
          description: The name scans use for this branch or tag
          maxLength: 191
          minLength: 1
          type: string
        open:
          description: Issues open against it, counted at components rather than at every place they sit. Absent unless counts were asked for
          format: int64
          type: integer
        parent:
          description: For a tag, the branch it was cut from
          type: string
        released_on:
          description: For a tag, the day it went out, as YYYY-MM-DD. Absent where nobody has said, and the day it was declared here stands in
          type: string
        retired:
          description: Whether it has been taken out of use. Nothing filed against it is affected and no new scan is accepted
          type: boolean
      required:
        - name
        - kind
      type: object
    TLP:
      additionalProperties: false
      properties:
        label:
          type: string
        url:
          type: string
      required:
        - label
      type: object
    TeamBody:
      additionalProperties: false
      properties:
        display_name:
          type: string
        members:
          items:
            type: string
          type:
            - array
            - "null"
        name:
          description: The team's name. Matched without regard to capitals
          type: string
      required:
        - name
        - members
      type: object
    TeamRecordBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TeamRecordBody.json
          format: uri
          readOnly: true
          type: string
        display_name:
          description: The display spelling. Defaults to the name
          type: string
        members:
          description: The members, by sign-in identity. Everybody named must already have been recorded
          items:
            type: string
          type:
            - array
            - "null"
        name:
          description: The team's name
          minLength: 1
          type: string
      required:
        - name
      type: object
    TokenBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TokenBody.json
          format: uri
          readOnly: true
          type: string
        created_at:
          description: The moment it was minted
          type: string
        expires_at:
          description: The moment it stops working
          type: string
        holds:
          description: Optionally, which of its owner's roles it carries. Intersected with what they hold, so naming one they do not have reaches nothing. Absent means all of them, and an empty list is refused because it would reach none
          items:
            enum:
              - approver
              - assigner
              - public-read
              - private-read
              - public-triage
              - private-triage
            type: string
          type:
            - array
            - "null"
        last_used_at:
          description: The last time it was used
          type: string
        lifetime:
          description: The lifetime, such as "720h". There is a configured maximum
          type: string
        name:
          description: The owner's name for it
          maxLength: 191
          minLength: 1
          type: string
        owner:
          description: The owner. Shown to an administrator listing everybody's
          type: string
        product:
          description: Optionally, the one product it may reach, by the name that addresses it
          type: string
        product_display_name:
          description: That product's display name, where it was declared with one
          type: string
        secret:
          description: Shown once, at creation. It is stored hashed and cannot be shown again
          type: string
        withdrawn:
          description: Whether it has been withdrawn
          type: boolean
      required:
        - name
      type: object
    ToldBody:
      additionalProperties: false
      properties:
        at:
          type: string
        body:
          type: string
        cleared:
          description: The condition it was about stopped being true
          type: boolean
        kind:
          type: string
        link:
          type: string
        private:
          description: It is about a finding nobody has announced
          type: boolean
        read:
          description: They have acknowledged it
          type: boolean
      required:
        - at
        - kind
        - body
      type: object
    Tracking:
      additionalProperties: false
      properties:
        current_release_date:
          format: date-time
          type: string
        generator:
          $ref: "#/components/schemas/Generator"
        id:
          type: string
        initial_release_date:
          format: date-time
          type: string
        revision_history:
          items:
            $ref: "#/components/schemas/Revision"
          type:
            - array
            - "null"
        status:
          type: string
        version:
          type: string
      required:
        - id
        - status
        - version
        - initial_release_date
        - current_release_date
        - revision_history
      type: object
    TriageFloorBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/TriageFloorBody.json
          format: uri
          readOnly: true
          type: string
        floor:
          description: The least severity worth triaging here, "everything" to hide nothing, or empty to follow the deployment
          enum:
            - everything
            - low
            - medium
            - high
            - critical
            - ""
          type: string
      required:
        - floor
      type: object
    UnagreedBody:
      additionalProperties: false
      properties:
        claims:
          description: The number of acts
          format: int64
          type: integer
        outcome:
          enum:
            - not-applicable
            - mismatched
            - deferred
            - wont-fix
            - already-fixed
            - upgrade-needed
            - patch-needed
          type: string
        rows:
          description: The number of decisions those acts wrote
          format: int64
          type: integer
      required:
        - outcome
        - claims
        - rows
      type: object
    UnansweredBody:
      additionalProperties: false
      properties:
        checked:
          description: The moment the pass last reached it. For a name of ours that is when it was last decided against rather than when anything was asked
          type: string
        ecosystem:
          description: The ecosystem the identifier names, read out of it rather than stored
          type: string
        purl:
          description: The package identifier, which is the name that would be sent
          type: string
        why:
          description: "'ours' was never sent: this deployment calls the name its own. 'unknown' was sent and no index had heard of it. 'unreadable' is an identifier nothing can turn into a request"
          enum:
            - ours
            - unknown
            - unreadable
          type: string
      required:
        - purl
        - why
        - checked
      type: object
    UnansweredOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UnansweredOutputBody.json
          format: uri
          readOnly: true
          type: string
        items:
          items:
            $ref: "#/components/schemas/UnansweredBody"
          type:
            - array
            - "null"
        ours:
          description: The names this deployment holds back, from its publisher namespace, from what it stated, and from what the builds you may read declared themselves to be. A name here is matched against each part of a package's name, either exactly or followed by one of - . _, and a name carrying a dot also covers a host under it
          items:
            type: string
          type:
            - array
            - "null"
        total:
          description: The total, through the same filter as the page
          format: int64
          type: integer
        whole:
          description: Whether every candidate was examined. False means the estate is past what one read classifies, and the total is a floor
          type: boolean
      required:
        - items
        - total
        - ours
        - whole
      type: object
    UnassignedBody:
      additionalProperties: false
      properties:
        builds:
          description: The number of builds holding it. More than one means the same code built more than one way, which one judgment answers
          format: int64
          type: integer
        component:
          type: string
        exploited:
          type: boolean
        places:
          description: The number of findings a judgment here would be recorded against, across every build it is in
          format: int64
          type: integer
        product:
          type: string
        severity:
          type: string
        stream:
          description: A branch or tag holding it. Where builds is more than one, any of them
          type: string
        variant:
          description: A build variant holding it. Where builds is more than one, any of them
          type: string
        version:
          type: string
        vulnerability:
          type: string
      required:
        - vulnerability
        - component
        - version
        - product
        - stream
        - variant
        - places
        - builds
      type: object
    Undo-batchResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Undo-batchResponse.json
          format: uri
          readOnly: true
          type: string
        undone:
          format: int64
          type: integer
      required:
        - undone
      type: object
    UpgradeBody:
      additionalProperties: false
      properties:
        fixed_here:
          description: The number of open findings naming this exact version as their fix — that release's own security content
          format: int64
          type: integer
        ordered:
          description: Whether these versions could be ordered at all. False means the list is not ranked and reached says no more than fixed_here
          type: boolean
        reached:
          description: The number moving here would close altogether, counting everything fixed at or before it. Equal to fixed_here where the versions could not be ordered
          format: int64
          type: integer
        to:
          description: The version, as whoever packages the component wrote it
          type: string
      required:
        - to
        - fixed_here
        - reached
        - ordered
      type: object
    UploadResult:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/UploadResult.json
          format: uri
          readOnly: true
          type: string
        built_at:
          description: The build time the producer states
          type: string
        outcome:
          description: Whether this upload was taken or matched one already held
          enum:
            - queued
            - already_held
          type: string
        scan_id:
          description: The scan this upload became, or the one it matched
          format: int64
          type: integer
        serial:
          description: The identity the inventory carries for itself
          type: string
      required:
        - scan_id
        - outcome
      type: object
    UpwardBody:
      additionalProperties: false
      properties:
        beneath:
          description: Your own open issues at or under it along these chains. Never how much the build holds there
          format: int64
          type: integer
        component:
          type: string
        depth:
          description: The depth below the build's root, so the tree is drawn by indenting
          format: int64
          type: integer
        findings:
          description: Your own open issues on this component itself
          format: int64
          type: integer
        placed:
          description: False for a component the inventory put nowhere. Those sit at the end with no chain
          type: boolean
        version:
          type: string
      required:
        - component
        - version
        - depth
        - findings
        - beneath
        - placed
      type: object
    VEXIssuanceBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/VEXIssuanceBody.json
          format: uri
          readOnly: true
          type: string
        digest:
          description: A digest of what the document said, so that what is published and what we would generate stay answerable against each other
          type: string
        issued_at:
          type: string
        issued_by:
          description: Who published it. The act leaves this row and nothing else, so the row names them
          type: string
        version:
          description: Which revision went out, counting from one. It is the version that document carries
          format: int64
          type: integer
      required:
        - version
        - digest
        - issued_by
        - issued_at
      type: object
    VariantBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/VariantBody.json
          format: uri
          readOnly: true
          type: string
        customer_facing:
          description: Whether this reaches customers. Defaults to yes
          type: boolean
        display_name:
          description: The same name as it was spelled. Absent where it is the name itself
          type: string
        name:
          description: The name scans use for this build of the stream
          maxLength: 191
          minLength: 1
          type: string
        open:
          description: Issues open against it here, counted at components rather than at every place they sit. Absent unless counts were asked for
          format: int64
          type: integer
        retired:
          description: Whether it has been taken out of use. A release still lists what it was built as
          type: boolean
      required:
        - name
      type: object
    VexSaidBody:
      additionalProperties: false
      properties:
        at:
          description: The moment it was uploaded here
          type: string
        document:
          description: The document it came from
          type: string
        id:
          description: Pass as from_statement when starting a decision from this, so a later revision can be noticed
          format: int64
          type: integer
        justification:
          description: The term they gave for it, where the status is one that takes one
          type: string
        offers:
          description: The outcome this offers as a prefill. Never applied by itself
          enum:
            - not-applicable
            - wont-fix
            - already-fixed
          type: string
        publisher:
          type: string
        statement:
          description: Their reasoning. What a triager otherwise types from memory
          type: string
        status:
          description: Their statement, in the format's own vocabulary
          type: string
      required:
        - id
        - publisher
        - status
        - document
        - at
      type: object
    Vulnerability:
      additionalProperties: false
      properties:
        acknowledgments:
          items:
            $ref: "#/components/schemas/Acknowledgment"
          type:
            - array
            - "null"
        cve:
          type: string
        cwe:
          $ref: "#/components/schemas/Weakness"
        discovery_date:
          type: string
        ids:
          items:
            $ref: "#/components/schemas/Issued"
          type:
            - array
            - "null"
        notes:
          items:
            $ref: "#/components/schemas/Note"
          type:
            - array
            - "null"
        product_status:
          $ref: "#/components/schemas/Status"
        remediations:
          items:
            $ref: "#/components/schemas/Remediation"
          type:
            - array
            - "null"
        scores:
          items:
            $ref: "#/components/schemas/Score"
          type:
            - array
            - "null"
        title:
          type: string
      required:
        - product_status
      type: object
    VulnerabilityDataBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/VulnerabilityDataBody.json
          format: uri
          readOnly: true
          type: string
        moved_at:
          description: "The moment the data last moved: the most recent time any version was seen for the first time. A version that comes back is not a change"
          format: date-time
          type: string
        stale:
          description: Whether it has been that long. The same question the condition told to administrators asks
          type: boolean
        stale_after:
          description: The span without moving that counts as stopped, as this deployment has it set
          type: string
        version:
          description: The version the newest finished run stated, in the scanner's own spelling. Absent where nothing has finished a scan and said
          type: string
      required:
        - stale_after
        - stale
      type: object
    WaitingBody:
      additionalProperties: false
      properties:
        age_days:
          description: The age of the claim. An old judgment should look like one
          format: int64
          type: integer
        builds:
          description: Every build the claim currently covers, as stream and variant
          items:
            type: string
          type:
            - array
            - "null"
        claim:
          $ref: "#/components/schemas/ClaimBody"
        counter:
          $ref: "#/components/schemas/CounterBody"
          description: "The context a careful reader looks up: what was decided about this issue elsewhere, and how much else at the same place nobody has answered"
        decision:
          $ref: "#/components/schemas/DecisionBody"
        decisions:
          description: The number of rows the claim wrote
          format: int64
          type: integer
        deferred_days:
          description: The total this finding has been put off for
          format: int64
          type: integer
        finding:
          $ref: "#/components/schemas/FindingRefBody"
          description: The representative decision's subject — build, issue, component, where it sits — so the card can be judged without opening it. Absent where no open finding sits at its place
        issues:
          description: The number of distinct issues it covers
          format: int64
          type: integer
        outliers:
          $ref: "#/components/schemas/OutliersBody"
          description: "For a claim over many issues: the rows that do not look like the rest, and how many there are"
        place:
          $ref: "#/components/schemas/PlaceBody"
        places:
          description: The number of distinct places it covers
          format: int64
          type: integer
        previously_approved:
          description: This was agreed to before and came back
          type: boolean
        proposed_by:
          type: string
        reasoning:
          type: string
      required:
        - claim
        - decision
        - place
        - reasoning
        - proposed_by
        - age_days
        - decisions
        - issues
        - places
        - builds
      type: object
    WasSaidBody:
      additionalProperties: false
      properties:
        body:
          description: Its text, in markdown
          type: string
        replaced_at:
          description: The moment it stopped saying that
          type: string
        version:
          description: The version number, counting from one
          format: int64
          type: integer
      required:
        - version
        - body
        - replaced_at
      type: object
    Weakness:
      additionalProperties: false
      properties:
        id:
          type: string
        name:
          type: string
      required:
        - id
        - name
      type: object
    WentBody:
      additionalProperties: false
      properties:
        advisory:
          description: The identifier it went out under
          type: string
        digest:
          description: The digest of what the document stated when it went out, which is what makes comparing it against what would be generated now possible
          type: string
        issued_at:
          type: string
        issued_by:
          type: string
        issues:
          description: How many issues it covered
          format: int64
          type: integer
        ordinal:
          description: The issuance number, counting from one. Above one is a revision
          format: int64
          type: integer
        products:
          description: How many products those sat in
          format: int64
          type: integer
        summary:
          type: string
        title:
          type: string
      required:
        - advisory
        - issues
        - products
        - ordinal
        - issued_by
        - issued_at
        - digest
      type: object
    WhoBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/WhoBody.json
          format: uri
          readOnly: true
          type: string
        admin:
          description: Administers this deployment
          type: boolean
        audits:
          description: May read this deployment's own records — the settings, who holds what, and the administrative change log — and write none of them
          type: boolean
        bulk_cap:
          description: The number of rows one action may write here. A screen acting on a selection bounds it by this, and says so, rather than discovering the limit one refusal at a time
          format: int64
          type: integer
        deferral_days:
          description: The deferral a second person has to agree past, in days. A screen taking a date needs it before the date is written, not after it is submitted
          format: int64
          type: integer
        digest:
          description: They asked for a daily digest
          type: boolean
        digest_unassigned:
          description: Their digest lists findings nobody owns as well as their own outstanding work
          type: boolean
        identity:
          description: The identity this deployment holds for them
          type: string
        kind:
          description: A person who signed in, or a credential
          enum:
            - person
            - key
          type: string
        name:
          description: The display name, where one is recorded
          type: string
        reach:
          description: The products they can reach, and what they may do in each
          items:
            $ref: "#/components/schemas/CanBody"
          type:
            - array
            - "null"
        reachable:
          description: An address is recorded for them, so anything can be sent at all
          type: boolean
      required:
        - identity
        - name
        - admin
        - kind
        - reach
      type: object
    Withdraw-estate-roleResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Withdraw-estate-roleResponse.json
          format: uri
          readOnly: true
          type: string
        released:
          description: Findings handed back because that was their last role there
          format: int64
          type: integer
      required:
        - released
      type: object
    Withdraw-roleResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/schemas/Withdraw-roleResponse.json
          format: uri
          readOnly: true
          type: string
        released:
          description: Findings handed back because that was their last role here
          format: int64
          type: integer
      required:
        - released
      type: object
    WorkedBody:
      additionalProperties: false
      properties:
        approved:
          description: "Claims they agreed to, dated by the agreement: an approver's week is the week they approved in"
          format: int64
          type: integer
        person:
          type: string
        proposed:
          description: Claims they made in the window
          format: int64
          type: integer
        withdrawn:
          description: Claims of theirs they took back
          format: int64
          type: integer
      required:
        - person
        - proposed
        - approved
        - withdrawn
      type: object
info:
  description: Track vulnerabilities in the products you ship.
  title: OpenPSIRT
  version: dev
openapi: 3.1.0
paths:
  /v1/administration/changes:
    get:
      description: |-
        Who changed a setting, a role grant, a routing rule, a support date, a credential, an account, a team or an issue's names — with what it held before and what it holds now.

        This is the layer above the triage record rather than part of it. Three of the things listed here silently rewrite what the tool reports: the deadline windows recompute every open finding's deadline, the triage floor takes the deadline off everything below it, and an end-of-life date takes it off everything past it.

        Newest first, and paged: it only grows.

        Takes a period, because the question an audit asks is what changed in the stretch the certificate covers. Asked for none, it answers about everything it holds.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: list-administrative-changes
      parameters:
        - description: Keep only changes of one kind
          explode: false
          in: query
          name: kind
          schema:
            description: Keep only changes of one kind
            enum:
              - setting
              - role
              - routing
              - support
              - release
              - credential
              - account
              - team
              - case
              - alias
              - catalog
            type: string
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-administrative-changesResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List administrative changes
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
  /v1/administration/changes.{format}:
    get:
      description: |-
        Every administrative change the same filters would show, as a file, rather than one page of them.

        One row per change, with who made it, what it was about, and what it held before and after. An absent value is not an empty one: `unset` says nobody had set it, and `cleared` that the change removed it.

        Takes the kind and the period the list takes. Asked for no period it writes everything this deployment holds.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: export-administrative-changes
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Keep only changes of one kind
          explode: false
          in: query
          name: kind
          schema:
            description: Keep only changes of one kind
            enum:
              - setting
              - role
              - routing
              - support
              - release
              - credential
              - account
              - team
              - case
              - alias
              - catalog
            type: string
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export administrative changes
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
  /v1/advisories:
    get:
      description: |-
        Every advisory you may see, newest first.

        An advisory covering a product you hold nothing on is not listed, and the total says the same. A document is read whole or not at all: one with a product quietly left out reads as a complete statement about a product it says nothing about.

        `issues` and `products` are both given because one issue in three products and three issues in one are different documents, and a single count reads the same for each.

        `product` and `vulnerability` narrow it to what covers them, which is what a screen about one flaw asks before somebody starts another advisory about it.

        Requires: public-triage or private-triage. What you hold decides what comes back rather than whether you may ask
      operationId: list-advisories
      parameters:
        - description: Only advisories covering something in this product
          explode: false
          in: query
          name: product
          schema:
            description: Only advisories covering something in this product
            type: string
        - description: Only advisories covering this issue
          explode: false
          in: query
          name: vulnerability
          schema:
            description: Only advisories covering this issue
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            default: 0
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyAdvisoryListedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List advisories
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        narrowed: true
    post:
      description: |-
        Mints an identifier and returns the empty advisory under it.

        The identifier is minted here rather than chosen. It is what a reader cites the document by and what a revision of it keeps, so two advisories under one name is a state this has no way back from.

        An advisory covers issues, which are added one at a time and each names the product it is covered in. Until one is added the advisory generates no document: the standard requires at least one vulnerability, and a document about nothing is not a draft of anything.

        Requires a prefix configured for this deployment to mint under.

        Requires: public-triage or private-triage. A triage role on some product. An advisory names no product until an issue is added to it, so there is none for the role to be held on here.
      operationId: start-advisory
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Start-advisoryRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdvisoryBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Start an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on some product. An advisory names no product until an issue is added to it, so there is none for the role to be held on here.
  /v1/advisories/published:
    get:
      description: |-
        Every advisory published from this deployment in a period, newest first, with how much it covered, which revision it was, who published it and what the document hashed to at the time.

        Advisories are about flaws in our own product, recorded here by hand. Known issues in third-party components are tracked and fixed rather than published about, and the document for those is a VEX statement per build.

        `ordinal` above one is a revision of an advisory already out, which is the entry a period is usually read for.

        Narrowed by what you may see: a flaw nobody has disclosed is absent for anybody who may not read it, and a count is as much a disclosure as a row.

        Asked for neither a period nor a window, this is the last 365 days. An auditor asking what went out in a financial year names the two dates instead.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-published-advisories
      parameters:
        - description: Limit to one product, by name
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name
            type: string
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OverPeriodWentBodyBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List advisories that have gone out
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/advisories/{advisory}:
    get:
      description: |-
        The advisory and the issues it covers, in the order they were added.

        An advisory covering a product you hold nothing on answers as one that does not exist. Told apart, the pair of answers says what exists.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-advisory
      parameters:
        - description: The identifier this deployment minted
          in: path
          name: advisory
          required: true
          schema:
            description: The identifier this deployment minted
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdvisoryBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Read an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    patch:
      description: |-
        Gives the advisory a new title, as a new edition of what it says.

        Every agreement standing on the old title is taken back. A second person agreed to particular words, and different words are a document nobody has agreed to.

        Requires a triage role on every product the advisory covers, because what it says about one of them is part of the same document as what it says about another.

        Requires: public-triage or private-triage. A triage role on every product the advisory covers. What it says about one product is part of the same document as what it says about another.
      operationId: retitle-advisory
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Retitle-advisoryRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdvisoryBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retitle an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on every product the advisory covers. What it says about one product is part of the same document as what it says about another.
  /v1/advisories/{advisory}/approval:
    delete:
      description: |-
        Takes back every agreement standing on what the advisory says now.

        Every agreement standing, not only your own. It needs no agreement of its own, and the advisory cannot go out until somebody agrees again.

        Answers 422 where no agreement is standing.

        Requires: public-triage or private-triage. A triage role on every product the advisory covers. What it says about one product is part of the same document as what it says about another.
      operationId: withdraw-advisory-approval
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw approval of an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on every product the advisory covers. What it says about one product is part of the same document as what it says about another.
    post:
      description: |-
        Records that you have read what this advisory says and agree to it.

        The agreement names the edition it was given against rather than the advisory. Retitling it, naming another flaw on it or taking one off opens a new edition and takes the agreement back, so what stands is always an agreement to the document as it reads now.

        You may not agree to an advisory you started or whose current edition you wrote. Both answer 409, and there is no override, so a deployment with one person publishes no advisory.

        Requires a triage role on every product the advisory covers.

        Requires: public-triage or private-triage. A triage role on every product the advisory covers. What it says about one product is part of the same document as what it says about another.
      operationId: approve-advisory
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgreementBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Approve an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on every product the advisory covers. What it says about one product is part of the same document as what it says about another.
  /v1/advisories/{advisory}/document:
    get:
      description: |-
        Returns a CSAF 2.0 document for this advisory: what it covers, and which releases hold each issue and which no longer do.

        One entry per issue and one product branch per product, so several flaws released together are one document on one date.

        The document is generated, not published. Nothing is sent anywhere. Recording that it was issued is a separate request, and what it keeps is a digest of what was generated, so that whether what you published is still what this would generate can be answered.

        How far the document may travel is its distribution label, red while anything it covers is still held back. `tracking.status` says where the document is in its life. Reaching a disclosure date discloses nothing, so nothing here does either.

        An advisory covering no issue is refused: the standard requires at least one. Requires a publisher configured for this deployment, because a document naming none is not a valid CSAF document.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-advisory-document
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Document"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Generate a CSAF document for an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/advisories/{advisory}/issuance:
    get:
      description: |-
        What has been published for this advisory, oldest first: which revision, when, and what the document hashed to at the time.

        Readable without generating a document. Every issuance is in the document's own revision history, which is right for a reader of the document — but it made "has this gone out, and is what is published still what we would generate" a question you had to build a CSAF document to answer, and somebody deciding whether to publish a revision is asking before they generate anything.

        The digest is what makes the comparison possible, and it was taken from the document generated here rather than from anything sent.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-advisory-issuances
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyIssuanceBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the times an advisory went out
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Records that this advisory was published: when, by whom, and a digest of the document as it stands now.

        A fact about a moment rather than a derived value. What was published on a date cannot be worked out again once the record it came from has moved on — a release is added, a decision is revised, a fix lands — so if it is not written down when it happens it is gone.

        It is what lets a second document be a revision. Without it a second document cannot carry a revision history or a higher version, and both are things CSAF validators check; a document that fails validation is one a customer's tooling drops.

        The document as it goes out is kept, because it cannot be worked out again: what would be generated tomorrow is a different document. The digest beside it makes "is what is published still what we generate" a question with an answer, and it is taken from the document generated here rather than from anything sent — a digest of whatever a caller says answers nothing.

        Answers 409 where nobody has agreed to what the advisory says.

        Requires: public-triage or private-triage. A triage role on some product. An advisory names no product until an issue is added to it, so there is none for the role to be held on here.
      operationId: record-advisory-issued
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Record-advisory-issuedRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssuanceBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record that an advisory went out
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on some product. An advisory names no product until an issue is added to it, so there is none for the role to be held on here.
  /v1/advisories/{advisory}/issues:
    post:
      description: |-
        Names an issue in a product as covered by this advisory.

        The pair, not the issue alone: an issue in two products is two entries, because the releases that carry it differ and a status is stated about releases.

        Only a flaw recorded here. An issue a scanner reported against a third-party component is refused, and refused at this point rather than when the document is generated, so the refusal names the issue you chose.

        Requires: public-triage or private-triage. A triage role on the product named in the request, and on every product the advisory already covers. Naming a flaw on an advisory is what puts it into a document published about that product, and opens an edition of the whole document.
      operationId: add-advisory-issue
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Add-advisory-issueRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoveredBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Add an issue to an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on the product named in the request, and on every product the advisory already covers. Naming a flaw on an advisory is what puts it into a document published about that product, and opens an edition of the whole document.
  /v1/advisories/{advisory}/issues/{product}/{vulnerability}:
    delete:
      description: |-
        Removes one issue in one product from this advisory.

        Nothing here asks whether the advisory has gone out. An issuance records what went out at a moment, and editing the advisory afterwards is how the next revision differs from the last.

        Requires: public-triage or private-triage. A triage role on the product named in the request, and on every product the advisory already covers. Naming a flaw on an advisory is what puts it into a document published about that product, and opens an edition of the whole document.
      operationId: drop-advisory-issue
      parameters:
        - in: path
          name: advisory
          required: true
          schema:
            type: string
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Take an issue off an advisory
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
        note: A triage role on the product named in the request, and on every product the advisory already covers. Naming a flaw on an advisory is what puts it into a document published about that product, and opens an edition of the whole document.
  /v1/approval-batches/{batch}:
    delete:
      description: |-
        Withdraws every approval recorded under this batch name, returning those decisions to the review queue. The decisions themselves stand — only the approvals are undone. Returns how many were affected.

        Only decisions you may reach are touched.

        Requires: approver or public-triage or private-triage on the product. What you hold decides what comes back rather than whether you may ask. A batch is one reviewer's afternoon and may span products, so it is undone as far as you reach and no further.
      operationId: undo-batch
      parameters:
        - in: path
          name: batch
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Undo-batchResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Undo a bulk approval
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - approver
          - public-triage
          - private-triage
        note: A batch is one reviewer's afternoon and may span products, so it is undone as far as you reach and no further.
        narrowed: true
  /v1/approvals/scrutiny:
    get:
      description: |-
        Five answers about approvals over a period, for somebody auditing whether they mean anything here.

        `alone` is risk standing with nobody's agreement behind it, by outcome. That is not a failure by itself: a short deferral and an upgrade promised inside the deadline the work already had are both deliberately exempt. A dismissal in this list is a different matter.

        `bulk` is agreements given as one act over many claims, `pairs` is who agrees with whom and what share of everything ran between them, `lapsed` is agreements standing from somebody who no longer holds the right to give one, and `grew` is what somebody agreed to against what the same claim reaches now — a claim reaches by matching, so a build appearing afterwards is covered with nobody acting.

        Everything is dated by when the claim was proposed, not by when it was agreed to, and narrowed by what you may see.

        Asked for neither a period nor a window, this is the last 90 days.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-approval-scrutiny
      parameters:
        - description: Limit to one product, by name
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name
            type: string
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
        - description: The number of rows each section carries at most. capped says a section reached it
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: The number of rows each section carries at most. capped says a section reached it
            format: int64
            maximum: 500
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScrutinyOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get approval coverage
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/assessments:
    get:
      description: |-
        Every claim about an issue you may be told about, or those in one state, or those belonging to one product. The ones waiting are milder ratings somebody has proposed and nobody has agreed to yet, which are the ones not yet affecting anything.

        A rating belongs to one product, so a row says which. Two products may rate the same issue differently and both rows appear, each narrowed by what you may read in its own product.

        A claim carries the severity recorded against its issue, so claims about findings you cannot read are absent rather than refused.

        Requires: any signed-in person, and not a pipeline key. Narrowed to issues you may read a finding of in the product the rating belongs to. A rating is about one product, and an issue this deployment minted for a flaw nobody has announced is not public knowledge.
      operationId: list-assessments
      parameters:
        - description: Limit to one product, by name
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name
            type: string
        - description: Limit to one state
          explode: false
          in: query
          name: state
          schema:
            description: Limit to one state
            enum:
              - proposed
              - live
              - withdrawn
            type: string
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyAssessmentBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List issue assessments
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Narrowed to issues you may read a finding of in the product the rating belongs to. A rating is about one product, and an issue this deployment minted for a flaw nobody has announced is not public knowledge.
  /v1/assessments/{id}:
    delete:
      description: |-
        The rating in force in that product returns to the published one, and everything that reads it — where a finding sits in the list, how long it has, whether it is above the line the product triages — follows it back. No other product is touched.

        Asked of triage on the product the rating belongs to: taking a rating back is making one.

        Requires: public-triage or private-triage on the product. The product is the rating's own, not one in the path.
      operationId: withdraw-assessment
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw an assessment, and take the published rating back
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: The product is the rating's own, not one in the path.
  /v1/assessments/{id}/agreement:
    post:
      description: |-
        Only a milder rating waits for this. Somebody other than whoever proposed it, for the same reason every other second person here is somebody else: a control one person can complete alone is not a control.

        The second person holds their role on the product the rating belongs to. Agreeing is what puts a milder rating into force, so it moves that product's deadlines and its triage line; a rating in a product you hold nothing on answers as one that is not there.

        Requires: approver or public-triage or private-triage on the product. The proposer may not approve their own. The product is the rating's own, not one in the path.
      operationId: agree-assessment
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssessmentBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Approve a rating assessment
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - approver
          - public-triage
          - private-triage
        note: The proposer may not approve their own. The product is the rating's own, not one in the path.
  /v1/assignments:
    get:
      description: |-
        Returns everyone holding open work you can see, with how much.

        Counted in pieces of work — an issue in a component in a product — which is the unit the list behind each person is in. `places` says how many findings those cover: one flaw in a kernel is one thing to answer and can be dozens of rows to write.

        The number worth watching is not how many findings exist but how many are waiting behind somebody: an idle account holding nothing is harmless, and work stuck behind a person who has gone is the problem — nothing tells this software that somebody has left.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-holdings
      parameters:
        - description: Limit to work held in one product, by name. Empty means every product you can see; a name you cannot see is refused rather than answered empty
          explode: false
          in: query
          name: product
          schema:
            description: Limit to work held in one product, by name. Empty means every product you can see; a name you cannot see is refused rather than answered empty
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyHoldingBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List assignment totals
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/attachments/{token}:
    delete:
      description: |-
        Takes the bytes back out and leaves the record. The reference in the text stays and says the file was removed, which is the difference between a redaction and a hole in the record.

        Administrators only. A reason is required, and it is recorded and shown wherever the text referred to the file.

        Requires: administrator
      operationId: redact-attachment
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Redact-attachmentRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Remove an attached file
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
    get:
      description: |-
        Authorized against the issue the file hangs off — whoever may read the text may read what it refers to — and only then served.

        Two shapes, and a caller has to follow both. An image displayed in the page is sent from here; everything else answers 303 with a short-lived address at the store. Either way the content type and the disposition are the ones decided at upload, never the ones the file arrived with.

        A file an administrator removed answers 410: the record and the reference remain, and the bytes are gone on purpose.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: fetch-attachment
      parameters:
        - description: "The identifier text refers to, without the attachment: prefix"
          in: path
          name: token
          required: true
          schema:
            description: "The identifier text refers to, without the attachment: prefix"
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Fetch an attached file
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/audit:
    get:
      description: |-
        Every judgment recorded in a period, newest first, with what it was about, the reasoning it rests on, who proposed it and when, and who agreed and when — including agreements later taken back.

        The period is the date a judgment was proposed, not approved: a judgment belongs to when it was argued, and dating it by its agreement would move it out of that period whenever an approval came late, which is the ordinary case.

        Narrowed by what you may see, like every other list here. Nothing about this view is exempt from the visibility rules — a report showing more than the screens it summarizes would be a way around them.

        `alone=true` returns judgments no second person has a standing agreement on. That population is large and legitimate on its own — an outcome that hides nothing needs no second person, and a short deferral stands alone — so ask it with an outcome. Asked of a dismissal it should return nothing: `not-applicable`, `mismatched`, `wont-fix` and `already-fixed` all require approval, so a row in that answer is a control that failed.

        `in_force=true` returns what applies now rather than what was once agreed to. With `outcome=mismatched` that is the set of standing corrections: the matches this deployment has recorded as wrong, which no version bump expires.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-audit
      parameters:
        - description: Limit to these products, by name. Repeatable; any of them matches
          explode: true
          in: query
          name: product
          schema:
            description: Limit to these products, by name. Repeatable; any of them matches
            items:
              type: string
            type:
              - array
              - "null"
        - description: Limit to these kinds of judgment. Repeatable; any of them matches
          explode: true
          in: query
          name: outcome
          schema:
            description: Limit to these kinds of judgment. Repeatable; any of them matches
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: Limit to these states. Repeatable; any of them matches
          explode: true
          in: query
          name: state
          schema:
            description: Limit to these states. Repeatable; any of them matches
            items:
              enum:
                - proposed
                - approved
                - withdrawn
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: Only judgments proposed on or after this date, as YYYY-MM-DD
          explode: false
          in: query
          name: from
          schema:
            description: Only judgments proposed on or after this date, as YYYY-MM-DD
            type: string
        - description: Only judgments proposed before this date, as YYYY-MM-DD
          explode: false
          in: query
          name: to
          schema:
            description: Only judgments proposed before this date, as YYYY-MM-DD
            type: string
        - description: Only judgments no second person has a standing agreement on. Asked of a dismissal this should answer nothing
          explode: false
          in: query
          name: alone
          schema:
            description: Only judgments no second person has a standing agreement on. Asked of a dismissal this should answer nothing
            type: boolean
        - description: "Only judgments that apply now: agreed to, or standing without needing agreement, and still holding the place they were made about. A judgment can be approved and have lapsed since, which is why this is not the same as asking for the approved state. Every outcome that dismisses needs agreement, so asked of one of those this is what has been agreed to"
          explode: false
          in: query
          name: in_force
          schema:
            description: "Only judgments that apply now: agreed to, or standing without needing agreement, and still holding the place they were made about. A judgment can be approved and have lapsed since, which is why this is not the same as asking for the approved state. Every outcome that dismisses needs agreement, so asked of one of those this is what has been agreed to"
            type: boolean
        - description: Only judgments this person proposed, by sign-in identity
          explode: false
          in: query
          name: proposed_by
          schema:
            description: Only judgments this person proposed, by sign-in identity
            type: string
        - description: Only judgments this person has a standing agreement on, by sign-in identity. An agreement later taken back does not match
          explode: false
          in: query
          name: approved_by
          schema:
            description: Only judgments this person has a standing agreement on, by sign-in identity. An agreement later taken back does not match
            type: string
        - description: Only judgments about this vulnerability, under the name it is filed here
          explode: false
          in: query
          name: issue
          schema:
            description: Only judgments about this vulnerability, under the name it is filed here
            type: string
        - description: Only judgments about this component, by name
          explode: false
          in: query
          name: component
          schema:
            description: Only judgments about this component, by name
            type: string
        - description: Only judgments about places this branch or tag holds. Needs exactly one product and a variant
          explode: false
          in: query
          name: stream
          schema:
            description: Only judgments about places this branch or tag holds. Needs exactly one product and a variant
            type: string
        - description: The build of that stream. Needs exactly one product and a stream
          explode: false
          in: query
          name: variant
          schema:
            description: The build of that stream. Needs exactly one product and a stream
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 500
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-auditResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List judgments with who made them and who agreed
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/audit.{format}:
    get:
      description: |-
        The audit list as a file: every judgment the same filters would show, not one page of them.

        One row per judgment, with who proposed it, who has a standing agreement on it, and whether a second person does. Approvals are joined with `;` in the CSV because a spreadsheet has one cell per column and an auditor reads them as a list; the JSON keeps them as one field of the same shape.

        `agreements` is the whole of the record, with dates: who agreed, when, whether the agreement was carried from an earlier claim, and when it was taken back. `approved by` stays who agrees *now*, because those are different questions and a column mixing them is the one answer an auditor must not be given.

        Read with your own visibility, as it streams. Nothing about a report is exempt from the rules the screens follow — a file showing more than the screen that summarizes it would be a way around them.

        Takes every filter the audit list takes, including the period.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-audit
      parameters:
        - description: Limit to these products, by name. Repeatable; any of them matches
          explode: true
          in: query
          name: product
          schema:
            description: Limit to these products, by name. Repeatable; any of them matches
            items:
              type: string
            type:
              - array
              - "null"
        - description: Limit to these kinds of judgment. Repeatable; any of them matches
          explode: true
          in: query
          name: outcome
          schema:
            description: Limit to these kinds of judgment. Repeatable; any of them matches
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: Limit to these states. Repeatable; any of them matches
          explode: true
          in: query
          name: state
          schema:
            description: Limit to these states. Repeatable; any of them matches
            items:
              enum:
                - proposed
                - approved
                - withdrawn
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: Only judgments proposed on or after this date, as YYYY-MM-DD
          explode: false
          in: query
          name: from
          schema:
            description: Only judgments proposed on or after this date, as YYYY-MM-DD
            type: string
        - description: Only judgments proposed before this date, as YYYY-MM-DD
          explode: false
          in: query
          name: to
          schema:
            description: Only judgments proposed before this date, as YYYY-MM-DD
            type: string
        - description: Only judgments no second person has a standing agreement on. Asked of a dismissal this should answer nothing
          explode: false
          in: query
          name: alone
          schema:
            description: Only judgments no second person has a standing agreement on. Asked of a dismissal this should answer nothing
            type: boolean
        - description: "Only judgments that apply now: agreed to, or standing without needing agreement, and still holding the place they were made about. A judgment can be approved and have lapsed since, which is why this is not the same as asking for the approved state. Every outcome that dismisses needs agreement, so asked of one of those this is what has been agreed to"
          explode: false
          in: query
          name: in_force
          schema:
            description: "Only judgments that apply now: agreed to, or standing without needing agreement, and still holding the place they were made about. A judgment can be approved and have lapsed since, which is why this is not the same as asking for the approved state. Every outcome that dismisses needs agreement, so asked of one of those this is what has been agreed to"
            type: boolean
        - description: Only judgments this person proposed, by sign-in identity
          explode: false
          in: query
          name: proposed_by
          schema:
            description: Only judgments this person proposed, by sign-in identity
            type: string
        - description: Only judgments this person has a standing agreement on, by sign-in identity. An agreement later taken back does not match
          explode: false
          in: query
          name: approved_by
          schema:
            description: Only judgments this person has a standing agreement on, by sign-in identity. An agreement later taken back does not match
            type: string
        - description: Only judgments about this vulnerability, under the name it is filed here
          explode: false
          in: query
          name: issue
          schema:
            description: Only judgments about this vulnerability, under the name it is filed here
            type: string
        - description: Only judgments about this component, by name
          explode: false
          in: query
          name: component
          schema:
            description: Only judgments about this component, by name
            type: string
        - description: Only judgments about places this branch or tag holds. Needs exactly one product and a variant
          explode: false
          in: query
          name: stream
          schema:
            description: Only judgments about places this branch or tag holds. Needs exactly one product and a variant
            type: string
        - description: The build of that stream. Needs exactly one product and a stream
          explode: false
          in: query
          name: variant
          schema:
            description: The build of that stream. Needs exactly one product and a stream
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export the record of judgments
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/claims/{id}:
    delete:
      description: |-
        Withdraws the claim so none of it applies to any finding. A claim is one argument however many places it covers, so this takes back all of it; holding part of one back is setting rows aside, at `POST /v1/claims/{id}/approval`. The record is kept — a withdrawn claim reads as proposed, approved, then withdrawn. Requires no approval.

        Requires: public-triage or private-triage on the product
      operationId: withdraw-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw a triage claim
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
    get:
      description: |-
        Returns one claim whole: what it says, where it lands, what it covers now, and what became of it.

        A claim is one proposer's action however many decisions it wrote, so the outcome, the reason, the dates and the version are the claim's rather than any row's. `happened` reads the claim as a whole — `mixed` where its rows did not all end the same way — and `argument` names no decision, because nothing here acts on one.

        `rows`, `issues` and `places` are what it wrote. `folds`, `packages`, `consumers` and `findings` are what it covers now, which grows as builds appear with nobody acting; what somebody consented to is on the approval, at `GET /v1/claims/{id}/approvals`.

        For the earlier justifications see `GET /v1/claims/{id}/revisions` and for the discussion `GET /v1/claims/{id}/comments`.

        Requires: any signed-in person, and not a pipeline key. Answers only claims you may read every row of.
      operationId: get-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimDetail"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get a claim
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only claims you may read every row of.
  /v1/claims/{id}/approval:
    post:
      description: |-
        Approves every waiting decision in a claim as one action, under the same rules each decision is approved under: not by the person who proposed it, and against the revision of the reasoning that stands now.

        Name decisions in `except` to set them aside. The rest is approved; those are moved into a claim of their own, marked sent back, and `because` is recorded on each as a comment — the same way sending back records a reason. The response names that claim.

        Pass `batch` to approve several claims under one name, undone together with `DELETE /v1/approval-batches/{batch}`.

        Returns 404 for a claim you may not act on every row of, and 409 if you proposed it.

        Requires: approver or public-triage or private-triage on the product. The proposer may not approve their own.
      operationId: approve-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimApprovalBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimApprovedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Approve a claim
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - approver
          - public-triage
          - private-triage
        note: The proposer may not approve their own.
  /v1/claims/{id}/approvals:
    get:
      description: |-
        Returns every approval recorded against this claim, including ones later withdrawn, each naming the revision of the justification it was given for.

        An approval carrying `carried_from` was not given for this claim. A re-affirmation states its own reasoning and stands on the agreement its predecessor had, so the person named agreed to the earlier claim's words; `carried_from` is the approval where those are.

        A withdrawn approval is kept rather than deleted: who agreed to what, and when it stopped counting, is part of the record.

        `covered` is how many findings the claim covered when it was agreed to. A claim applies to every build running the same versions, so it covers more as builds appear — with nobody acting, and nobody having agreed to the larger number. Comparing this against what it covers now is the point of keeping it.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-claim-approvals
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyApprovalBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List who approved a claim
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/claims/{id}/comments:
    get:
      description: |-
        Returns the comments on a claim, oldest first, with who wrote each and when. A comment that has been edited also carries when it was last changed.

        Comments are separate from the justification and never affect an approval.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-claim-comments
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyCommentBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List comments on a claim
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Adds a markdown comment to a claim. Comments are separate from the justification and never affect an approval, so an approved claim can be annotated at any time.

        A comment may later be edited by its author only, and editing overwrites it rather than keeping revisions.

        Requires: approver or public-triage or private-triage on the product
      operationId: comment-on-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Comment-on-claimRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comment-on-claimResponse"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Add a comment to a claim
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - approver
          - public-triage
          - private-triage
  /v1/claims/{id}/elsewhere:
    put:
      description: |-
        Stores a link to a ticket, a thread or a change — and sends nothing to it. A stored link has no egress at all, which is what makes it available to a deployment that has not decided to let anything out; the half that *sends* is a separate thing a deployment configures.

        Anybody who may argue about the claim may set it: a link is a note about where the conversation is rather than a judgment, and needing a second person for it would leave it unset.

        Sent empty it is cleared. A stale link is worse than none — it sends somebody to a ticket that closed for a different reason.

        Requires: public-triage or private-triage on the product
      operationId: point-claim-elsewhere
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Point-claim-elsewhereRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record where this claim's work is happening
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/claims/{id}/promise:
    put:
      description: |-
        Changes the version a promised upgrade moves to, or the date it is promised by, on the claim and on every commitment it wrote.

        This withdraws any existing approval and returns every row of the claim to the review queue. An approver agreed to a version by a date; changing either is changing what they agreed to, so it goes through the same act revising the words does.

        `reasoning` is required and is recorded as a revision — saying why a date moved is what the second person has to read, and the revisions are where what was said before survives being changed.

        Requires: public-triage or private-triage on the product
      operationId: repromise-upgrade
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Repromise-upgradeRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Change what a release is moving to
      tags:
        - Remediation
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/claims/{id}/reaffirmation:
    post:
      description: |-
        Re-makes every row of this claim that stopped applying because an upstream version moved, at the versions each place has now, as one act with one reasoning.

        Deciding is bulk-capable and re-deciding was not. A team answering one kernel issue writes a decision at each of its places in one action; when the kernel moves, those lapse, and restoring them was one request each with a separately typed justification.

        Only the person who made the original may do this. It normally needs no second approver, for the reason the single form does not: two people already agreed, and a version upgrade is a prompt to re-check rather than a new claim.

        One act, one approval. Where any row would need approval again — the severity has risen since it was agreed to, or nothing was ever agreed to — the whole act does. An approver works at the unit the proposer acted at, and agreeing to part of an argument they were shown whole is not review.

        Bounded like the judgment it re-makes. The outcome comes from the claim, so re-affirming a bulk dismissal is a bulk judgment and is held to `triage.together-cap`; only a promise to upgrade goes through unbounded, because the next scan re-checks it.

        A place that is open nowhere any more is not re-made, which is a finding that closed rather than a fault. `reasoning` is required.

        Requires: public-triage or private-triage
      operationId: reaffirm-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Reaffirm-claimRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReaffirmedBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Re-affirm everything one action claimed
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        any_of:
          - public-triage
          - private-triage
  /v1/claims/{id}/reasoning:
    put:
      description: |-
        Replaces the justification text with a new revision. Earlier revisions are kept and remain readable.

        A claim is one argument however many places it covers, so this revises all of it. It withdraws any existing approval and returns every row of the claim to the review queue, marked as previously approved. Requires no approval of its own.

        The text is markdown and is validated before it is stored; a 422 names the line and the offending text.

        Requires: public-triage or private-triage on the product
      operationId: revise-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Revise-claimRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MentionsBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Update a claim's justification
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/claims/{id}/revisions:
    get:
      description: |-
        Returns every revision of the justification, oldest first, with who wrote each and when.

        A claim is one argument however many places it covers, so its reasoning is one text with one history. An approval names the specific revision that was agreed to, so this is how to read what an approver actually saw rather than what the text says now.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-claim-revisions
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyRevisionBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List a claim's justification history
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/claims/{id}/send-back:
    post:
      description: |-
        Asks the author for more before agreeing to any of a claim. Every waiting decision in it leaves the review queue together and comes back when the author revises.

        `because` is required and is recorded as a comment on each decision. Needs no approval of its own. You cannot send back a claim whose words are your own.

        Requires: approver or public-triage or private-triage on the product. The proposer may not approve their own.
      operationId: send-claim-back
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Send-claim-backRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Send a claim back for more
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - approver
          - public-triage
          - private-triage
        note: The proposer may not approve their own.
  /v1/claims/{id}/split:
    post:
      description: |-
        Moves the rows named into a claim of their own, belonging to you, carrying the argument they were made under and sitting with you rather than in the review queue. Revise it to give them an argument of their own.

        This is the proposer's side of setting rows aside, and each act is refused to the other: an approver holding rows back is agreeing to the rest in the same action, and a proposer doing that would be approving their own claim.

        `because` is required and is recorded as a comment on the new claim. Naming a row that is not part of the claim is refused rather than ignored, and so is naming all of what is still being argued — that is a revision or a withdrawal.

        Requires: public-triage or private-triage on the product
      operationId: split-claim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Split-claimRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Split-claimResponse"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Hold part of a claim back
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/comments/{id}:
    put:
      description: |-
        Replaces the text of a comment. Only its author may do this.

        What it said before is kept, and read back with `GET /v1/comments/{id}/history`. A comment is part of the record that goes public at disclosure, and a record whose earlier text is unrecoverable is readable rather than checkable — which is the property the whole append-only history exists for.

        The text is markdown and is validated before it is stored; a 422 names the line and the offending text.

        Requires: approver or public-triage or private-triage on the product. Only the author may edit a comment.
      operationId: edit-comment
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Edit-commentRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MentionsBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Edit a comment
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - approver
          - public-triage
          - private-triage
        note: Only the author may edit a comment.
  /v1/comments/{id}/history:
    get:
      description: |-
        Every version of a comment that has been replaced, oldest first. The comment itself carries what it says now.

        A comment is part of the record that goes public at disclosure, so what it said before has to be recoverable: an edit that overwrites leaves a record somebody can read and nobody can check.

        Answers only where you may read what the comment is about — the same rule as reading the comment itself, asked of the decision rather than of the comment, because two rules for one question is one rule out of step.

        Requires: public-triage or private-triage on the product
      operationId: get-comment-history
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyWasSaidBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List earlier revisions of a comment
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/compliance:
    get:
      description: |-
        By severity: how much closed inside its deadline, how much did not, how much is deferred by decision, and how much is plainly late.

        A deferral is its own number and not a failure, so `deferred` is neither met nor late.

        Counted in the same unit as every other screen: one issue at one component, not one row per place. A group is closed when no place is still open, met when none of the closed ones was late, deferred when every open place is covered by a standing deferral, and overdue when anything open is past its date and uncovered.

        A closed finding keeps the deadline it carried; only open ones lose theirs at end of life or below the triage line.

        A product is required — a place identity carries no product, so this cannot be asked across the deployment.

        A period, or the whole of it. `from` and `to` bound what closed in them, which is the number a report on a quarter or a financial year is about; `days` is the rolling window, and only one of the two may be sent. Asked for neither, this is the lifetime figure.

        What is open is always now. Deadlines are recomputed as the policy moves and dropped below the line and past end of life, so what stood open on a date gone by is not recoverable and is not reconstructed.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-compliance-rate
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OverPeriodRateBodyBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Report what proportion of work met its deadline
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/decisions:
    get:
      description: |-
        Returns triage decisions on products you may triage, newest first, with the justification text for each.

        Filter by `outcome` to list dismissals (`not-applicable`, `mismatched`, `wont-fix`, `already-fixed`) or postponements (`deferred`), by `state` to separate what is approved from what is still waiting or has been withdrawn, and by `product` to limit to one product.

        Set `expired=true` to list deferrals whose date has passed — the findings that have come back and need judging again.

        Set `stopped=true` for everything that has stopped standing — lapsed decisions and expired deferrals as one list. A decision can be both, so asking the two separately and adding the totals counts some of them twice.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-decisions
      parameters:
        - description: Limit to one product, by name
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name
            type: string
        - description: Limit to one outcome
          explode: false
          in: query
          name: outcome
          schema:
            description: Limit to one outcome
            enum:
              - affected
              - not-applicable
              - mismatched
              - deferred
              - wont-fix
              - already-fixed
              - upgrade-needed
              - patch-needed
            type: string
        - description: Limit to one state
          explode: false
          in: query
          name: state
          schema:
            description: Limit to one state
            enum:
              - proposed
              - approved
              - withdrawn
              - lapsed
            type: string
        - description: Only deferrals whose date has passed
          explode: false
          in: query
          name: expired
          schema:
            description: Only deferrals whose date has passed
            type: boolean
        - description: Lapsed decisions and expired deferrals as one list. A decision can be both, so the two asked separately do not add up
          explode: false
          in: query
          name: stopped
          schema:
            description: Lapsed decisions and expired deferrals as one list. A decision can be both, so the two asked separately do not add up
            type: boolean
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List triage decisions
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/decisions/{id}:
    get:
      description: |-
        Returns one decision with the justification as it currently stands, where it applies, who proposed it and how long it has stood.

        What it says — the outcome, the justification, the dates — belongs to the claim it is one row of, at `claim_id`. For the earlier justifications see `GET /v1/claims/{id}/revisions`, and for who agreed to which of them see `GET /v1/claims/{id}/approvals`.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-decision
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionDetail"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get a triage decision
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/deferrals/repeated:
    get:
      description: |-
        Places deferred more than once, most-deferred first, with how long they have been put off for in total.

        The cumulative threshold already refuses a further deferral past a point, one item at a time. What it cannot show is the shape across everything: one item deferred three times is a judgment, and forty of them is a policy nobody wrote down.

        Counted over the judgments rather than the findings they cover, so the order is not decided by how far a component spreads through an image.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-repeated-deferrals
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: The number of deferrals that makes something worth listing. One is an ordinary judgment
          explode: false
          in: query
          name: at_least
          schema:
            default: 2
            description: The number of deferrals that makes something worth listing. One is an ordinary judgment
            format: int64
            maximum: 50
            minimum: 2
            type: integer
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 500
            minimum: 1
            type: integer
        - description: The offset into the list
          explode: false
          in: query
          name: offset
          schema:
            description: The offset into the list
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyRepeatBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List repeated deferrals
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/deferrals/repeated.{format}:
    get:
      description: |-
        The same list as a file: places deferred more than once, most-deferred first, with how long they have been put off for in total.

        What the screen shows is a shape rather than a page — one item deferred three times is a judgment and forty of them is a policy nobody wrote down — and the file is what that goes into a review as.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-repeated-deferrals
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: The number of deferrals that makes something worth listing. One is an ordinary judgment
          explode: false
          in: query
          name: at_least
          schema:
            default: 2
            description: The number of deferrals that makes something worth listing. One is an ordinary judgment
            format: int64
            maximum: 50
            minimum: 2
            type: integer
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export repeated deferrals
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/disclosing:
    get:
      description: |-
        Returns findings nobody has announced whose embargo is running out, soonest first, and the ones whose date has already arrived.

        Before the date, not on it. The date arriving is the last moment to act on something rather than the first useful warning, and a list that only ever showed what was already past would be a list of decisions somebody has already failed to make.

        Nothing here discloses anything. Reaching the date escalates: the row appears and the people who can act on it are told. Publishing embargoed detail because a timer expired is the wrong default — if the fix is not ready, disclosing anyway is a decision a person makes.

        Every row is undisclosed by definition, so this list is a disclosure in its own right: a product you may not read undisclosed work in contributes nothing to it, not even a count.

        `within` is how many days ahead to look. Left off, it is this deployment's own embargo length — the screen opened on thirty days against a ninety-day policy and drew nothing while five embargoes were running.

        Requires: private-read or private-triage on the product. What you hold decides what comes back rather than whether you may ask. A product you may not read undisclosed work in contributes nothing, not even a count.
      operationId: list-approaching-disclosure
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The number of days ahead to look. Left off, this deployment's own embargo length
          explode: false
          in: query
          name: within
          schema:
            description: The number of days ahead to look. Left off, this deployment's own embargo length
            format: int64
            maximum: 365
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 500
            minimum: 1
            type: integer
        - description: The offset into the list
          explode: false
          in: query
          name: offset
          schema:
            description: The offset into the list
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyEmbargoedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List what is approaching disclosure
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-read
          - private-triage
        note: A product you may not read undisclosed work in contributes nothing, not even a count.
        narrowed: true
  /v1/disclosure-movements:
    get:
      description: |-
        Every request to move a disclosure date that nobody has agreed to yet, across the products you may read undisclosed work in, newest first. Both acts are here, and `act` says which each one is.

        Without this there is nowhere to be that second person. A request could be read on the finding it belongs to and nowhere else, so the only way to find one was to already know it existed — which is the failure the review queue exists to prevent, in the one place where what is being agreed to is how long something stays hidden.

        Your own requests are here too, marked as yours. You cannot agree to one — the endpoint refuses it — but a proposer looking for what is holding a case up should not have their own request hidden from them.

        Agree with `POST /v1/disclosure-movements/{id}/approval`.

        Requires: any signed-in person, and not a pipeline key. Only where you may read undisclosed work.
      operationId: list-pending-disclosure-movements
      parameters:
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The offset into the list
          explode: false
          in: query
          name: offset
          schema:
            description: The offset into the list
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyPendingMovementBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List disclosure-date movements waiting for a second person
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Only where you may read undisclosed work.
  /v1/disclosure-movements/{id}/approval:
    post:
      description: |-
        Records a second person agreeing, and moves the date. Either act.

        The person who asked may not be the one who agrees. That is the control the threshold exists to reach, and a movement somebody approved for themselves is the same as one nobody approved.

        Requires: private-triage on the product. Not the person who asked for it.
      operationId: agree-to-disclosure-movement
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Approve a disclosure-date movement
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-triage
        note: Not the person who asked for it.
  /v1/effort:
    get:
      description: |-
        What the judgments in a period were about, most argued first: which component in which product, how many arguments were made, how many places they reached, how many people made them, and what came out of them.

        Counted in claims, not in the rows they wrote. A claim is one person's act; counting its rows measures how far a component fans out through an image. Both numbers come back.

        Dated by when a judgment was proposed. Asked for neither a period nor a window, this is the last 90 days.

        Takes the same period and scope the other reports do, and is narrowed by what you may see.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-effort
      parameters:
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
        - description: Limit to judgments made in one product, by name
          explode: false
          in: query
          name: product
          schema:
            description: Limit to judgments made in one product, by name
            type: string
        - description: Limit to judgments this team's members proposed, by team name
          explode: false
          in: query
          name: team
          schema:
            description: Limit to judgments this team's members proposed, by team name
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OverPeriodSpentBodyBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Report where triage effort went
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/findings:
    get:
      description: |-
        The findings list, without a product picked.

        One row per product, issue and component. The same library carrying the same issue in two products is two pieces of work, decided separately by different people; in three builds of one product it is one row, and `builds` says how many. Each row names one of those builds so there is somewhere to link to.

        Every product's own triage line still applies. `severity` raises the line for the whole page and never lowers it below what a product decided; `below_floor` turns every line off, which is how the rows a line keeps out are asked for.

        An issue that exists only in products you hold nothing on answers as an issue that does not exist, and `total` says the same.

        `beneath` is not offered: a subtree is a walk over one build's edges. Neither is `differs`, which is a statement about a selection of builds. Both are on the per-product list, which is where a build can be named.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-findings-anywhere
      parameters:
        - description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only issues somebody is known to be exploiting
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only issues somebody is known to be exploiting
            type: boolean
        - description: Keep only issues where an upstream fixed version is known
          explode: false
          in: query
          name: fixable
          schema:
            description: Keep only issues where an upstream fixed version is known
            type: boolean
        - description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
          explode: false
          in: query
          name: below_floor
          schema:
            description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
            type: boolean
        - description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
          explode: true
          in: query
          name: component
          schema:
            description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
          explode: true
          in: query
          name: tag
          schema:
            description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
            maxLength: 200
            type: string
        - description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
          explode: true
          in: query
          name: ecosystem
          schema:
            description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
            items:
              maxLength: 64
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
          explode: true
          in: query
          name: "on"
          schema:
            description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
            items:
              enum:
                - branch
                - tag
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
          explode: true
          in: query
          name: support
          schema:
            description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
            items:
              enum:
                - in-support
                - past-eol
              type: string
            type:
              - array
              - "null"
        - description: Keep only what sits inside the container of this name
          explode: false
          in: query
          name: under
          schema:
            description: Keep only what sits inside the container of this name
            maxLength: 191
            type: string
        - description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
          explode: true
          in: query
          name: declared_as
          schema:
            description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
            items:
              enum:
                - required
                - optional
                - excluded
                - build
                - design
                - development
                - other
                - runtime
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what the build holds directly, which is what has no container above it
          explode: false
          in: query
          name: under_build
          schema:
            description: Keep only what the build holds directly, which is what has no container above it
            type: boolean
        - description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
          explode: true
          in: query
          name: state
          schema:
            description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
          explode: true
          in: query
          name: outcome
          schema:
            description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
          explode: true
          in: query
          name: assigned
          schema:
            description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
            items:
              enum:
                - me
                - somebody
                - nobody
              type: string
            type:
              - array
              - "null"
        - description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
          explode: false
          in: query
          name: epss_at_least
          schema:
            description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
            format: double
            maximum: 1
            minimum: 0
            type: number
        - description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
          explode: false
          in: query
          name: open_for
          schema:
            description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
          explode: false
          in: query
          name: due_within
          schema:
            description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what is already past its deadline
          explode: false
          in: query
          name: overdue
          schema:
            description: Keep only what is already past its deadline
            type: boolean
        - description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
          explode: true
          in: query
          name: fix_state
          schema:
            description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
            items:
              enum:
                - fixed
                - none
                - wont-fix
                - unknown
                - mixed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
          explode: true
          in: query
          name: weakness
          schema:
            description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
            items:
              maxLength: 32
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only groups where a claim is with its author, sent back for more
          explode: false
          in: query
          name: sent_back
          schema:
            description: Keep only groups where a claim is with its author, sent back for more
            type: boolean
        - description: Keep only what one of these VEX publishers has a standing statement about
          explode: true
          in: query
          name: vex_publisher
          schema:
            description: Keep only what one of these VEX publishers has a standing statement about
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only what was first seen here after this date, as 2026-03-31
          explode: false
          in: query
          name: opened_after
          schema:
            description: Keep only what was first seen here after this date, as 2026-03-31
            type: string
        - description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
          explode: false
          in: query
          name: opened_by_run
          schema:
            description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
          explode: false
          in: query
          name: closed_after
          schema:
            description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
            type: string
        - description: Keep only what somebody claimed something about after this date
          explode: false
          in: query
          name: proposed_after
          schema:
            description: Keep only what somebody claimed something about after this date
            type: string
        - description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
          explode: true
          in: query
          name: vex_status
          schema:
            description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
            items:
              enum:
                - not_affected
                - affected
                - fixed
                - under_investigation
              type: string
            type:
              - array
              - "null"
        - description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
          explode: false
          in: query
          name: reassessed
          schema:
            description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
            type: boolean
        - description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
          explode: false
          in: query
          name: origin
          schema:
            description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
            enum:
              - scanner
              - manual
            type: string
        - description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
          explode: false
          in: query
          name: planned
          schema:
            description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
            enum:
              - planned
              - unplanned
              - either
            type: string
        - description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
          explode: false
          in: query
          name: unconfirmed
          schema:
            description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
            type: boolean
        - description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
          explode: true
          in: query
          name: exclude
          schema:
            description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
          explode: false
          in: query
          name: sort
          schema:
            description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
            enum:
              - urgency
              - age
              - deadline
              - places
              - epss
              - severity
            type: string
        - description: Order the other way — oldest, nearest deadline, fewest places, lowest first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — oldest, nearest deadline, fewest places, lowest first
            type: boolean
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnywhereOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List findings across every product
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/findings.{format}:
    get:
      description: |-
        The cross-product findings list as a file: every row the same filters would show, not one page of them.

        Read with your own visibility, as it streams. It is the same query the screen reads, paged and written out as it goes.

        Each product applies its own triage line, so the file states that rather than naming one line, and `product` is a column.

        Takes every filter the cross-product list takes. `beneath` and `differs` are not offered here, for the reason that list gives.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-findings-anywhere
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only issues somebody is known to be exploiting
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only issues somebody is known to be exploiting
            type: boolean
        - description: Keep only issues where an upstream fixed version is known
          explode: false
          in: query
          name: fixable
          schema:
            description: Keep only issues where an upstream fixed version is known
            type: boolean
        - description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
          explode: false
          in: query
          name: below_floor
          schema:
            description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
            type: boolean
        - description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
          explode: true
          in: query
          name: component
          schema:
            description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
          explode: true
          in: query
          name: tag
          schema:
            description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
            maxLength: 200
            type: string
        - description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
          explode: true
          in: query
          name: ecosystem
          schema:
            description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
            items:
              maxLength: 64
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
          explode: true
          in: query
          name: "on"
          schema:
            description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
            items:
              enum:
                - branch
                - tag
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
          explode: true
          in: query
          name: support
          schema:
            description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
            items:
              enum:
                - in-support
                - past-eol
              type: string
            type:
              - array
              - "null"
        - description: Keep only what sits inside the container of this name
          explode: false
          in: query
          name: under
          schema:
            description: Keep only what sits inside the container of this name
            maxLength: 191
            type: string
        - description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
          explode: true
          in: query
          name: declared_as
          schema:
            description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
            items:
              enum:
                - required
                - optional
                - excluded
                - build
                - design
                - development
                - other
                - runtime
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what the build holds directly, which is what has no container above it
          explode: false
          in: query
          name: under_build
          schema:
            description: Keep only what the build holds directly, which is what has no container above it
            type: boolean
        - description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
          explode: true
          in: query
          name: state
          schema:
            description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
          explode: true
          in: query
          name: outcome
          schema:
            description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
          explode: true
          in: query
          name: assigned
          schema:
            description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
            items:
              enum:
                - me
                - somebody
                - nobody
              type: string
            type:
              - array
              - "null"
        - description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
          explode: false
          in: query
          name: epss_at_least
          schema:
            description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
            format: double
            maximum: 1
            minimum: 0
            type: number
        - description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
          explode: false
          in: query
          name: open_for
          schema:
            description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
          explode: false
          in: query
          name: due_within
          schema:
            description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what is already past its deadline
          explode: false
          in: query
          name: overdue
          schema:
            description: Keep only what is already past its deadline
            type: boolean
        - description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
          explode: true
          in: query
          name: fix_state
          schema:
            description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
            items:
              enum:
                - fixed
                - none
                - wont-fix
                - unknown
                - mixed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
          explode: true
          in: query
          name: weakness
          schema:
            description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
            items:
              maxLength: 32
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only groups where a claim is with its author, sent back for more
          explode: false
          in: query
          name: sent_back
          schema:
            description: Keep only groups where a claim is with its author, sent back for more
            type: boolean
        - description: Keep only what one of these VEX publishers has a standing statement about
          explode: true
          in: query
          name: vex_publisher
          schema:
            description: Keep only what one of these VEX publishers has a standing statement about
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only what was first seen here after this date, as 2026-03-31
          explode: false
          in: query
          name: opened_after
          schema:
            description: Keep only what was first seen here after this date, as 2026-03-31
            type: string
        - description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
          explode: false
          in: query
          name: opened_by_run
          schema:
            description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
          explode: false
          in: query
          name: closed_after
          schema:
            description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
            type: string
        - description: Keep only what somebody claimed something about after this date
          explode: false
          in: query
          name: proposed_after
          schema:
            description: Keep only what somebody claimed something about after this date
            type: string
        - description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
          explode: true
          in: query
          name: vex_status
          schema:
            description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
            items:
              enum:
                - not_affected
                - affected
                - fixed
                - under_investigation
              type: string
            type:
              - array
              - "null"
        - description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
          explode: false
          in: query
          name: reassessed
          schema:
            description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
            type: boolean
        - description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
          explode: false
          in: query
          name: origin
          schema:
            description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
            enum:
              - scanner
              - manual
            type: string
        - description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
          explode: false
          in: query
          name: planned
          schema:
            description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
            enum:
              - planned
              - unplanned
              - either
            type: string
        - description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
          explode: false
          in: query
          name: unconfirmed
          schema:
            description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
            type: boolean
        - description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
          explode: true
          in: query
          name: exclude
          schema:
            description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
          explode: false
          in: query
          name: sort
          schema:
            description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
            enum:
              - urgency
              - age
              - deadline
              - places
              - epss
              - severity
            type: string
        - description: Order the other way — oldest, nearest deadline, fewest places, lowest first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — oldest, nearest deadline, fewest places, lowest first
            type: boolean
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export findings across every product
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/issues/{vulnerability}:
    get:
      description: |-
        Every build that carries this issue, across every product you may see, with how far it has been decided in each.

        One row per build and component, not per place: the same component in two builds is two things somebody ships, and sixty places of it in one build is one piece of work with a count.

        Narrowed the way every other read is, per product and per visibility. A page that spans products is exactly where filtering afterwards gets forgotten, and the count is the leak even when no row is shown.

        Nothing affected is an answer, not a 404: `total` is zero and `items` is empty, which is what a customer inquiry is asking for. An identifier nobody here has seen answers the same way as one that sits only in products you cannot read — told apart, the pair would say which issues this deployment holds, one guess at a time.

        Answers by any name the issue goes by.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-issue
      parameters:
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 200
            format: int64
            maximum: 500
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List findings for one issue across every product
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/issues/{vulnerability}/document:
    get:
      description: |-
        What the issue is, every build of yours that carries it, what was decided about each and the reasoning behind it, as markdown — the form a customer inquiry is answered from.

        It is an internal document and says so. The reasoning is this deployment's own argument; what goes to a customer is the advisory or the VEX document, both of which are assembled elsewhere and say less on purpose.

        Narrowed by what you may see, like every other read: two people asking get different documents rather than one of them getting an error.

        Nothing of yours affected is an answer, and the document says that rather than refusing — which is what the inquiry is usually asking.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-issue-document
      parameters:
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Render everything known about one issue
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/keys:
    get:
      description: |-
        Which credentials exist, what each may send, when it was last used and whether it still works. The secrets are not here and cannot be: what is stored is a digest.

        Requires: administrator
      operationId: list-keys
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyKeyBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List API keys
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
    post:
      description: |-
        Creates a credential a build may send scans with, and returns its secret. The secret is shown once and stored hashed: a credential store that can hand back what it holds gives up every pipeline's key with a copy of the database.

        Requires a session. A credential cannot create another, and a key created by one would outlive it.

        Requires: administrator
      operationId: create-key
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KeyBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeclaredKeyBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create an API key
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/keys/{name}:
    delete:
      description: |-
        Stops it working, without removing the record of what it sent. Revoking one credential leaves every other pipeline running.

        Requires: administrator
      operationId: revoke-key
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw an API key
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/measures:
    get:
      description: |-
        Four figures about how this deployment is working, as against what it holds: how long a finding sits before anybody proposes anything, how long a claim waits for a second person, what each person got through, and how much came back.

        The two waits come back three ways each — the middle, what nine in ten came in under, and the longest — so a caller reads three numbers per wait rather than one.

        Per severity, because a critical waiting a week and a low waiting a week are not the same fact.

        A period or a rolling window. `from` and `to` name the stretch a manager or an auditor is reporting on; `days` is the rolling window, and the two are ways of saying the same thing so only one may be sent.

        Bounded, and it says so. The two waits are worked out from at most the most recent few thousand claims in the window; `sampled` says how many and `capped` says whether the ceiling was reached. A figure quoted from part of a window without saying so is the one thing a number like this must not be.

        Send-backs are counted for the deployment rather than per person: the record holds that a claim was sent back and not by whom, and the reason travels as a comment.

        Narrowed to what you may read, like every count here — so two people asking get different answers rather than one of them getting an error.

        Asked for neither a period nor a window, this is the last 90 days.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-measures
      parameters:
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
        - description: Limit to judgments made in one product, by name
          explode: false
          in: query
          name: product
          schema:
            description: Limit to judgments made in one product, by name
            type: string
        - description: Limit to judgments this team's members proposed, and to what they themselves agreed to and withdrew, by team name
          explode: false
          in: query
          name: team
          schema:
            description: Limit to judgments this team's members proposed, and to what they themselves agreed to and withdrew, by team name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeasuresBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show how long triage is taking and who is doing it
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/my-claims:
    get:
      description: |-
        Returns the claims you proposed, newest first, with what happened to each: still waiting, sent back, approved, withdrawn, lapsed, an agreement undone, or several of those where a claim's rows ended differently.

        This is the other half of the review queue's `mine=true`, which lists only what is still pending — so approved, withdrawn, lapsed and undone all present there as the row disappearing. Approval itself sends no message; this is where it is seen.

        Narrowed to what you may still read.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-my-claims
      parameters:
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BecameOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the outcomes of your own claims
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/notes/{id}:
    put:
      description: |-
        Replaces the text of a note. Only its author may do this: an edit another person could make is not a correction.

        What it said before is kept, and read back with `GET /v1/notes/{id}/history`. A note is part of the record that goes public at disclosure, and a record whose earlier text is unrecoverable is readable rather than checkable.

        The text is markdown and is validated before it is stored; a 422 names the line and the offending text.

        Requires: public-triage or private-triage on the product. Only the author may edit a note. The product is the note's own, not one in the path.
      operationId: edit-issue-note
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Edit-issue-noteRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteWritten"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Edit a note on an issue
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: Only the author may edit a note. The product is the note's own, not one in the path.
  /v1/notes/{id}/history:
    get:
      description: |-
        Every version of a note that has been replaced, oldest first. The note itself carries what it says now.

        A note is part of the record that goes public at disclosure, so what it said before has to be recoverable: an edit that overwrites leaves a record somebody can read and nobody can check.

        Answers only where you may read what the note is about — the same rule as reading the note itself, asked of the issue rather than of the note, because two rules for one question is one rule out of step.

        Requires: any signed-in person, and not a pipeline key. Answers only where you may read what the note is about, which is the note's own product and issue rather than anything in the path.
      operationId: get-issue-note-history
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyWasSaidBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List earlier revisions of a note
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only where you may read what the note is about, which is the note's own product and issue rather than anything in the path.
  /v1/notifications:
    delete:
      description: |-
        Takes everything off your list at once, and says how many that was. Conditions that are still true will not come back while they hold.

        Requires: your own credential
      operationId: acknowledge-all-notifications
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Acknowledge-all-notificationsResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Acknowledge everything waiting on you
      tags:
        - Notifications
      x-openpsirt-requires:
        scope: self
    get:
      description: |-
        Returns what you have not dealt with, newest first, and how many there are.

        Everyone has one of these, and what appears in it differs by what you hold: work arriving, a dismissal sent back, an approval an edit withdrew, or — for an administrator — that the tool itself is unwell.

        Two lifetimes, and the difference matters to a caller. An event happened once and goes away when you acknowledge it. A condition is true while something is true and clears itself when that stops, so a build that resumes being scanned leaves this list without anybody dismissing it.

        Requires: your own credential
      operationId: list-notifications
      parameters:
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List your notifications
      tags:
        - Notifications
      x-openpsirt-requires:
        scope: self
  /v1/notifications/{id}:
    delete:
      description: |-
        Takes one off your list.

        Yours only. A notification identifier is a number a caller supplies, and one belonging to somebody else answers the same way as one that does not exist.

        Acknowledging a condition hides it rather than resolving it: what it is about is still true, and the pass that derives it will not raise it again while it holds.

        Requires: your own credential
      operationId: acknowledge-notification
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Acknowledge one notification
      tags:
        - Notifications
      x-openpsirt-requires:
        scope: self
  /v1/outbound:
    get:
      description: |-
        The destinations configured, which kinds go to each, and whether they are working.

        The signing secret is never returned. It signs our requests rather than authenticating anybody to us, so it has to be stored recoverably — and showing it would put a shared secret on a page.

        Requires: administrator
      operationId: list-outbound
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyOutboundBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List where this deployment sends things
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
    post:
      description: |-
        Records a destination: a URL, a shared secret to sign with, and which kinds go there — one kind by name, or `*` for all of them.

        One signed request, not an adapter each. Slack, Teams, a tracker driven by automation and paging all take an HTTP request with a JSON body, so one shape reaches all of them.

        What it carries is what the channel rules already allow. A notification about a finding nobody has announced carries the fact that there is something and a link, and nothing else — the same body a mail would carry, composed by the same code.

        Every request is signed. `X-OpenPSIRT-Timestamp` and `X-OpenPSIRT-Signature: sha256=…`, an HMAC over the timestamp, a dot, and the body — so a receiver can tell one of ours from one anybody could make, and cannot replay yesterday's.

        https only, and a redirect is refused rather than followed. The body is signed and not encrypted, and a redirect asks us to send a signed request somewhere else, which is what the restriction exists to prevent.

        Requires: administrator
      operationId: add-outbound
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Add-outboundRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutboundBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Send a kind of notification somewhere
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/outbound/{name}/{kind}:
    delete:
      description: |-
        Takes a destination out of use. What was already sent stays recorded, because where something went is a question asked afterwards.

        Requires: administrator
      operationId: retire-outbound
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
        - in: path
          name: kind
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Stop sending a kind somewhere
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people:
    get:
      description: |-
        Lists everybody who may sign in, with the roles each of them holds and the products those apply to.

        Nobody appears here by having authenticated. Access is granted in advance, so this list is what an administrator has decided rather than who has turned up.

        `product` and `role` narrow it to the grants in force. The approvers on one product are what an access review asks for, and reading that off a list of everybody is reading the grid sideways. A grant that is not in force does not match: holding is a statement about now.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: list-people
      parameters:
        - description: Keep only people holding something on this product, by the name that addresses it
          explode: false
          in: query
          name: product
          schema:
            description: Keep only people holding something on this product, by the name that addresses it
            type: string
        - description: Keep only people holding this role
          explode: false
          in: query
          name: role
          schema:
            description: Keep only people holding this role
            enum:
              - approver
              - assigner
              - public-read
              - private-read
              - public-triage
              - private-triage
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyPersonBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List users
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
    post:
      description: |-
        Records somebody so that they may sign in, and optionally what they hold. Recording the same person again confirms them and adds any roles named.

        Requires a session. A personal token cannot record a person, because the account it would create outlives the token and is not bounded by it.

        Requires: administrator
      operationId: record-person
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeclaredPersonBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a user and grant roles
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/tokens:
    get:
      description: |-
        Lists every personal token in the deployment, whose it is, what it may send and when it was last used.

        Without it a stale token is found only when somebody leaves and nobody knows what breaks if it is turned off.

        Requires: administrator
      operationId: list-all-tokens
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyTokenBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List all users' API tokens
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}:
    get:
      description: |-
        Returns one person: the roles in force for them, every grant and withdrawal against them, how much of the triage record they proposed and agreed to, and what this deployment has told them.

        `told` includes what they have already acknowledged and what has since cleared, because the question it answers is what was sent rather than what is waiting. It is not narrowed by what they may read now: a line about an undisclosed finding, sent while they held the role that reached it, is exactly what an investigation is looking for.

        `held` and `told` are the first page of each; `held_total` and `told_total` say how many there are.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: read-person
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            maxLength: 191
            type: string
        - description: The number of each list returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number of each list returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AboutPersonBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Read one user
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
  /v1/people/{identity}/assignments:
    get:
      description: |-
        The open findings assigned to somebody, most urgent first, in the same units as what nobody is dealing with: one item per issue in a component in a product, not one per build. The same code built several ways is one piece of work, and it was taken on as one.

        Send `me` as the identity for your own.

        An identity nobody holds answers with an empty list rather than a 404, which is also what an identity somebody holds answers when none of their work is yours to see. The two are deliberately the same.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see. An identity nobody holds answers as one whose work you cannot see.
      operationId: list-assigned
      parameters:
        - description: Their sign-in identity, or 'me' for your own
          in: path
          name: identity
          required: true
          schema:
            description: Their sign-in identity, or 'me' for your own
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-assignedResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List what one person is dealing with
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see. An identity nobody holds answers as one whose work you cannot see.
  /v1/people/{identity}/assignments/hand-back:
    post:
      description: |-
        Returns all their open findings to the unassigned list. For when somebody has left, or their last role is removed.

        Nothing tells this software that somebody has gone — membership is read at sign-in, and a person who has left never signs in again. So this is an action an administrator takes rather than something that happens on its own, and until it is taken their work is in no list at all: not in the shared one because it is assigned, and not in anybody's own because they are not here.

        Send `to` instead to hand it to a named person rather than to nobody.

        Requires: administrator
      operationId: hand-back-assignments
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Hand-back-assignmentsRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Hand-back-assignmentsResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Hand back everything one person is dealing with
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}/deactivation:
    delete:
      description: |-
        Lets somebody who was deactivated sign in again, with whatever they still hold. Their roles were never withdrawn, so nothing has to be granted back.

        What they were dealing with was handed back when they left and is not returned to them: somebody else may have picked it up, and reassigning it here would take it off them silently.

        Requires: administrator
      operationId: reactivate-person
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            maxLength: 191
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Reactivate a user
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
    put:
      description: |-
        Records that somebody has left. They are refused at every way in — a session, a personal token, a group-bound sign-in — from the next request onward, their sessions are ended, and what they were dealing with is handed back so it is not held by somebody who is gone.

        Nobody is deleted. The record names them as the proposer of judgments and the approver of others, and their roles are left where they are: what somebody held is part of why the record reads as it does, and bringing them back should not mean reconstructing it. What stops them is the date.

        Deactivating somebody already deactivated succeeds and moves nothing, because the date is when they left.

        `released` says how much work was handed back.

        Requires: administrator
      operationId: deactivate-person
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            maxLength: 191
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Deactivate-personResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Deactivate a user
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}/identifier:
    delete:
      description: |-
        Clears the identifier a sign-in provider pinned to somebody, so that the next person to arrive under their username binds it again. Their authorization and their roles are untouched.

        Use it after changing sign-in provider. An identifier belongs to the provider that issued it, so every account pinned to the old one is refused once a new one is configured: the name matches and the identifier does not.

        It re-opens the window a pinned identifier closes, in which whoever arrives under that username is taken to be its holder. Do it when you expect them to sign in.

        Requires: administrator
      operationId: unbind-identifier
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Unbind a user's provider identifier
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}/roles/{product}/{role}:
    delete:
      description: |-
        Withdraws one role from one person. Takes effect at their next request; end their sessions to cut them off now.

        If it was their last role on that product, everything they were dealing with there goes back to the unassigned list. Otherwise that work is in no list at all: not in the shared one because it is assigned, and not in theirs because they can no longer open it. `released` says how much moved.

        The grant is removed rather than marked as ended. What somebody used to hold is answered by the record of what they did, so this list only ever says what is true today.

        Requires: administrator
      operationId: withdraw-role
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            type: string
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: role
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Withdraw-roleResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw a user's role on a product
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}/roles/{role}:
    delete:
      description: |-
        Withdraws a role held across the estate. Takes effect at their next request; end their sessions to cut them off now.

        It leaves no per-product grants in its place: anything still wanted on one product is granted there deliberately. Roles held against a named product are untouched, and are withdrawn one at a time through the path that names the product.

        Where this was their last role in a product, what they were dealing with there goes back to the unassigned list. `released` says how much moved in total.

        Requires: administrator
      operationId: withdraw-estate-role
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            type: string
        - in: path
          name: role
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Withdraw-estate-roleResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw a user's role on every product
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}/sessions:
    delete:
      description: |-
        Takes effect at once, whichever copy of the application answers next. Roles and group mappings are re-read at sign-in, so withdrawing one takes effect then; this is what makes somebody leaving immediate instead.

        Requires: administrator
      operationId: end-sessions
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: End all of a user's sessions
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/people/{identity}/tokens/{name}:
    delete:
      description: |-
        Revokes one person's token on their behalf. It stops working immediately; the record of what it sent stays.

        An owner withdraws their own through their own token paths. This is for the ones whose owner is no longer here to do it.

        Requires: administrator
      operationId: revoke-anyones-token
      parameters:
        - in: path
          name: identity
          required: true
          schema:
            type: string
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw another user's API token
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/products:
    get:
      description: |-
        Lists the products declared here, with what each one holds.

        A scan may only be filed against something declared, so this is the first question to ask after an upload is refused for naming something unknown.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-products
      parameters:
        - description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
          explode: false
          in: query
          name: counts
          schema:
            description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyProductBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List products
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Records a product so scans may be filed against it. Declaring one that already exists succeeds without changing anything, so this can run on every build.

        Requires: administrator
      operationId: declare-product
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeclaredProductBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a product
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}:
    delete:
      description: |-
        Takes a product out of use. It is offered nowhere and no scan may be filed against it. Everything already filed against it stays: its findings are still open, its decisions still stand, and the documents published for it still name it.

        Its releases and variants go out of every list with it and are otherwise left as they are. Declaring the product again brings it back with them.

        An end-of-support date is a separate setting with a separate effect: it takes the deadline off what is open and leaves the product listed and scannable.

        Requires: administrator
      operationId: retire-product
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retire a product
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
    patch:
      description: |-
        Corrects what a product is called. Both names are left alone where the request omits them.

        The name is what scans, paths and published documents use. It is refused once a VEX document has gone out for any build of this product, or a published advisory covers it. Retire the product and declare the intended one instead.

        The displayed name is what screens show and what a document names the product in prose. It may be corrected at any time.

        Requires: administrator
      operationId: amend-product
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Amend-productRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Amend a product
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/comparison:
    get:
      description: |-
        Returns what was fixed, what is newly present, and what is still there between two builds of one product.

        Between any two, not only adjacent ones: what a release note has to answer is usually about the last release a customer has, which is rarely the previous one.

        Each fixed entry says why it went, because "fixed by upgrading" and "fixed by a carried patch" are different sentences to a reader. `superseded` is the one to read carefully — it means the version moved and the issue came with it, so it was not fixed at all.

        A still-present entry carrying `arrived_from` is the same failure seen from the other side: somebody moved that version since the earlier build and the issue came with it, so the upgrade did not reach the fix.

        Public findings only unless you ask otherwise. Its destination is usually a public document, so including something undisclosed should be deliberate rather than something pasted in without noticing.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: compare-releases
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The earlier build's stream — a branch or a tag
          explode: false
          in: query
          name: from
          required: true
          schema:
            description: The earlier build's stream — a branch or a tag
            type: string
        - description: The earlier build's variant
          explode: false
          in: query
          name: from_variant
          required: true
          schema:
            description: The earlier build's variant
            type: string
        - description: The later build's stream
          explode: false
          in: query
          name: to
          required: true
          schema:
            description: The later build's stream
            type: string
        - description: The later build's variant
          explode: false
          in: query
          name: to_variant
          required: true
          schema:
            description: The later build's variant
            type: string
        - description: Include findings nobody has disclosed
          explode: false
          in: query
          name: include_undisclosed
          schema:
            description: Include findings nobody has disclosed
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComparisonBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Compare two builds
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/comparison.{format}:
    get:
      description: |-
        What was fixed, what is newly present and what is still there between two builds, as a file.

        One file with a `change` column rather than three files: it is one comparison, and three of anything that has to be kept together is three chances to send somebody two of them.

        Each fixed row says why it went. `superseded` is the one to read carefully — the version moved and the issue came with it, so it was not fixed at all. A still-present row carrying `arrived from` is that same failure from the other side.

        Public findings only unless you ask otherwise, because the destination is usually a public document.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-comparison
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: The earlier build's stream — a branch or a tag
          explode: false
          in: query
          name: from
          required: true
          schema:
            description: The earlier build's stream — a branch or a tag
            type: string
        - description: The earlier build's variant
          explode: false
          in: query
          name: from_variant
          required: true
          schema:
            description: The earlier build's variant
            type: string
        - description: The later build's stream
          explode: false
          in: query
          name: to
          required: true
          schema:
            description: The later build's stream
            type: string
        - description: The later build's variant
          explode: false
          in: query
          name: to_variant
          required: true
          schema:
            description: The later build's variant
            type: string
        - description: Include findings nobody has disclosed
          explode: false
          in: query
          name: include_undisclosed
          schema:
            description: Include findings nobody has disclosed
            type: boolean
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export a comparison of two builds
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/products/{product}/comparison/notes:
    get:
      description: |-
        The same comparison as markdown, in the form somebody pastes into a release note. Returned as `text/markdown` rather than as a string in a JSON field, because the point of it is that it goes straight in.

        It carries what was fixed and nothing else. Not what is still present, not what newly appeared, and not an upgrade that carried the issue with it — those are statements about what a build contains, and the document for them is a VEX, which a customer's own scanner reads. The comparison itself still answers all three.

        Worst first and stably ordered, so two runs over the same pair of builds produce the same document. A lead line names both builds, the day, and the scanner and vulnerability-database versions the later build was last measured with.

        Public findings only unless you ask otherwise, as the comparison itself is. Where fixes are left out for not having been disclosed, the note says how many and never which.

        A release that fixed nothing answers with a sentence saying so, not with an empty body: zero bytes is also what a truncated response and the wrong pair of builds look like.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-release-notes
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The earlier build's stream
          explode: false
          in: query
          name: from
          required: true
          schema:
            description: The earlier build's stream
            type: string
        - description: The earlier build's variant
          explode: false
          in: query
          name: from_variant
          required: true
          schema:
            description: The earlier build's variant
            type: string
        - description: The later build's stream
          explode: false
          in: query
          name: to
          required: true
          schema:
            description: The later build's stream
            type: string
        - description: The later build's variant
          explode: false
          in: query
          name: to_variant
          required: true
          schema:
            description: The later build's variant
            type: string
        - description: Include findings nobody has disclosed
          explode: false
          in: query
          name: include_undisclosed
          schema:
            description: Include findings nobody has disclosed
            type: boolean
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Render a comparison as release notes
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/components/{component}:
    get:
      description: |-
        What each build ships, what is open against it there, where it could go, and what has already been promised.

        Answered per build, because the answer differs by build. A stream staying on a maintained older line and a stream that has moved on are different work with different testing, and one target across both would be wrong for one of them.

        Where it could go carries two counts. `fixed_here` is how many of what is open name that exact version as their fix, which is the release's own security content; `reached` is how many the upgrade closes altogether, counting everything fixed at or before it. The second is the one somebody choosing a version is asking about, and it needs the ecosystem's ordering: where that is not defined the two counts are equal, `ordered` is false, and the list is not ranked. Ranked on `fixed_here` a quiet release late on a maintained line sorts near the bottom while carrying every fix before it.

        A build is listed because it ships the component, not because something is open against it. A package carrying nothing of its own still answers with the version it ships and how many things pull it in, which is the ordinary case for anything vendored in pre-built.

        One entry per version rather than per build. A build shipping a name at two versions holds two components, and they are two different pieces of code to decide about.

        `due_at` is what a commitment about that build is gated against, and is absent where nothing is open.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-component
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyPerBuildBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show a component across the builds that carry it
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/components/{component}/upgrade:
    post:
      description: |-
        Answers everything open against a component in the releases named, in one act: each is recorded as `upgrade-needed`, carrying the version it moves to and the date the work will be done.

        Coverage is the component, not a version pair. Moving to 3.5.2 is a claim that it answers what is open on the package — including findings recorded as fixed in 3.5.0 — and the next scan says which of that was true. Deciding it from the versions would need an ordering per ecosystem this does not have.

        Whether a second person agrees depends on the date. At or before the earliest deadline among what this covers, it stands on its own: nothing is hidden for longer than the policy already allowed. Past it, the promise defers the worst thing it covers and it waits for approval. The response says which.

        Not bounded, unlike a bulk judgment: this one writes as many rows as the component has open findings in the releases named.

        Saying who carries it is part of the act, not a second one: name a `person` or a `team`, and every finding the promise covers is handed to them in the same transaction, so a promise nobody is carrying and a holder with no promise are both impossible. A team is a perfectly good holder — moving a package is work a queue tracks rather than a judgment one person makes — and it stays unheld until somebody on it takes it. The handover is product-wide like every other, because the promise is per build and who is carrying the work is not.

        Requires: public-triage or private-triage on the product
      operationId: plan-upgrade
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlanUpgradeBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlannedUpgradeBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record moving a component as the answer to what is open against it
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/end-of-life:
    put:
      description: |-
        Sets the date support ends for every release of a product that has not stated its own. Past it, nothing on the product carries a remediation deadline and a build that stops being scanned is expected rather than a fault.

        Nothing is deleted or hidden: the findings and the history stay, and stay reportable. What ends is what is expected of us.

        An empty date clears it, because extended support happens. Deadlines are rewritten afterwards, away from the request; the response returns before that has finished.

        Requires: administrator
      operationId: set-product-end-of-life
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndOfLifeBody"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set when a product goes out of support
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/findings:
    get:
      description: |-
        Returns one row per vulnerability-and-component pair, not one row per place the component appears. Each row gives the number of places it occupies and how many of those the build's VEX documents already answer.

        Ordered by urgency — known-exploited first, then whether the build ships to customers, then severity, then likelihood. Supports limit and offset.

        Every filter is applied here, and `total` counts what it admits rather than what the page holds.

        `under` keeps what one container holds directly; `beneath` keeps what sits at a component or anywhere under it, which is what the dependency tree's cumulative count counts. The tree counts distinct issues and this list is one row per issue and component, so a subtree holding one issue at two components is two rows here and one there.

        `stream` and `variant` are optional and independent. With both named the list is one build. With either left out it answers for every build under the product that matches the rest: a row is still one issue in one component, its place count is across every build it is in, and `builds` says how many those are. Across more than one build a row carries `stream` and `variant` naming one of them to link to, and carries no `owner`, `parent`, `middle` or `chains` — a chain belongs to one build's graph. `beneath` is a walk over one build's edges and is refused unless both are named.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-findings
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: Limit to one branch or tag. Left out, every one under the product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Left out, every one under the product
            type: string
        - description: Limit to one variant. Left out, every one under the product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Left out, every one under the product, and independent of the branch
            type: string
        - description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
          explode: false
          in: query
          name: beneath
          schema:
            description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
            type: string
        - description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
          explode: false
          in: query
          name: differs
          schema:
            description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
            type: boolean
        - description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
          explode: false
          in: query
          name: across_variants
          schema:
            description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
            enum:
              - only
              - every
            type: string
        - description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only issues somebody is known to be exploiting
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only issues somebody is known to be exploiting
            type: boolean
        - description: Keep only issues where an upstream fixed version is known
          explode: false
          in: query
          name: fixable
          schema:
            description: Keep only issues where an upstream fixed version is known
            type: boolean
        - description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
          explode: false
          in: query
          name: below_floor
          schema:
            description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
            type: boolean
        - description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
          explode: true
          in: query
          name: component
          schema:
            description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
          explode: true
          in: query
          name: tag
          schema:
            description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
            maxLength: 200
            type: string
        - description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
          explode: true
          in: query
          name: ecosystem
          schema:
            description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
            items:
              maxLength: 64
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
          explode: true
          in: query
          name: "on"
          schema:
            description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
            items:
              enum:
                - branch
                - tag
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
          explode: true
          in: query
          name: support
          schema:
            description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
            items:
              enum:
                - in-support
                - past-eol
              type: string
            type:
              - array
              - "null"
        - description: Keep only what sits inside the container of this name
          explode: false
          in: query
          name: under
          schema:
            description: Keep only what sits inside the container of this name
            maxLength: 191
            type: string
        - description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
          explode: true
          in: query
          name: declared_as
          schema:
            description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
            items:
              enum:
                - required
                - optional
                - excluded
                - build
                - design
                - development
                - other
                - runtime
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what the build holds directly, which is what has no container above it
          explode: false
          in: query
          name: under_build
          schema:
            description: Keep only what the build holds directly, which is what has no container above it
            type: boolean
        - description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
          explode: true
          in: query
          name: state
          schema:
            description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
          explode: true
          in: query
          name: outcome
          schema:
            description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
          explode: true
          in: query
          name: assigned
          schema:
            description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
            items:
              enum:
                - me
                - somebody
                - nobody
              type: string
            type:
              - array
              - "null"
        - description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
          explode: false
          in: query
          name: epss_at_least
          schema:
            description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
            format: double
            maximum: 1
            minimum: 0
            type: number
        - description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
          explode: false
          in: query
          name: open_for
          schema:
            description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
          explode: false
          in: query
          name: due_within
          schema:
            description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what is already past its deadline
          explode: false
          in: query
          name: overdue
          schema:
            description: Keep only what is already past its deadline
            type: boolean
        - description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
          explode: true
          in: query
          name: fix_state
          schema:
            description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
            items:
              enum:
                - fixed
                - none
                - wont-fix
                - unknown
                - mixed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
          explode: true
          in: query
          name: weakness
          schema:
            description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
            items:
              maxLength: 32
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only groups where a claim is with its author, sent back for more
          explode: false
          in: query
          name: sent_back
          schema:
            description: Keep only groups where a claim is with its author, sent back for more
            type: boolean
        - description: Keep only what one of these VEX publishers has a standing statement about
          explode: true
          in: query
          name: vex_publisher
          schema:
            description: Keep only what one of these VEX publishers has a standing statement about
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only what was first seen here after this date, as 2026-03-31
          explode: false
          in: query
          name: opened_after
          schema:
            description: Keep only what was first seen here after this date, as 2026-03-31
            type: string
        - description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
          explode: false
          in: query
          name: opened_by_run
          schema:
            description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
          explode: false
          in: query
          name: closed_after
          schema:
            description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
            type: string
        - description: Keep only what somebody claimed something about after this date
          explode: false
          in: query
          name: proposed_after
          schema:
            description: Keep only what somebody claimed something about after this date
            type: string
        - description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
          explode: true
          in: query
          name: vex_status
          schema:
            description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
            items:
              enum:
                - not_affected
                - affected
                - fixed
                - under_investigation
              type: string
            type:
              - array
              - "null"
        - description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
          explode: false
          in: query
          name: reassessed
          schema:
            description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
            type: boolean
        - description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
          explode: false
          in: query
          name: origin
          schema:
            description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
            enum:
              - scanner
              - manual
            type: string
        - description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
          explode: false
          in: query
          name: planned
          schema:
            description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
            enum:
              - planned
              - unplanned
              - either
            type: string
        - description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
          explode: false
          in: query
          name: unconfirmed
          schema:
            description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
            type: boolean
        - description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
          explode: true
          in: query
          name: exclude
          schema:
            description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
          explode: false
          in: query
          name: sort
          schema:
            description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
            enum:
              - urgency
              - age
              - deadline
              - places
              - epss
              - severity
            type: string
        - description: Order the other way — oldest, nearest deadline, fewest places, lowest first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — oldest, nearest deadline, fewest places, lowest first
            type: boolean
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FindingsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List vulnerability findings
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Records a vulnerability in your own product — one no scanner reported, usually because nobody outside knows about it yet.

        It starts undisclosed, which needs the private triage role on the product. Send `disclosed` for one that is already public, which needs the ordinary one.

        It is filed under an identifier this deployment mints — the product's name, the year and a number. A CVE assigned later becomes another name for the same issue; nothing about the finding, the decisions or the approvals moves.

        `component` names what in the build carries it, as the build calls it. Leave it out for the build itself, which is where a flaw in how the pieces fit together goes. A name the build holds at more than one version is refused with the choices rather than resolved to one; send `version`, and `ecosystem` where two share a version.

        From here it behaves like any other finding: triaged, assigned, decided, on the same clock and in the same reports. No scan will close it, so it is closed by a person through the resolve endpoint or it stays open.

        Requires: public-triage or private-triage on the product. private-triage where the finding is undisclosed.
      operationId: record-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Record-findingRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnteredBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record a flaw in what this product ships
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: private-triage where the finding is undisclosed.
  /v1/products/{product}/findings.{format}:
    get:
      description: |-
        The findings list as a file: every row the same filters would show, not one page of them.

        Read with your own visibility, as it streams. It is the same query the screen reads, paged and written out as it goes — there is no point at which a whole unnarrowed list exists to be filtered afterwards, which is the failure an export is the easiest place in a codebase to make.

        The line this deployment triages at is stated in the file, because a spreadsheet opened six months later has nothing else to say that everything below it was never in there.

        Takes every filter the findings list takes.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-findings
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - explode: false
          in: query
          name: stream
          schema:
            type: string
        - explode: false
          in: query
          name: variant
          schema:
            type: string
        - description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
          explode: false
          in: query
          name: beneath
          schema:
            description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
            type: string
        - description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
          explode: false
          in: query
          name: differs
          schema:
            description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
            type: boolean
        - description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
          explode: false
          in: query
          name: across_variants
          schema:
            description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
            enum:
              - only
              - every
            type: string
        - description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only issues somebody is known to be exploiting
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only issues somebody is known to be exploiting
            type: boolean
        - description: Keep only issues where an upstream fixed version is known
          explode: false
          in: query
          name: fixable
          schema:
            description: Keep only issues where an upstream fixed version is known
            type: boolean
        - description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
          explode: false
          in: query
          name: below_floor
          schema:
            description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
            type: boolean
        - description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
          explode: true
          in: query
          name: component
          schema:
            description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
          explode: true
          in: query
          name: tag
          schema:
            description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
            maxLength: 200
            type: string
        - description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
          explode: true
          in: query
          name: ecosystem
          schema:
            description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
            items:
              maxLength: 64
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
          explode: true
          in: query
          name: "on"
          schema:
            description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
            items:
              enum:
                - branch
                - tag
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
          explode: true
          in: query
          name: support
          schema:
            description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
            items:
              enum:
                - in-support
                - past-eol
              type: string
            type:
              - array
              - "null"
        - description: Keep only what sits inside the container of this name
          explode: false
          in: query
          name: under
          schema:
            description: Keep only what sits inside the container of this name
            maxLength: 191
            type: string
        - description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
          explode: true
          in: query
          name: declared_as
          schema:
            description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
            items:
              enum:
                - required
                - optional
                - excluded
                - build
                - design
                - development
                - other
                - runtime
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what the build holds directly, which is what has no container above it
          explode: false
          in: query
          name: under_build
          schema:
            description: Keep only what the build holds directly, which is what has no container above it
            type: boolean
        - description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
          explode: true
          in: query
          name: state
          schema:
            description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
          explode: true
          in: query
          name: outcome
          schema:
            description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
          explode: true
          in: query
          name: assigned
          schema:
            description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
            items:
              enum:
                - me
                - somebody
                - nobody
              type: string
            type:
              - array
              - "null"
        - description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
          explode: false
          in: query
          name: epss_at_least
          schema:
            description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
            format: double
            maximum: 1
            minimum: 0
            type: number
        - description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
          explode: false
          in: query
          name: open_for
          schema:
            description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
          explode: false
          in: query
          name: due_within
          schema:
            description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what is already past its deadline
          explode: false
          in: query
          name: overdue
          schema:
            description: Keep only what is already past its deadline
            type: boolean
        - description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
          explode: true
          in: query
          name: fix_state
          schema:
            description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
            items:
              enum:
                - fixed
                - none
                - wont-fix
                - unknown
                - mixed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
          explode: true
          in: query
          name: weakness
          schema:
            description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
            items:
              maxLength: 32
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only groups where a claim is with its author, sent back for more
          explode: false
          in: query
          name: sent_back
          schema:
            description: Keep only groups where a claim is with its author, sent back for more
            type: boolean
        - description: Keep only what one of these VEX publishers has a standing statement about
          explode: true
          in: query
          name: vex_publisher
          schema:
            description: Keep only what one of these VEX publishers has a standing statement about
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only what was first seen here after this date, as 2026-03-31
          explode: false
          in: query
          name: opened_after
          schema:
            description: Keep only what was first seen here after this date, as 2026-03-31
            type: string
        - description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
          explode: false
          in: query
          name: opened_by_run
          schema:
            description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
          explode: false
          in: query
          name: closed_after
          schema:
            description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
            type: string
        - description: Keep only what somebody claimed something about after this date
          explode: false
          in: query
          name: proposed_after
          schema:
            description: Keep only what somebody claimed something about after this date
            type: string
        - description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
          explode: true
          in: query
          name: vex_status
          schema:
            description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
            items:
              enum:
                - not_affected
                - affected
                - fixed
                - under_investigation
              type: string
            type:
              - array
              - "null"
        - description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
          explode: false
          in: query
          name: reassessed
          schema:
            description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
            type: boolean
        - description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
          explode: false
          in: query
          name: origin
          schema:
            description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
            enum:
              - scanner
              - manual
            type: string
        - description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
          explode: false
          in: query
          name: planned
          schema:
            description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
            enum:
              - planned
              - unplanned
              - either
            type: string
        - description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
          explode: false
          in: query
          name: unconfirmed
          schema:
            description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
            type: boolean
        - description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
          explode: true
          in: query
          name: exclude
          schema:
            description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
          explode: false
          in: query
          name: sort
          schema:
            description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
            enum:
              - urgency
              - age
              - deadline
              - places
              - epss
              - severity
            type: string
        - description: Order the other way — oldest, nearest deadline, fewest places, lowest first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — oldest, nearest deadline, fewest places, lowest first
            type: boolean
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export the findings list
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/products/{product}/findings/components:
    get:
      description: |-
        One row per component and version, with how many distinct issues are open against it and how many places those sit at. The level above the findings list: it answers where the weight is rather than what is wrong, which is the question somebody asks before deciding what to read and what to put aside.

        It is also how a person finds the one package worth hiding. On a switch operating-system image the kernel carried 4,943 of 6,822 findings rows and the next largest contributor carried 58 — a fact no list of issues makes visible, because ordered by urgency it just looks like a long list.

        Takes the same filters as the findings list, so the two agree about what is being counted. Ordered by how many issues, not by urgency: making urgency the default would reproduce the findings list at worse resolution. Each row says what its weight is made of — the issues by severity, and the worst among them — because ranking by count alone answers this view's own question backwards: a package with forty-four issues outranks one with three criticals. `sort` takes every key the findings list takes, read of the package rather than of one finding — the worst of what is open against it (`severity`, `urgency`, `epss`), the oldest thing in it (`age`), the soonest deadline in it (`deadline`) and how far it reaches (`places`) — and `asc` orders the other way.

        `stream` and `variant` are optional and independent, as they are on the findings list: with either left out this counts across every build under the product that matches the rest. `beneath` is a walk over one build's edges and is refused unless both are named.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-finding-components
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: Limit to one branch or tag. Left out, every one under the product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Left out, every one under the product
            type: string
        - description: Limit to one variant. Left out, every one under the product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Left out, every one under the product, and independent of the branch
            type: string
        - description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
          explode: false
          in: query
          name: beneath
          schema:
            description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
            type: string
        - description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
          explode: false
          in: query
          name: differs
          schema:
            description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
            type: boolean
        - description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
          explode: false
          in: query
          name: across_variants
          schema:
            description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
            enum:
              - only
              - every
            type: string
        - description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only issues somebody is known to be exploiting
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only issues somebody is known to be exploiting
            type: boolean
        - description: Keep only issues where an upstream fixed version is known
          explode: false
          in: query
          name: fixable
          schema:
            description: Keep only issues where an upstream fixed version is known
            type: boolean
        - description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
          explode: false
          in: query
          name: below_floor
          schema:
            description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
            type: boolean
        - description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
          explode: true
          in: query
          name: component
          schema:
            description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
          explode: true
          in: query
          name: tag
          schema:
            description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
            maxLength: 200
            type: string
        - description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
          explode: true
          in: query
          name: ecosystem
          schema:
            description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
            items:
              maxLength: 64
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
          explode: true
          in: query
          name: "on"
          schema:
            description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
            items:
              enum:
                - branch
                - tag
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
          explode: true
          in: query
          name: support
          schema:
            description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
            items:
              enum:
                - in-support
                - past-eol
              type: string
            type:
              - array
              - "null"
        - description: Keep only what sits inside the container of this name
          explode: false
          in: query
          name: under
          schema:
            description: Keep only what sits inside the container of this name
            maxLength: 191
            type: string
        - description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
          explode: true
          in: query
          name: declared_as
          schema:
            description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
            items:
              enum:
                - required
                - optional
                - excluded
                - build
                - design
                - development
                - other
                - runtime
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what the build holds directly, which is what has no container above it
          explode: false
          in: query
          name: under_build
          schema:
            description: Keep only what the build holds directly, which is what has no container above it
            type: boolean
        - description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
          explode: true
          in: query
          name: state
          schema:
            description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
          explode: true
          in: query
          name: outcome
          schema:
            description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
          explode: true
          in: query
          name: assigned
          schema:
            description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
            items:
              enum:
                - me
                - somebody
                - nobody
              type: string
            type:
              - array
              - "null"
        - description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
          explode: false
          in: query
          name: epss_at_least
          schema:
            description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
            format: double
            maximum: 1
            minimum: 0
            type: number
        - description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
          explode: false
          in: query
          name: open_for
          schema:
            description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
          explode: false
          in: query
          name: due_within
          schema:
            description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what is already past its deadline
          explode: false
          in: query
          name: overdue
          schema:
            description: Keep only what is already past its deadline
            type: boolean
        - description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
          explode: true
          in: query
          name: fix_state
          schema:
            description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
            items:
              enum:
                - fixed
                - none
                - wont-fix
                - unknown
                - mixed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
          explode: true
          in: query
          name: weakness
          schema:
            description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
            items:
              maxLength: 32
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only groups where a claim is with its author, sent back for more
          explode: false
          in: query
          name: sent_back
          schema:
            description: Keep only groups where a claim is with its author, sent back for more
            type: boolean
        - description: Keep only what one of these VEX publishers has a standing statement about
          explode: true
          in: query
          name: vex_publisher
          schema:
            description: Keep only what one of these VEX publishers has a standing statement about
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only what was first seen here after this date, as 2026-03-31
          explode: false
          in: query
          name: opened_after
          schema:
            description: Keep only what was first seen here after this date, as 2026-03-31
            type: string
        - description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
          explode: false
          in: query
          name: opened_by_run
          schema:
            description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
          explode: false
          in: query
          name: closed_after
          schema:
            description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
            type: string
        - description: Keep only what somebody claimed something about after this date
          explode: false
          in: query
          name: proposed_after
          schema:
            description: Keep only what somebody claimed something about after this date
            type: string
        - description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
          explode: true
          in: query
          name: vex_status
          schema:
            description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
            items:
              enum:
                - not_affected
                - affected
                - fixed
                - under_investigation
              type: string
            type:
              - array
              - "null"
        - description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
          explode: false
          in: query
          name: reassessed
          schema:
            description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
            type: boolean
        - description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
          explode: false
          in: query
          name: origin
          schema:
            description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
            enum:
              - scanner
              - manual
            type: string
        - description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
          explode: false
          in: query
          name: planned
          schema:
            description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
            enum:
              - planned
              - unplanned
              - either
            type: string
        - description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
          explode: false
          in: query
          name: unconfirmed
          schema:
            description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
            type: boolean
        - description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
          explode: true
          in: query
          name: exclude
          schema:
            description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
          explode: false
          in: query
          name: sort
          schema:
            description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
            enum:
              - urgency
              - age
              - deadline
              - places
              - epss
              - severity
            type: string
        - description: Order the other way — oldest, nearest deadline, fewest places, lowest first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — oldest, nearest deadline, fewest places, lowest first
            type: boolean
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComponentFindingsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List findings by component
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/findings/components.{format}:
    get:
      description: |-
        One row per component and version, with how many distinct issues are open against it and how many places those sit at — every row, not one page.

        This is the shape a release meeting argues over: where the weight is rather than what is wrong. On a switch operating-system image the kernel carried 4,943 of 6,822 findings rows and the next largest contributor carried 58, which is a one-line answer here and invisible in a list of issues.

        Takes the same filters as the by-component list, and the line this deployment triages at is stated in the file.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-finding-components
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - explode: false
          in: query
          name: stream
          schema:
            type: string
        - explode: false
          in: query
          name: variant
          schema:
            type: string
        - description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
          explode: false
          in: query
          name: beneath
          schema:
            description: Keep only what sits at this component or anywhere under it — what the dependency tree's cumulative count counts. The name must be in the build; a name that is not, or that the build holds at more than one version, is refused
            type: string
        - description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
          explode: false
          in: query
          name: differs
          schema:
            description: Keep only groups open in some builds of this selection and not others. Meaningless where the selection is one build, and ignored there
            type: boolean
        - description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
          explode: false
          in: query
          name: across_variants
          schema:
            description: Keep only what is spread over the variants of its own branch one of these ways. 'only' keeps what no other variant of that branch holds open, and is refused unless a variant is named. 'every' keeps what every build of that branch holds open. The same issue at another version is a different row and counts as not held
            enum:
              - only
              - every
            type: string
        - description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse. 'low' excludes nothing, including issues carrying no rating
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only issues somebody is known to be exploiting
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only issues somebody is known to be exploiting
            type: boolean
        - description: Keep only issues where an upstream fixed version is known
          explode: false
          in: query
          name: fixable
          schema:
            description: Keep only issues where an upstream fixed version is known
            type: boolean
        - description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
          explode: false
          in: query
          name: below_floor
          schema:
            description: Include what this product does not consider worth triaging. Those are always recorded and counted; this asks to see them in the list
            type: boolean
        - description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
          explode: true
          in: query
          name: component
          schema:
            description: "Keep only what is open against components of these names, whatever version. Any of them, not all: a component is one name and asking for two means either"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
          explode: true
          in: query
          name: tag
          schema:
            description: "Keep only what somebody marked with one of these words, matched without regard to capitals. Any of them, not all. Free text: what is in use here is listed at /v1/products/{product}/tags"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component name or issue name contains this, ignoring capitals. Issue names include every alias, so searching the name a reporter used reaches the row filed under the name a scanner used. A way to find a package, or an advisory, in a list of thousands — where component is the exact package name
            maxLength: 200
            type: string
        - description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
          explode: true
          in: query
          name: ecosystem
          schema:
            description: Keep only components of these package kinds, as the package identifier spells them — deb, apk, rpm, golang, cargo, pypi, npm, gem, generic, oci, github, maven, or anything else a producer emits. The kind is read out of the identifier rather than chosen from a list, so any string is accepted and one nothing carries matches nothing. Not the language's name — Rust is cargo and Python is pypi
            items:
              maxLength: 64
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
          explode: true
          in: query
          name: "on"
          schema:
            description: "Keep only what sits in releases of these kinds. Defaults to branches: no work lands in a tag, whatever anybody decides about it. Ask for both to see everything"
            items:
              enum:
                - branch
                - tag
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
          explode: true
          in: query
          name: support
          schema:
            description: Keep only what sits in releases in this state of support, its own end-of-life date or the product's. Defaults to what is still in support. Ask for both to see everything
            items:
              enum:
                - in-support
                - past-eol
              type: string
            type:
              - array
              - "null"
        - description: Keep only what sits inside the container of this name
          explode: false
          in: query
          name: under
          schema:
            description: Keep only what sits inside the container of this name
            maxLength: 191
            type: string
        - description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
          explode: true
          in: query
          name: declared_as
          schema:
            description: "Keep only components a producer scoped one of these ways in this build, in the producer's own word: a CycloneDX component scope, or an SPDX lifecycle scope. Any of them, not all. Asked of the component's incoming edges, so one reached from two consumers scoped differently answers to both words. Nothing here ranks by it or decides anything from it — reading 'build' as 'does not ship' is wrong for every compiled language"
            items:
              enum:
                - required
                - optional
                - excluded
                - build
                - design
                - development
                - other
                - runtime
              type: string
            type:
              - array
              - "null"
            uniqueItems: true
        - description: Keep only what the build holds directly, which is what has no container above it
          explode: false
          in: query
          name: under_build
          schema:
            description: Keep only what the build holds directly, which is what has no container above it
            type: boolean
        - description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
          explode: true
          in: query
          name: state
          schema:
            description: "Keep only groups this far decided. A group covers every place an issue sits at in one component, so this is a statement about all of them: undecided means nothing stands, waits or has lapsed at any place, agreed means every place is answered"
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
          explode: true
          in: query
          name: outcome
          schema:
            description: "Keep only groups a standing judgment of this kind covers — how to ask what has been dismissed, which state cannot answer: agreed says a judgment stands, not which one. Every place must be answered the same way, and only the claim standing now counts"
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
          explode: true
          in: query
          name: assigned
          schema:
            description: "Keep only groups by who is dealing with them. 'me' means mine or a team I am on. A group whose places are held by different parties is none of these. Several answers hold together: mine and whatever nobody has picked up is one question"
            items:
              enum:
                - me
                - somebody
                - nobody
              type: string
            type:
              - array
              - "null"
        - description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
          explode: false
          in: query
          name: epss_at_least
          schema:
            description: Keep only issues the published estimate rates at least this likely to be exploited, 0 to 1
            format: double
            maximum: 1
            minimum: 0
            type: number
        - description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
          explode: false
          in: query
          name: open_for
          schema:
            description: Keep only what has been open here for at least this many days. The finding's own age, not the year in its identifier
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
          explode: false
          in: query
          name: due_within
          schema:
            description: Keep only what runs out within this many days. What is already past its deadline is asked for with overdue instead
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what is already past its deadline
          explode: false
          in: query
          name: overdue
          schema:
            description: Keep only what is already past its deadline
            type: boolean
        - description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
          explode: true
          in: query
          name: fix_state
          schema:
            description: Keep only what upstream has done one of these about. 'none' and 'wont-fix' are the rows that need a judgment rather than an upgrade, and the fixable flag cannot ask for either. 'unknown' is the scanner declining to say, which is not the same as upstream having released nothing. 'mixed' is a group whose places disagree — fixed in one build and not another — which has no single answer and is the population a half-landed upgrade shows up in
            items:
              enum:
                - fixed
                - none
                - wont-fix
                - unknown
                - mixed
              type: string
            type:
              - array
              - "null"
        - description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
          explode: true
          in: query
          name: weakness
          schema:
            description: "Keep only issues of these kinds of flaw, by CWE identifier — CWE-79. Any of them, not all: a class of flaw is usually several identifiers"
            items:
              maxLength: 32
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only groups where a claim is with its author, sent back for more
          explode: false
          in: query
          name: sent_back
          schema:
            description: Keep only groups where a claim is with its author, sent back for more
            type: boolean
        - description: Keep only what one of these VEX publishers has a standing statement about
          explode: true
          in: query
          name: vex_publisher
          schema:
            description: Keep only what one of these VEX publishers has a standing statement about
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: Keep only what was first seen here after this date, as 2026-03-31
          explode: false
          in: query
          name: opened_after
          schema:
            description: Keep only what was first seen here after this date, as 2026-03-31
            type: string
        - description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
          explode: false
          in: query
          name: opened_by_run
          schema:
            description: Keep only what one scan run opened, by its identifier. What a run reports having opened, as the list of it
            format: int64
            minimum: 1
            type: integer
        - description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
          explode: false
          in: query
          name: closed_after
          schema:
            description: Keep only what stopped being present after this date. Closed rows are outside this list's own population, so asking changes what it is about rather than narrowing it
            type: string
        - description: Keep only what somebody claimed something about after this date
          explode: false
          in: query
          name: proposed_after
          schema:
            description: Keep only what somebody claimed something about after this date
            type: string
        - description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
          explode: true
          in: query
          name: vex_status
          schema:
            description: Keep only what a VEX statement says one of these about, in the format's own vocabulary. With a publisher, both must hold
            items:
              enum:
                - not_affected
                - affected
                - fixed
                - under_investigation
              type: string
            type:
              - array
              - "null"
        - description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
          explode: false
          in: query
          name: reassessed
          schema:
            description: Keep only groups whose issue we rated differently from the world — what has been re-prioritized here
            type: boolean
        - description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
          explode: false
          in: query
          name: origin
          schema:
            description: Keep only what a person recorded here, or only what a scanner reported. Left out, both. The ones a person recorded are the only ones a person may close by hand
            enum:
              - scanner
              - manual
            type: string
        - description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
          explode: false
          in: query
          name: planned
          schema:
            description: Keep only what a promised upgrade covers, or only what none covers. Derived from the decisions rather than stored, so withdrawing a promise puts what it covered back with nothing to clean up. 'unplanned' is the working list once planned work is out of view, and is what the by-issue list asks unless told otherwise; 'either' is how a reader asks for it back, and is what leaving this out means
            enum:
              - planned
              - unplanned
              - either
            type: string
        - description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
          explode: false
          in: query
          name: unconfirmed
          schema:
            description: Keep only groups a scanner reached by comparing a published identifier against an upstream version range, never against an advisory for the package in its own ecosystem. A distribution backports fixes without moving the upstream version, so these are neither confirmed nor refuted — somebody has to look, and finding them one at a time is not a thing anybody does
            type: boolean
        - description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
          explode: true
          in: query
          name: exclude
          schema:
            description: "Drop components of these names. One package can drown the list: on a switch image the kernel carried 4,943 of 6,822 rows"
            items:
              maxLength: 191
              type: string
            maxItems: 200
            type:
              - array
              - "null"
        - description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
          explode: false
          in: query
          name: sort
          schema:
            description: "The order to page in. Urgency by default, which is what the list is designed around: what somebody with an hour should look at first. A finding with no deadline sorts last whichever direction is asked for"
            enum:
              - urgency
              - age
              - deadline
              - places
              - epss
              - severity
            type: string
        - description: Order the other way — oldest, nearest deadline, fewest places, lowest first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — oldest, nearest deadline, fewest places, lowest first
            type: boolean
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export findings by component
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/products/{product}/fix-bundles:
    get:
      description: |-
        One row per upstream upgrade, with the issues it closes.

        Keyed on the source package where one is recorded and on the component's own name otherwise, so packages built from one source are one row — curl, libcurl4t64 and libcurl3t64 are upgraded once.

        Only what has a fix: a bundle is a version to move to, so a finding upstream has released nothing for is not in one.

        Grouping is presentation: one act still writes one decision per component and per place.

        Takes the same selection as the findings list, and six of its filters: severity, exploited, component, search, ecosystem and state. Not the rest: a filter that answers about a place or a deadline has no row here to narrow.

        Ordered worst first, and `sort` takes any of: what the upgrade would close (`issues`, `places`), how far it reaches (`builds`), how bad the worst of it is (`urgency`, `severity`) and the soonest deadline it would meet (`deadline`). `asc` orders the other way. The default answers what should worry you; `sort=issues` answers what to do this afternoon.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-fix-bundles
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: Limit to one branch or tag
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag
            type: string
        - description: Limit to one variant
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant
            type: string
        - description: Keep only issues rated this badly or worse
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only upgrades closing something known to be exploited
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only upgrades closing something known to be exploited
            type: boolean
        - description: Keep only upgrades moving a component of this name
          explode: false
          in: query
          name: component
          schema:
            description: Keep only upgrades moving a component of this name
            type: string
        - description: Keep only rows whose component or issue name contains this
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component or issue name contains this
            maxLength: 200
            type: string
        - description: Keep only components of one package kind
          explode: false
          in: query
          name: ecosystem
          schema:
            description: Keep only components of one package kind
            type: string
        - description: Keep only groups this far decided
          explode: false
          in: query
          name: state
          schema:
            description: Keep only groups this far decided
            enum:
              - undecided
              - waiting
              - agreed
              - lapsed
            type: string
        - description: The order to page in. Worst first by default. A bundle with no deadline sorts last whichever direction is asked for
          explode: false
          in: query
          name: sort
          schema:
            description: The order to page in. Worst first by default. A bundle with no deadline sorts last whichever direction is asked for
            enum:
              - urgency
              - severity
              - issues
              - places
              - builds
              - deadline
            type: string
        - description: Order the other way — fewest, least urgent, nearest deadline first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — fewest, least urgent, nearest deadline first
            type: boolean
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-fix-bundlesResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List findings by upgrade
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/fix-bundles.{format}:
    get:
      description: |-
        The same list as a file: one row per upstream upgrade, with what it closes and the builds that hold it.

        Takes the same selection and the same filters as the screen, from the same struct. The builds an upgrade is held in are one cell, separated by spaces, because a spreadsheet has no second dimension.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-fix-bundles
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Limit to one branch or tag
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag
            type: string
        - description: Limit to one variant
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant
            type: string
        - description: Keep only issues rated this badly or worse
          explode: false
          in: query
          name: severity
          schema:
            description: Keep only issues rated this badly or worse
            enum:
              - low
              - medium
              - high
              - critical
            type: string
        - description: Keep only upgrades closing something known to be exploited
          explode: false
          in: query
          name: exploited
          schema:
            description: Keep only upgrades closing something known to be exploited
            type: boolean
        - description: Keep only upgrades moving a component of this name
          explode: false
          in: query
          name: component
          schema:
            description: Keep only upgrades moving a component of this name
            type: string
        - description: Keep only rows whose component or issue name contains this
          explode: false
          in: query
          name: q
          schema:
            description: Keep only rows whose component or issue name contains this
            maxLength: 200
            type: string
        - description: Keep only components of one package kind
          explode: false
          in: query
          name: ecosystem
          schema:
            description: Keep only components of one package kind
            type: string
        - description: Keep only groups this far decided
          explode: false
          in: query
          name: state
          schema:
            description: Keep only groups this far decided
            enum:
              - undecided
              - waiting
              - agreed
              - lapsed
            type: string
        - description: The order to page in. Worst first by default. A bundle with no deadline sorts last whichever direction is asked for
          explode: false
          in: query
          name: sort
          schema:
            description: The order to page in. Worst first by default. A bundle with no deadline sorts last whichever direction is asked for
            enum:
              - urgency
              - severity
              - issues
              - places
              - builds
              - deadline
            type: string
        - description: Order the other way — fewest, least urgent, nearest deadline first
          explode: false
          in: query
          name: asc
          schema:
            description: Order the other way — fewest, least urgent, nearest deadline first
            type: boolean
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export findings by upgrade
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/products/{product}/holders:
    get:
      description: |-
        People and teams that can hold work in this product, for a picker.

        People are those who can already read what they would be given, at the visibility asked for. Offering somebody who cannot open what they are handed is how work arrives with a person who cannot act on it — and on an undisclosed finding the offer itself would say a finding exists.

        Teams are listed whole. A team holds work and grants nothing, so one carries mixed clearance as a matter of course and what each member sees of what is routed to it is what they could see anyway.

        Narrow with `q`, which matches the identity and the displayed name without regard to capitals. This is not the people list: that is the deployment's directory and needs administration.

        Requires: public-read or public-triage or private-read or private-triage on the product. Asking about undisclosed findings needs private-read or private-triage.
      operationId: list-holders
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The visibility of the work being handed over
          explode: false
          in: query
          name: visibility
          schema:
            default: public
            description: The visibility of the work being handed over
            enum:
              - public
              - private
            type: string
        - description: Narrow to names containing this, ignoring capitals
          explode: false
          in: query
          name: q
          schema:
            description: Narrow to names containing this, ignoring capitals
            maxLength: 100
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyHolderBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List who work here can be given to
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-read
          - public-triage
          - private-read
          - private-triage
        note: Asking about undisclosed findings needs private-read or private-triage.
  /v1/products/{product}/issues/{vulnerability}/aliases/{alias}:
    put:
      description: |-
        Records that this issue is also known by another identifier — a CVE or a GHSA assigned after we minted our own.

        Nothing about the finding, the decisions or the approvals moves, because they are keyed on the issue rather than on what it is called. What changes is that the name travels with it: a report arriving under the new name resolves here rather than opening a second issue, the finding shows it, and the advisory carries it in the field a reader looks in — which is the one lookup a published advisory exists to serve.

        A name is identity, and identity is deployment-wide. From here on a scan of any product reporting that name resolves to this issue and inherits its decisions. So this asks for the right to triage the issue in every product it is currently open in, at the visibility each one carries, and is refused rather than partly done.

        Recording a name it already goes by succeeds and changes nothing.

        Requires: public-triage or private-triage on the product. Also asks for triage in every other product the issue is open in.
      operationId: add-alias
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - description: The other identifier, as it is written
          in: path
          name: alias
          required: true
          schema:
            description: The other identifier, as it is written
            maxLength: 191
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record another name for an issue
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: Also asks for triage in every other product the issue is open in.
  /v1/products/{product}/issues/{vulnerability}/assessment:
    post:
      description: |-
        Recorded against the issue, not against a place, and against one product. A published rating being wrong, or a report being disputed, is one statement about the vulnerability in this product — true in every build of it, including builds it has not reached yet, and it does not stop being true because somebody rebuilt something.

        It belongs to a product because a rating is a judgment about how a component is used, and two products do not use one the same way: one may ship the vulnerable configuration and another may not. Two products may record different ratings of the same issue, and neither reaches the other. A product nobody has rated the issue in reads the published rating.

        It changes the order, which is what makes it worth having rather than a note nobody acts on. Rating something worse than published takes effect at once: nobody needs protecting from being told something is worse than the world says. Rating it milder waits for a second person, because that is the direction that hides things — and it hides more than a position in a list. Severity sets the deadline, so calling a high a low pushes its deadline out by months, and where a product has said what is worth triaging at all, a downgrade below that line takes the finding off the working list and off any clock entirely.

        The published rating is never overwritten. The product's is what ranks there; the world's stays beside it, because a rating of ours shown where the world's goes reads as the world's.

        Requires: public-triage or private-triage on the product
      operationId: assess-issue
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssessmentBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssessmentBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record what a product thinks of an issue, as against what was published
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/issues/{vulnerability}/attachments:
    get:
      description: |-
        What text about this issue refers to. An upload nothing refers to yet is not listed, because it is not attached to anything — it becomes visible here when the justification or comment naming it is saved.

        A file an administrator removed is still listed, saying so, because the text that pointed at it still does.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-attachments
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-attachmentsResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List files attached to an issue
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Stores one file against an issue and returns the reference to put in text. Refer to it as `attachment:<token>` — as a link for anything downloaded, or as a markdown image for the raster types displayed in the page. An address of the store never appears in text.

        The content type is decided here from the bytes and is never the one that was uploaded. Everything outside a small allowlist of raster images is served as an attachment download whatever it is.

        Refused when the file is larger than this deployment accepts, when it has no room left, or when it would take you past your own share of the store; all three limits are settings. A deployment that has configured no store holds no attachments and says so.

        An upload nothing refers to is removed after a day, so a file attached and then abandoned does not accumulate. Send `evidence=true` where the file hangs off the issue itself — a test case that proves the flaw — rather than off text you are about to write: it is then listed at once and never swept, because the issue is what points at it.

        Requires: public-triage or private-triage on the product. A collaborator brought onto this issue may attach to it too.
      operationId: upload-attachment
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The issue, under any identifier it goes by
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, under any identifier it goes by
            type: string
      requestBody:
        content:
          multipart/form-data:
            encoding:
              evidence:
                contentType: text/plain
              file:
                contentType: application/octet-stream
            schema:
              properties:
                evidence:
                  type: boolean
                file:
                  contentEncoding: binary
                  contentMediaType: application/octet-stream
                  format: binary
                  type: string
              required:
                - file
                - evidence
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttachmentBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Attach a file to an issue
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: A collaborator brought onto this issue may attach to it too.
  /v1/products/{product}/issues/{vulnerability}/builds:
    put:
      description: |-
        Makes the builds this flaw is filed against exactly the ones named.

        Widening opens findings; narrowing closes them as `invalid` — never affected rather than no longer affected, so they count as no fix and appear in no release note. The record stays, with the reason.

        A reason is required whenever anything is taken out.

        Only a flaw recorded here. Which builds hold an issue a scanner reported is what the scans found, and this would overwrite it.

        `invalid` never means the finding exists but does not apply. That is a triage decision of `not-applicable` with the justification that fits.

        Requires: public-triage or private-triage on the product. private-triage where the finding is undisclosed.
      operationId: set-affected-builds
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The identifier it was filed under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The identifier it was filed under
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Set-affected-buildsRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AffectsBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set which builds a recorded flaw affects
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: private-triage where the finding is undisclosed.
  /v1/products/{product}/issues/{vulnerability}/collaborators:
    get:
      description: |-
        Everybody granted this one issue in this product, oldest first.

        Being on a case is not reading the product. A collaborator sees this issue wherever it sits here and nothing else, may argue about it and comment on it, and may not agree to anybody's claim.

        Requires: private-read or private-triage on the product. Only where you may read undisclosed work.
      operationId: list-collaborators
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyCollaboratorBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List who has been brought into a case
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-read
          - private-triage
        note: Only where you may read undisclosed work.
  /v1/products/{product}/issues/{vulnerability}/collaborators/{identity}:
    delete:
      description: |-
        Withdraws the grant. The record of it is kept, because who could see an embargoed case, and when, is exactly what is asked afterwards.

        Taking somebody off a case they are not on succeeds and changes nothing.

        Requires: private-read or private-triage on the product. Only where you may read undisclosed work.
      operationId: remove-collaborator
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: identity
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Take somebody off a case
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-read
          - private-triage
        note: Only where you may read undisclosed work.
    put:
      description: |-
        Grants one person this one issue in this product, without granting them private reading on the product.

        It is an access change and is recorded as one: it lands in the administration trail, and they are told at once — in the notification area inside the application, where an undisclosed finding may be named.

        They must already have been recorded here: this grants access to somebody who has some, and cannot bring anybody into the deployment.

        Adding somebody who is already on the case succeeds and changes nothing.

        Requires: private-read or private-triage on the product. Only where you may read undisclosed work.
      operationId: add-collaborator
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: identity
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Bring somebody into a case
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-read
          - private-triage
        note: Only where you may read undisclosed work.
  /v1/products/{product}/issues/{vulnerability}/disclosure:
    get:
      description: |-
        Every time this embargo was moved, oldest first, with which act it was, why, and by whom.

        Kept in full and never overwritten. One movement is a judgment and six is a policy nobody wrote down, and the difference is invisible if each replaces the last. A request still waiting for agreement is here too: what was asked for is part of how long this stayed hidden, whether or not it was granted.

        Requires: private-read or private-triage on the product. Only where you may read undisclosed work.
      operationId: list-disclosure-movements
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyMovementBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List how an embargo has been moved
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-read
          - private-triage
        note: Only where you may read undisclosed work.
  /v1/products/{product}/issues/{vulnerability}/disclosure/extension:
    post:
      description: |-
        Moves the end of an embargo later, across every undisclosed finding of this issue in this product.

        A reason is required, always, however short the extension. One with no reason is a record saying somebody moved it and nothing else.

        Past a threshold it needs a second person, and the threshold is measured against how far this embargo's end has already been carried rather than against this request alone — measured per request, the exception swallows the rule three weeks at a time. It is the same act a deferral is, and the same shape.

        An extension that needs agreement moves nothing until it has it. The request is on record either way; `in_force` says whether the date follows it.

        A date sent earlier is refused here. Ending an embargo sooner is a different act, recorded as one: `POST .../disclosure/shortening`.

        Requires: private-triage on the product. A second person agrees past the threshold.
      operationId: extend-disclosure
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Extend-disclosureRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MovementBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Extend a disclosure date
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-triage
        note: A second person agrees past the threshold.
  /v1/products/{product}/issues/{vulnerability}/disclosure/shortening:
    post:
      description: |-
        Moves the end of an embargo sooner, across every undisclosed finding of this issue in this product. What a coordinator or a peer vendor publishing on a date of their own asks for, and what a leak leaves.

        Its own act rather than an extension sent a smaller date. Shortening an embargo because it leaked and extending one because a fix slipped are different events, and which of them happened is read off the record rather than inferred from the direction a date moved.

        A reason is required, and the same threshold applies: how far this embargo's end has already been carried, counting a date brought forward the same distance as one pushed back. Past it a second person agrees, and until they do the date does not move.

        A date sent later is refused here. Extend with `POST .../disclosure/extension`.

        Requires: private-triage on the product. A second person agrees past the threshold.
      operationId: shorten-disclosure
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Shorten-disclosureRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MovementBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Bring a disclosure date forward
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - private-triage
        note: A second person agrees past the threshold.
  /v1/products/{product}/issues/{vulnerability}/notes:
    get:
      description: |-
        Returns the notes written about this issue in this product, oldest first, with who wrote each and when. A note that has been edited also carries when it was last changed.

        A note records no judgment and changes nothing: not what ranks, not a deadline, not a triage line. It is context for whoever decides.

        It is about the issue in this product, not about one component. A row in the findings list is one issue at one source package, and one issue is often several rows — so a note kept against a row would be written on one of them and hidden from the rest. What is about a judgment at a place is a comment on that claim instead.

        Requires: any signed-in person, and not a pipeline key. Answers where you may read a finding of this issue in this product, at its visibility — an issue with one undisclosed place here is undisclosed for this. Anywhere else it answers as an issue that is not there.
      operationId: list-issue-notes
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyNoteBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List notes on an issue in a product
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers where you may read a finding of this issue in this product, at its visibility — an issue with one undisclosed place here is undisclosed for this. Anywhere else it answers as an issue that is not there.
    post:
      description: |-
        Adds a markdown note about this issue in this product. It records no judgment: nothing about what ranks, what a deadline is, or what the product triages changes because somebody wrote one.

        This is the way to leave something for whoever decides without deciding. A comment hangs off a claim; a note does not, so nothing has to be judged before anything can be said.

        It reaches every build of the product and does not lapse when a version moves. Something true of one copy and not another — "we do not call that function in the vendored build" — is about a place, and belongs on the claim there.

        A name written after an `@` is told, where that person may read what the note is about; the response lists the names that reached nobody.

        The text is markdown and is validated before it is stored; a 422 names the line and the offending text.

        Requires: public-triage or private-triage on the product
      operationId: note-on-issue
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Note-on-issueRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteWritten"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Add a note to an issue in a product
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/issues/{vulnerability}/report:
    get:
      description: |-
        Who reported it, how to reach them, when it arrived, when somebody answered them, and how they wish to be credited.

        The received date is what the embargo runs from: a report arriving on 1 June and typed in on 15 June otherwise puts our clock two weeks behind the one the reporter has a publication scheduled against, and they are the party who will publish regardless.

        Answers 404 where nobody recorded a reporter, which is every flaw we found ourselves.

        Requires: public-triage or private-triage on the product
      operationId: get-report
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show who reported a flaw
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/issues/{vulnerability}/report/acknowledgement:
    post:
      description: |-
        Records that somebody replied to whoever reported this, and when.

        It records that it happened rather than doing it. What reaches a researcher is a mail somebody sends from an address they already have; recording it is what turns "somebody probably replied" into a date the timeline can be evidenced from, and what clears the condition an unanswered report opens.

        Acknowledging twice keeps the first date: when somebody was answered is a fact about the past, and the second person to press it did not change it.

        Requires: public-triage or private-triage on the product
      operationId: acknowledge-report
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record that the reporter was answered
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/mentionable:
    get:
      description: |-
        Returns the people who can already read findings of this visibility in this product, so an editor can offer them.

        Only people who can already see the thing. An autocomplete listing everybody teaches somebody to name a colleague who then cannot open what they were called to — and on an undisclosed finding, the mention itself says a finding exists, which is the disclosure the visibility rule prevents.

        `visibility` says which kind of finding the text is about. Asking about undisclosed findings requires being able to read them.

        Requires: public-read or public-triage or private-read or private-triage on the product. Asking about undisclosed findings needs private-read or private-triage.
      operationId: list-mentionable
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The kind of finding the text is about
          explode: false
          in: query
          name: visibility
          schema:
            default: public
            description: The kind of finding the text is about
            enum:
              - public
              - private
            type: string
        - description: Narrow to names containing this, ignoring capitals. Matched on the identity and on the displayed name
          explode: false
          in: query
          name: q
          schema:
            description: Narrow to names containing this, ignoring capitals. Matched on the identity and on the displayed name
            maxLength: 100
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 25
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyMentionableBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List who may be mentioned here
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-read
          - public-triage
          - private-read
          - private-triage
        note: Asking about undisclosed findings needs private-read or private-triage.
  /v1/products/{product}/overview:
    get:
      description: |-
        One product, with each of its builds: what is open, how much is overdue, how much is exploited, how much nobody has claimed anything about, how much is answered at every place, and when a scan last arrived.

        Counted as issues at components, the unit every other count here uses. A component reached twenty ways carries the same issue twenty times, so counting rows would make a build look twenty times worse than the list somebody opens next.

        "Undecided" and "agreed" are the findings list's own words, by the same definition and read from the same expression: undecided means no place has a decision, agreed means every place is answered by one that stands. Two screens with two definitions of "decided" is how they come to disagree in front of somebody.

        A build whose release is out of support says so rather than reading as one that stopped being scanned: those are different facts and only one of them is a fault.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-product-overview
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OverviewOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show how one product is doing
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/releases:
    get:
      description: |-
        One number per build, which is what a release-over-release chart is drawn from. The comparison endpoint says what changed between two builds; this says whether the estate is getting better or worse across all of them.

        Counted before any triage line is applied, so it agrees with the findings list rather than with whatever a product has decided is worth working on — a line is about what to spend an afternoon on, not about what exists.

        Severities are folded the same four ways everything else here ranks by, through the one expression the working list and the deadline also read, so a chart cannot disagree with a list about what counts as high.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-releases
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyReleaseBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Report what is open in each build of a product
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/routing-rules:
    get:
      description: |-
        The standing rules for this product, in the order they are tried.

        First match wins, and which rule placed a finding is recorded on the finding: an unwritten precedence is forgettable, and the question it answers — where did this come from — is asked months later by somebody who was not there.

        Requires: public-triage or private-triage on the product
      operationId: list-routing-rules
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyRuleBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the rules that route work to teams
      tags:
        - Administration
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
    post:
      description: |-
        Records a standing rule and queues it against what is already open.

        It matches on component identity as well as on a place in the tree. The source package is the key that matters: one rule naming it catches every binary package built from it, wherever they sit — a kernel is one source package appearing at many places under many consumers, and a subtree rule would need a line per place and would still miss tomorrow's.

        It places only work nobody holds. A human assignment always wins, and adding a rule never takes something out of somebody's hands.

        Turning one on is a bulk write, so it is queued rather than done here: one rule naming a source package sweeps thousands of existing findings, and saving a form must not hold a transaction open across the estate. The reply says the rule was recorded, not that the sweep has finished.

        Requires: assigner on the product. A rule hands work to somebody, continuously, on behalf of whoever wrote it.
      operationId: add-routing-rule
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Add-routing-ruleRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RuleBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Add a rule that routes work to a team
      tags:
        - Administration
      x-openpsirt-requires:
        scope: product
        any_of:
          - assigner
        note: A rule hands work to somebody, continuously, on behalf of whoever wrote it.
  /v1/products/{product}/routing-rules/preview:
    get:
      description: |-
        Answers what a rule with these keys matches, without recording anything: the components it names, how many pieces of work sit at them, and how many of those nobody holds.

        A rule whose reach nobody can see before saving is a rule that sweeps the estate on a guess, and one naming something nothing is called places nothing, silently — which is the worst way for a rule to be wrong, because it still looks like a rule. `*` matches any run of characters in either key.

        It does not account for the rules already there. First match wins, so what this catches is what it would place only where no earlier rule claimed it first.

        Requires: public-triage or private-triage on the product
      operationId: preview-routing-rule
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: A source package name, with * for any run of characters
          explode: false
          in: query
          name: upstream
          schema:
            description: A source package name, with * for any run of characters
            maxLength: 191
            type: string
        - description: A component name, with * for any run of characters
          explode: false
          in: query
          name: beneath
          schema:
            description: A component name, with * for any run of characters
            maxLength: 191
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatchesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Preview what a routing rule would catch
      tags:
        - Administration
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/routing-rules/{id}:
    delete:
      description: |-
        Takes a rule out of use. What it already placed stays placed: changing a rule never takes something out of somebody's hands, and that holds for a team's queue as much as for a person.

        Requires: assigner on the product
      operationId: retire-routing-rule
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retire a routing rule
      tags:
        - Administration
      x-openpsirt-requires:
        scope: product
        any_of:
          - assigner
  /v1/products/{product}/saved-filters:
    get:
      description: |-
        The narrowings you have kept, by name.

        Personal, and nothing is shared. No ownership, no permissions and no arguing about whose filter is authoritative — which is also what lets somebody keep one that is half-formed. Yours are the only ones this answers with, whoever asks.

        A saved filter naming something the list no longer offers simply stops narrowing by it, which is a way back to a slightly wider list rather than a refusal to open one.

        Kept per product. A filter narrows one product's findings list and its query names branches and variants that usually exist in no other, so one offered everywhere would be offered where it matches nothing.

        Requires: any signed-in person, and not a pipeline key. Answers your own and nobody else's.
      operationId: list-saved-filters
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodySavedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List your saved filters
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers your own and nobody else's.
  /v1/products/{product}/saved-filters/{name}:
    delete:
      description: |-
        Drops one of your own. A name you have not kept is not there, which is the same answer as somebody else's — the filters are personal, and the query says so rather than only the screen.

        Requires: any signed-in person, and not a pipeline key. Yours alone.
      operationId: forget-filter
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: name
          required: true
          schema:
            maxLength: 120
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Forget a saved filter
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Yours alone.
    put:
      description: |-
        Keeps the findings list's current narrowing so it can be opened again. Saving under a name you already use replaces it: the act is deciding what that name means, and refusing would make somebody delete before they could correct.

        Requires: any signed-in person, and not a pipeline key. Yours alone.
      operationId: save-filter
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: name
          required: true
          schema:
            maxLength: 120
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Save-filterRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Keep a filter under a name
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Yours alone.
  /v1/products/{product}/streams:
    get:
      description: |-
        Every stream declared under this product, with how many variants each has and how much is open in it.

        A stream is a branch or a tag: a branch is scanned nightly and superseded, a tag is immutable and kept. Both are declared before a scan may name one, so a typo in a pipeline is refused rather than quietly creating a line with findings and reports of its own.

        A stream past its end-of-life date is listed and says so. It stops being a place a fix may be declared for, and what is open against it is still counted.

        A retired stream is left out unless asked for. Ask for it to resolve a name already in hand rather than to offer a choice; each one says that it is retired.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-streams
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: Include streams that have been retired. They say so, and no scan may be filed against one
          explode: false
          in: query
          name: retired
          schema:
            description: Include streams that have been retired. They say so, and no scan may be filed against one
            type: boolean
        - description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
          explode: false
          in: query
          name: counts
          schema:
            description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyStreamBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List a product's branches and tags
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Records a line of a product. A branch moves and is rebuilt; a tag never changes and is what somebody received.

        Requires: administrator
      operationId: declare-stream
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeclaredStreamBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a branch or tag
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/streams/{stream}:
    delete:
      description: |-
        Takes a release out of use. It is offered nowhere and no scan may be filed against it, while everything already filed against it stays. Declaring it again brings it back.

        An end-of-support date is a separate setting with a separate effect: it takes the deadline off what is open and leaves the release listed and scannable.

        Requires: administrator
      operationId: retire-stream
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retire a branch or tag
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
    patch:
      description: |-
        Corrects what a release is called.

        Refused once a VEX document has gone out for any build of this release, or a published advisory named it. Declare the intended name as a release of its own: retiring this one and declaring it again returns this same release under this same name.

        Whether it is a branch or a tag does not move here, and neither does the branch a tag was cut from.

        Requires: administrator
      operationId: amend-stream
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Amend-streamRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Amend a branch or tag
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/streams/{stream}/end-of-life:
    put:
      description: |-
        Sets the date support ends for one release, overriding what its product says. Past it, nothing on the release carries a remediation deadline and a build that stops being scanned is expected rather than a fault.

        An empty date clears the override, so the release follows its product again. Clearing is not the same as stating the product's current date: a release that stated it would stop following when the product changed.

        Requires: administrator
      operationId: set-stream-end-of-life
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndOfLifeBody"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set when a branch or tag goes out of support
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/streams/{stream}/release:
    put:
      description: |-
        Records the day a tag actually shipped, and the branch it was cut from.

        Both are settable after the fact, because that is when they are usually known. A tag is declared here so scans can be filed against it, which happens whenever somebody gets to it — before the release, months after, or while backfilling a year. Fixed at declaration, a release recorded late ordered after ones that came out before it, and a year entered in an afternoon plotted as a single day.

        The date orders and labels the release-over-release chart. Left unset, the day the release was declared here stands in.

        The parent fills in, and never changes. It is what a branch is compared against for release notes, and a pipeline that does not know declares the tag without it — so saying it late is the same act arriving late, because nothing had been said for it to contradict. Naming a different branch is refused, and so is clearing one: a tag is one frozen point and it came from wherever it came from. It is a branch and nothing is cut from itself; both are refused rather than stored, because a cycle here is a comparison that never returns.

        An empty date clears the date. An empty parent leaves whatever stands alone.

        Requires: administrator
      operationId: set-release-details
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Set-release-detailsRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set when a release went out and what it was cut from
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/streams/{stream}/variants:
    get:
      description: |-
        Lists the variants one release was actually built as — a subset of what the product declares.

        A release predating a variant has never been filed against it and does not list it, which is what keeps something introduced later from appearing to have shipped years ago.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-release-variants
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
          explode: false
          in: query
          name: counts
          schema:
            description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyVariantBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the variants a release was built as
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/carried:
    get:
      description: |-
        Returns what an existing line's decisions would mean for this one, without changing anything. Ask before creating a line: the answer is what somebody is agreeing to.

        Four groups, because they need four different things:

        `applying` reach this line by matching, and there is nothing to choose.

        `moved` held a claim at a version this line does not have. Each would come across as a proposal carrying the old reasoning, never as a decision.

        `postponed` were deferrals. Each says how long it has already been put off across every line it has come through, which is the total that carrying it again agrees to.

        `absent` cover nothing here and are left behind.

        Requires: public-triage or private-triage on the product
      operationId: preview-carried-decisions
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The stream to inherit from
          explode: false
          in: query
          name: from
          required: true
          schema:
            description: The stream to inherit from
            type: string
        - description: That stream's variant
          explode: false
          in: query
          name: from_variant
          required: true
          schema:
            description: That stream's variant
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CarriedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show what triage a new line would inherit
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
    post:
      description: |-
        Takes the judgments named onto this build as claims waiting for agreement, each carrying the words from the line it came from.

        Reasoning travels and conclusions do not. Every one arrives needing approval, however confident whoever carried it was: a version moved, which is exactly what made the old judgment stop applying, so somebody has to look at the new code. What is inherited is the thinking rather than the answer.

        Only what the preview offered. A judgment that already applies here has nothing to agree to, and one covering nothing here has nothing to apply to; naming either is refused rather than skipped, because a caller that got the set wrong should hear so.

        A deferral is carried with the date it had, not with a fresh one. Bounded by the same setting that bounds every other action writing many rows.

        Requires: public-triage or private-triage on the product
      operationId: carry-decisions
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The line to carry from — a branch or a tag
          explode: false
          in: query
          name: from
          required: true
          schema:
            description: The line to carry from — a branch or a tag
            type: string
        - description: That line's variant
          explode: false
          in: query
          name: from_variant
          required: true
          schema:
            description: That line's variant
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Carry-decisionsRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Carry-decisionsResponse"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Carry chosen triage onto a new line
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/carried-patches:
    get:
      description: |-
        Everything this build has argued about in its own inventories: carried patches saying what they fix, and statements it sent alongside.

        A history rather than a list of what is true tonight. Each row says when the build first said it and when it stopped, because a claim that stopped is the interesting one — somebody dropped a patch, and the finding it answered is back. A list of what is current would not have that row at all.

        A carried patch is the only way a backport can be seen here. No version comparison finds one: the fix is in the package and the version has not moved, so unless the build declares it, the finding sits open with nothing true to say about it.

        `suppresses` says whether a claim takes a finding off the list. Saying it is affected, or that it has not decided, is information rather than an answer.

        Narrow to one package with `component`, matched on what the claim says it is about rather than on a component this build carries — a claim naming something that is no longer here is exactly the row somebody asking why a patch stopped working is looking for.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-carried-patches
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: Narrow to what one package was claimed about
          explode: false
          in: query
          name: component
          schema:
            description: Narrow to what one package was claimed about
            type: string
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-carried-patchesResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List what a build says it deals with itself
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/components:
    get:
      description: |-
        Returns the build's own component and what it depends on, most findings first. The root is named separately from the list because it is what the list hangs from rather than a member of it.

        The starting point for walking the graph. A full render is not offered and would not be useful: a real image holds thousands of components and tens of thousands of edges, which neither draws nor reads. Ask for one step at a time.

        Every entry carries how many findings are open against it and how many components it pulls in, so descending follows something rather than being exploration.

        With `q` it searches instead: components anywhere in the build whose name contains that text, most findings first and no root. Nobody finds anything in a graph this size by opening nodes — a real image holds eight thousand components under a root with five thousand children — so searching is the way in, and browsing is for answering "what else is under this" once you are already somewhere.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-top-level-components
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: Find components anywhere in this build whose name contains this, instead of listing what the build pulls in directly
          explode: false
          in: query
          name: q
          schema:
            description: Find components anywhere in this build whose name contains this, instead of listing what the build pulls in directly
            maxLength: 200
            type: string
        - description: The number of matches returned. Only read when searching
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number of matches returned. Only read when searching
            format: int64
            maximum: 200
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RootsBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List what a build pulls in directly
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/components/mine:
    get:
      description: |-
        The build's dependency graph, seen upward from your own work: from each component you hold a finding on, up to the build's root.

        This is what the tree is for somebody who holds no reading on the product. Descended from the root, that tree is the inventory of what the product contains — the breadth they were not granted — so it cannot be offered with rows hidden: a container's count would still say how much sits under it. The chain upward is the part that makes a finding judgeable, because it says what pulled the thing in, and every node on it sits above something already granted.

        The counts are yours. A node says how much of your own work hangs beneath it along these chains, never how much the build holds there.

        Rows come back in the order they are drawn, parents before children, the fullest branch first. A component the inventory placed nowhere has no chain and sits at the end.

        Requires: any signed-in person, and not a pipeline key. Answers what you hold, whether or not you read the product.
      operationId: list-my-components
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OursOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show the chains your own findings sit on
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers what you hold, whether or not you read the product.
  /v1/products/{product}/streams/{stream}/variants/{variant}/components/{component}/around:
    get:
      description: |-
        Returns what pulls this component in, and what it pulls in.

        `above` is the direction people actually use. Somebody arrives from a finding and asks why the component is here, which is walking up — and up is short. Walking down is where the size lives.

        A component reached several ways appears once with several parents. It is a graph rather than a tree, so anything drawing it has to expect the same component under many places.

        A component name is not unique within a build. Where one ships at several versions, `version` says which — without it, a name that matches more than one is refused with 409, naming the choices, rather than guessed at.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-component-neighbors
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The component's name, as the findings list gives it
          in: path
          name: component
          required: true
          schema:
            description: The component's name, as the findings list gives it
            type: string
        - description: The version, where the build ships that name at more than one
          explode: false
          in: query
          name: version
          schema:
            description: The version, where the build ships that name at more than one
            type: string
        - description: The ecosystem, for the few names one build holds at one version as two components
          explode: false
          in: query
          name: ecosystem
          schema:
            description: The ecosystem, for the few names one build holds at one version as two components
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AroundBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show what is directly above and below a component
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/components/{component}/decisions:
    post:
      description: |-
        Records the same claim against every issue you name: one outcome, one justification, one reasoning, and a separate decision for every place each issue sits at, each keyed and expiring on its own.

        You name the issues; the places are resolved here. `selected_by` says how you narrowed the list and is recorded with every claim, so "how were these chosen" has an answer later — but it is never the claim. The reasoning has to hold for every issue in the list, since "these matched a word" is not a defense anybody would accept.

        `contains` is the same question an approver can re-run. Send the text you narrowed the candidate list by; the claim records how many issues that narrowing reaches, read here, against how many you named. Equal, the claim is exactly what that narrowing returns; far apart, the sentence does not describe the set.

        Always needs a second person to agree, whatever the outcome.

        Bounded. At most 2000 names per request, and a limit on how many findings one action may write, set under `triage.together-cap`. The limit is checked against the findings this resolves to, which is more than the number of names.

        Requires: public-triage or private-triage on the product
      operationId: decide-together
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Decide-togetherRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Decide-togetherResponse"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record one judgment about several issues at once
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/components/{component}/issues:
    get:
      description: |-
        Returns the distinct issues open against this component in this build, most urgent first, with how many places each sits at, the version that fixes it where the report names one, and what one judgment about it would be made on: what the issue says about itself, whether anybody is known to be exploiting it, the published estimate, and the earliest deadline among its places here.

        `contains` matches the text of a report. It narrows a list; it is not part of any claim made afterwards.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-issues-at-component
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
        - description: Match the text of the report
          explode: false
          in: query
          name: contains
          schema:
            description: Match the text of the report
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 500
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-issues-at-componentResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the issues open against one component
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/components/{component}:
    get:
      description: |-
        Returns the full record for one issue in one component of a build: the description, the advisory, every reference the data carries with patches listed first, the score and what it assumes, whether it is known to be exploited and how likely exploitation is, the weakness classification, what upstream has done about it, and every place the component sits at here.

        This is what a triage decision is made from, so it is gathered into one request. Each entry in `places` carries the `place` identity to name when recording a decision about it.

        A component name is not unique within a build. Where one ships at several versions, `version` says which — without it, a name that matches more than one is refused rather than guessed at.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
        - description: The component's name, as the findings list gives it
          in: path
          name: component
          required: true
          schema:
            description: The component's name, as the findings list gives it
            type: string
        - description: The version, where the build ships that name at more than one
          explode: false
          in: query
          name: version
          schema:
            description: The version, where the build ships that name at more than one
            type: string
        - description: The ecosystem, for the few names one build holds at one version as two components — a source repository and the package built from it
          explode: false
          in: query
          name: ecosystem
          schema:
            description: The ecosystem, for the few names one build holds at one version as two components — a source repository and the package built from it
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get everything known about one finding
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/components/{component}/assignment:
    put:
      description: |-
        Records who is dealing with this issue in this component.

        It covers the product, not the build named in the path. The path says which finding is being looked at; what is assigned is every build of the product holding the same component, and every place that component sits at.

        Send `person` as an empty string to hand it back to nobody, which is this same operation rather than one of its own.

        Send `team` instead to route it to a team. That is a queue rather than a holding: it stays unheld until somebody takes it, and anybody on the team may take it with the triage right alone. Routing to a team asks that at least one member may read what is being routed there — not that every member may.

        Findings arriving later under the same component start unassigned.

        Requires: public-triage or private-triage on the product. Giving work to somebody else also needs assigner. Taking unowned work, or handing back your own, does not.
      operationId: assign-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assign-findingRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Assign a finding to somebody
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: Giving work to somebody else also needs assigner. Taking unowned work, or handing back your own, does not.
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/components/{component}/decision:
    post:
      description: |-
        Records the same claim against every place this issue occupies in this component. Naming `places` narrows it; leaving it out covers all of them.

        A place left out stays open. Nothing is recorded against it and nothing is asked about it.

        One record is written per place, each keyed and expiring on its own, so this reads later as the several decisions it is rather than as one.

        The ordinary approval rules apply however many places this reaches: covering many places does not on its own require a second person.

        Pass `also` to apply the same judgment to other builds of this product, naming each as the reach gives it. All of it is written in one transaction: the response is what every build recorded, or a refusal and nothing written anywhere. In those builds only the places nothing already stands at are written, because the ones at matching versions are reached by lookup already.

        Pass `extends` to carry an approved claim to this issue: the source must be approved, sit at the same component under the same consumer, and the outcome and justification must match it. The new claim is recorded as an extension of it and still waits for a second person. `similar` on `GET .../findings/{vulnerability}/components/{component}` lists the claims that qualify.

        `patch-needed` is the backport case: a fix is being carried into this build and the version does not move, so it requires `committed_to`, the date the work lands. `upgrade-needed` is not recorded here — an upgrade answers a component and everything open on it, so it is recorded from the component.

        Requires: public-triage or private-triage on the product
      operationId: decide-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
        - description: The component, as the findings list gives it
          in: path
          name: component
          required: true
          schema:
            description: The component, as the findings list gives it
            type: string
        - description: The version, where the build ships more than one under that name
          explode: false
          in: query
          name: version
          schema:
            description: The version, where the build ships more than one under that name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FindingDecisionBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecidedBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record one judgment about a finding, covering its places
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/components/{component}/reach:
    get:
      description: |-
        The same three parts the per-place answer gives, for every place the finding sits at, merged.

        A judgment is made about an issue in a component, which is a group of places rather than one — a kernel flaw sits at sixty. Asking per place is a request each, so a screen doing that samples, and a sample decides which other builds it can offer to include: one reachable only from a place the sample missed is never offered and the judgment does not travel there.

        A build reached from two places of the finding is one thing to agree to, and carries the places of both.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-finding-reach
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
        - description: The version, where the build holds that name at more than one
          explode: false
          in: query
          name: version
          schema:
            description: The version, where the build holds that name at more than one
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReachBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show how far a decision about this finding would reach
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/components/{component}/tags/{tag}:
    delete:
      description: |-
        Removes a tag. Taking off one that is not there succeeds and changes nothing.

        Requires: public-triage or private-triage on the product
      operationId: untag-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
        - description: The word, matched without regard to capitals
          in: path
          name: tag
          required: true
          schema:
            description: The word, matched without regard to capitals
            maxLength: 191
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Take a tag off a finding
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
    put:
      description: |-
        Puts a free-text tag on one issue in one component of this product.

        No fixed vocabulary, because none has been earned yet. A tag that becomes universal is a signal that it should be promoted to a real concept — "waiting on vendor" is a state the tool would want to reason about rather than a string somebody typed.

        One issue in one component of one product, not one place and not one build: a kernel flaw at sixty places is one thing somebody is tagging, and a tag is about the work rather than about a release.

        Matched without regard to capitals and shown back as it was typed. Tagging what already carries the tag succeeds and keeps the first spelling.

        Tagging is triage, so it asks for the triage right: a tag changes what a filtered list answers, and somebody who may only read should not move work into or out of a saved filter.

        Requires: public-triage or private-triage on the product
      operationId: tag-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: component
          required: true
          schema:
            type: string
        - description: The word, matched without regard to capitals
          in: path
          name: tag
          required: true
          schema:
            description: The word, matched without regard to capitals
            maxLength: 191
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Tag a finding with a word
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/places/{place}/decision:
    get:
      description: |-
        Returns the decision currently suppressing this finding, if any, together with everything decided here before — withdrawn claims, and claims that stopped applying when an upstream version changed.

        `standing` is absent when nothing has been decided, or when a claim is still waiting for approval: a claim nobody has agreed to suppresses nothing.

        Read `previously` before deciding again. A claim that lapsed on a version upgrade is usually still the right answer, and re-affirming it is a different request from making a new one.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-finding-decision
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
        - description: The place, as the findings list gives it
          in: path
          name: place
          required: true
          schema:
            description: The place, as the findings list gives it
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandingBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get what has been decided about a finding
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Records how a finding was triaged: `affected`, `not-applicable`, `mismatched`, `deferred`, `wont-fix`, `already-fixed` or `patch-needed`.

        `not-applicable` and `mismatched` require a `justification` from the standard VEX vocabulary. `deferred` requires `deferred_until` as a date. `already-fixed` requires `fixed_version`, the version whoever packages the component states the fix arrived in — it is recorded for a reader and never compared against what ships.

        `patch-needed` is the backport case: a fix is being carried into this build and the version does not move. It requires `committed_to`, the date the work lands, and it closes the only way a backport can — the next inventory declares the patch it carries and says what that patch resolves, so the finding goes while the version stays where it was.

        `upgrade-needed` is not recorded here. An upgrade answers a component rather than one finding, so it is recorded from the component and covers everything open on it.

        The decision applies to every build running the same component and consumer upstream versions, including future releases — it is matched by code, not copied between releases. It stops applying automatically when either upstream version changes.

        `mismatched` is the exception, and the only one: it says the scanner matched this against something it is not, which is a claim about identity rather than about risk. It covers the place at whatever versions the place holds, and no version change expires it — a bump does not make a wrong match right. Its `justification` is limited to the two reasons that say something is not there, `component_not_present` and `vulnerable_code_not_present`; the three that describe how code is reached or what stops it are refused, because a version bump changes those and this outcome would carry them past it.

        The response says whether a second person must approve it. Most outcomes require approval; a deferral shorter than the configured threshold does not.

        It also says how many findings this one judgment covers, and how many distinct versions of the component sit at this place — more than one means a single decision cannot honestly cover all of them.

        Requires: public-triage or private-triage on the product
      operationId: decide
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The issue, by any name it is known under
          in: path
          name: vulnerability
          required: true
          schema:
            description: The issue, by any name it is known under
            type: string
        - description: The place, as the findings list gives it
          in: path
          name: place
          required: true
          schema:
            description: The place, as the findings list gives it
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecisionBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record a triage decision for a finding
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/places/{place}/decision/reaffirmation:
    post:
      description: |-
        Re-makes a decision that stopped applying because an upstream version moved, at the versions this finding has now. `previous` is the decision being re-made, from `previously` in `GET .../decision`.

        Only the person who made the original may do this, and it normally needs no second approver: two people already agreed to the claim, and a version upgrade is a prompt to re-check rather than a new claim.

        It does need approval again if the vulnerability's severity has risen since the original was agreed to, or if nothing was ever agreed to. What was agreed was that this did not matter much, which is not an agreement about what it has become. The response says whether a second person is needed.

        Where no second person is needed, the earlier agreement is carried onto the new claim and recorded as carried. The approver named agreed to the previous claim's reasoning, not to what is written here.

        `reasoning` is required. "Still true" with nothing behind it is what a re-affirmation becomes when it is made too easy.

        Requires: public-triage or private-triage on the product
      operationId: reaffirm-decision
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: place
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Reaffirm-decisionRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Re-affirm a decision after an upstream version changed
      tags:
        - Triage
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/places/{place}/reach:
    get:
      description: |-
        Returns the three parts of what a judgment made here covers.

        `here` is how many places in this build. `automatic` are other builds it reaches without anybody doing anything, because their upstream versions and chains already match — a decision is a claim about a combination of code, not about a release. `differing` hold the same issue at the same place at another version, so each is a separate judgment.

        Only `differing` is a choice. The first two follow from the matching rules and are there to be told, not agreed to — and showing them as one number is how a decision comes to reach builds the person making it never knew about.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-decision-reach
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
        - in: path
          name: place
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReachBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show how far a decision here would reach
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/findings/{vulnerability}/resolve:
    post:
      description: |-
        Closes a flaw somebody recorded here, in one build, because it has been fixed there. Every location of the issue in that build is closed together.

        Only a flaw somebody recorded. Everywhere else, resolution is computed from scans rather than declared, which is what stops a fix being reported that shipped in nobody's release. A flaw recorded by hand is the one case with no such evidence and no prospect of any — no scan reports it — so a person closes it or nothing does. An issue a scanner found is refused.

        A reason is required. A closure with no reason is a record saying somebody closed it and nothing else.

        Nothing reopens one. Closing is a considered act, and this is the way it is undone: it is not.

        Requires: public-triage or private-triage on the product
      operationId: resolve-finding
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: vulnerability
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Resolve-findingRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResolvedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Close a recorded flaw as fixed in this build
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
  /v1/products/{product}/streams/{stream}/variants/{variant}/pending-upgrades:
    get:
      description: |-
        Everything committed for this build, one row per upgrade — a source package at the version it was built at, moving to another version — with what it would still close here.

        What it covers is a match, not a list. A finding is covered when its component folds to the same key in this build, so changing the version a release is moving to is one row, and an issue published tonight against the same package is covered by this morning's commitment with nobody acting.

        The fix-bundle query read from the other end. A triager reads an upgrade and the issues it closes; a coordinator reads a build and the upgrades it is waiting on. One query rather than two reports that will eventually disagree.

        Nothing here is declared done. A piece of work has landed when the build stops holding it, which the scans already say — a declared fix that is still open after a scan has run is a missed target, and the scan is independent evidence against the claim.

        Each row says where it stands — planned, landed, or lapsed — derived on every read from the scans and the date the promise named, and set by nobody. A lapsed upgrade returns as one item to whoever holds it: the findings it covers stay covered, because deciding them again one at a time is the thing the promise was made instead of. They return on their own only when the component moved and they did not close, which needs nothing extra — a decision is keyed on the version it was made against and stops applying the moment that version changes.

        There is no separate 'replanned': re-promising writes a new date and the standing promise is the one read, so a replanned upgrade is a planned one with a later date.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-pending-upgrades
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyPlannedBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the upgrades one build is waiting on
      tags:
        - Remediation
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/pending-upgrades.{format}:
    get:
      description: |-
        The same list as a file: one row per upgrade this build is waiting on, where it stands, and what it would still close here.

        The packages one upgrade moves are a single cell, separated by spaces, because a spreadsheet has no second dimension.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-pending-upgrades
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export the upgrades one build is waiting on
      tags:
        - Remediation
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/readiness:
    get:
      description: |-
        Answers the question asked before shipping: is what we are about to ship better or worse than what we last shipped. "8 criticals now, v2.4.1 shipped with 4."

        The release is the newest one cut from this branch, built the same way, that has been scanned here — a branch built for one chip beside a release built for another compares two different pieces of software and reads as a regression somebody then goes looking for.

        Both sides come from scans already collected, so this asks nothing new of a build pipeline. Where there is nothing to compare against, `shipped` is absent and `why` says what is missing rather than reporting zeroes, because a release that shipped clean and a release nobody scanned are not the same answer.

        Counted as issues at components at or above the deployment's line, which `floor` names.

        `blocking` is the worst few of what the count is made of: the work nobody has agreed to ship with, worst first, read through the findings list's own reader with the same line — so the list it opens is the list it counts. Anything agreed is absent, because agreeing is the decision to ship with it. `blockers` says how many there are altogether.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-readiness
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Compare a branch against the last release cut from it
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/register:
    get:
      description: |-
        One row per issue and place in this build, with its state, what was claimed, who claimed it, who agreed, when each of those happened, its deadline and whether that was met.

        The complement of the audit list, not a variant of it. The audit list says what was decided; an auditor's first question is what was *known*, decided or not — so `undecided` rows are in here, and closed ones too. A register of only what is still open answers a different question.

        Current state, and no `as_of`. Reconstructing the view as of a past date was asked for and refused: each row already carries the dates that evidence what is being checked, and a reconstruction would be a second answer about the past that has to be kept honest against the first.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-disposition-register
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: Keep rows standing in any of these. Repeatable; any of them matches
          explode: true
          in: query
          name: state
          schema:
            description: Keep rows standing in any of these. Repeatable; any of them matches
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: Keep rows whose standing judgment is one of these. Repeatable
          explode: true
          in: query
          name: outcome
          schema:
            description: Keep rows whose standing judgment is one of these. Repeatable
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: Keep one component, by name
          explode: false
          in: query
          name: component
          schema:
            description: Keep one component, by name
            type: string
        - description: Keep one vulnerability, under the name it is filed here
          explode: false
          in: query
          name: issue
          schema:
            description: Keep one vulnerability, under the name it is filed here
            type: string
        - description: Keep one side of the build's history. Neither is the whole register, which is what it is for
          explode: false
          in: query
          name: standing
          schema:
            description: Keep one side of the build's history. Neither is the whole register, which is what it is for
            enum:
              - open
              - closed
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 200
            format: int64
            maximum: 500
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Get-disposition-registerResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List every known vulnerability in a build and its disposition
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/register.{format}:
    get:
      description: |-
        The register as a file. Read with your own visibility as it streams, like every other export here.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-disposition-register
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Keep rows standing in any of these. Repeatable; any of them matches
          explode: true
          in: query
          name: state
          schema:
            description: Keep rows standing in any of these. Repeatable; any of them matches
            items:
              enum:
                - undecided
                - waiting
                - agreed
                - lapsed
              type: string
            type:
              - array
              - "null"
        - description: Keep rows whose standing judgment is one of these. Repeatable
          explode: true
          in: query
          name: outcome
          schema:
            description: Keep rows whose standing judgment is one of these. Repeatable
            items:
              enum:
                - affected
                - not-applicable
                - mismatched
                - deferred
                - wont-fix
                - already-fixed
                - upgrade-needed
                - patch-needed
              type: string
            type:
              - array
              - "null"
        - description: Keep one component, by name
          explode: false
          in: query
          name: component
          schema:
            description: Keep one component, by name
            type: string
        - description: Keep one vulnerability, under the name it is filed here
          explode: false
          in: query
          name: issue
          schema:
            description: Keep one vulnerability, under the name it is filed here
            type: string
        - description: Keep one side of the build's history. Neither is the whole register, which is what it is for
          explode: false
          in: query
          name: standing
          schema:
            description: Keep one side of the build's history. Neither is the whole register, which is what it is for
            enum:
              - open
              - closed
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export the disposition register
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/runs/{run}:
    get:
      description: |-
        One run, with what it was measured with and what it changed — broken down by the rating in force, and with how much of what it opened is known to be exploited.

        A receipt says a run happened; this says what it did. A row reading "7,604 opened" is a number with no shape, and somebody looking at a build that jumped overnight is asking which of them matter.

        Counted as issues at components, the unit every other count here uses: a component reached twenty ways carries the same issue twenty times, so counting rows would report how much the dependency graph shares rather than how much changed.

        Derived when it is asked for rather than stored, so it moves as findings close and reopen — and narrowed by what you may see, like every other count here.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-scan-run
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The run, as a receipt or a finding names it
          in: path
          name: run
          required: true
          schema:
            description: The run, as a receipt or a finding names it
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show what one run of the scanner did
      tags:
        - Ingest
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/scans:
    get:
      description: |-
        Returns uploads newest first, each with its state: `reading` (accepted, not yet parsed), `scanning` (parsed, vulnerability scan pending), `scanned` (complete), or `failed` with the reason.

        Because uploads return 202 before parsing, this is how a build pipeline finds out whether its SBOM was usable. An API key sees only the uploads it sent itself.

        Requires: any recognized credential. Answers only what you may see.
      operationId: list-scans
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List uploaded scans and their status
      tags:
        - Ingest
      x-openpsirt-requires:
        scope: any
        note: Answers only what you may see.
    post:
      description: |-
        Accepts a CycloneDX 1.x, SPDX 2.x or SPDX 3.x SBOM and any number of OpenVEX documents as multipart form fields named `inventory` and `suppressions`. The format is taken from the document itself; a major version this does not read is rejected by name.

        The product, branch and variant must already exist; an upload naming something undeclared is rejected and the error says which part is missing.

        Returns 202 before the documents are parsed. A success here means they were accepted for processing, not that they were valid. Poll `GET .../scans` to find out whether they parsed and what the scan found.

        Requires: public-triage or private-triage on the product. A pipeline key covering this product, branch and variant sends without any of these, and is how a build sends.
      operationId: upload-scan
      parameters:
        - description: The declared product this build is of
          in: path
          name: product
          required: true
          schema:
            description: The declared product this build is of
            type: string
        - description: The declared branch or tag
          in: path
          name: stream
          required: true
          schema:
            description: The declared branch or tag
            type: string
        - description: The declared way that stream is built
          in: path
          name: variant
          required: true
          schema:
            description: The declared way that stream is built
            type: string
      requestBody:
        content:
          multipart/form-data:
            encoding:
              inventory:
                contentType: application/json,application/octet-stream
              suppressions:
                contentType: application/json,application/octet-stream
            schema:
              properties:
                inventory:
                  contentEncoding: binary
                  contentMediaType: application/octet-stream
                  format: binary
                  type: string
                suppressions:
                  items:
                    contentEncoding: binary
                    contentMediaType: application/octet-stream
                    format: binary
                    type: string
                  type: array
              required:
                - inventory
              type: object
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadResult"
          description: Accepted
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Upload an SBOM and its VEX documents
      tags:
        - Ingest
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: A pipeline key covering this product, branch and variant sends without any of these, and is how a build sends.
  /v1/products/{product}/streams/{stream}/variants/{variant}/scans/{scan}/documents/{document}:
    get:
      description: |-
        Returns the bytes as they arrived, byte for byte: the hash on the receipt is over what comes back from here, so a copy can be checked against what was actually read.

        A tagged release keeps its documents and a branch build does not. A nightly build's contents are let go once they have been read, because keeping them costs storage that grows with the calendar; a tag's are kept because re-scanning it years from now needs both what it contained and what the build had already argued about its own patches. One that was let go answers 410, which says the bytes went on purpose — the record of what arrived is still on the receipt.

        Named through the scan it belongs to rather than on its own, so whoever may read the receipt may read what it describes and there is one rule rather than two.

        Requires: any recognized credential. Answers only what you may see.
      operationId: fetch-scan-document
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: The upload, as the receipt names it
          in: path
          name: scan
          required: true
          schema:
            description: The upload, as the receipt names it
            format: int64
            type: integer
        - description: The document, as the receipt names it
          in: path
          name: document
          required: true
          schema:
            description: The document, as the receipt names it
            format: int64
            type: integer
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Fetch a scan document
      tags:
        - Ingest
      x-openpsirt-requires:
        scope: any
        note: Answers only what you may see.
  /v1/products/{product}/streams/{stream}/variants/{variant}/vex:
    get:
      description: |-
        Returns an OpenVEX document saying what stands about the third-party components this build ships: every approved `not applicable` and `already fixed` claim, as `not_affected` and `fixed` statements with the justification and the reasoning somebody wrote.

        Approved claims only, and a deferral is absent rather than exported as anything — silence already reads as affected in this format.

        Public findings only. `undisclosed=true` includes the rest for somebody who may read them, which is a preview rather than a thing to publish.

        Requires a publisher configured for this deployment: a document naming none has nobody as its author.

        Requires: public-read or public-triage or private-read or private-triage on the product. Answers only what you may see. A grant on one case does not reach it: the document is about the whole build rather than about one issue.
      operationId: get-vex
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
        - description: Include findings nobody has announced. A preview, not a document to publish
          explode: false
          in: query
          name: undisclosed
          schema:
            description: Include findings nobody has announced. A preview, not a document to publish
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Statements"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Generate a VEX document for a build
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-read
          - public-triage
          - private-read
          - private-triage
        note: "Answers only what you may see. A grant on one case does not reach it: the document is about the whole build rather than about one issue."
  /v1/products/{product}/streams/{stream}/variants/{variant}/vex/issuance:
    get:
      description: |-
        What has been published for this build, oldest first: which revision, when, and what the document hashed to at the time.

        Readable without generating a document. Somebody deciding whether to publish a revision is asking before they generate anything, and the digest beside each entry is what answers whether the last one still describes what this would produce.

        The published document itself belongs to whoever published it. The digest is over what the document says, with the moment it was generated, the version and the build of OpenPSIRT that wrote it left out, so a document regenerated unchanged hashes the same.

        Answered whether or not a publisher is configured for this deployment. Nothing here is assembled and no author is named.

        Requires: public-read or public-triage or private-read or private-triage on the product. Answers only what you may see. A grant on one case does not reach it: a row saying a document about this build went out is as much a disclosure as the document.
      operationId: list-vex-issuances
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyVEXIssuanceBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List the times a VEX document went out
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-read
          - public-triage
          - private-read
          - private-triage
        note: "Answers only what you may see. A grant on one case does not reach it: a row saying a document about this build went out is as much a disclosure as the document."
    post:
      description: |-
        Records that the document for this build was published: when, by whom, and a digest of the document as it stands now.

        A fact about a moment rather than a derived value. What was published on a date cannot be worked out again once a claim is withdrawn, a decision is revised or a scan closes a finding.

        It is what the document's version counts. A document is assembled from what stands now and holds no history of its own, so without this every generation is the first revision of something, and a reader keeping documents by their identifier cannot tell which supersedes which.

        The digest is taken from the document generated here rather than from anything sent: a digest of whatever a caller says answers nothing. The public document, never the preview that includes work nobody has announced.

        Requires: public-triage or private-triage on the product. The document is this deployment's word to a customer. The second pair of eyes on each statement it carries was taken when the claim was approved.
      operationId: record-vex-issued
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: stream
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VEXIssuanceBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Record that a VEX document went out
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-triage
          - private-triage
        note: The document is this deployment's word to a customer. The second pair of eyes on each statement it carries was taken when the claim was approved.
  /v1/products/{product}/tags:
    get:
      description: |-
        Every tag anybody has used here, most-used first.

        What a filter offers rather than a vocabulary: the list is what people have actually written, which is also the evidence for promoting one of them to a real concept.

        Only the words on findings you may read. A tag row carries no visibility of its own, so the list is narrowed by the findings it was written on — reading it is a read act, and writing one is the act that asks for triage.

        Requires: public-read or public-triage or private-read or private-triage on the product. Answers only the words on findings you may see.
      operationId: list-tags
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyString"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List a product's tags
      tags:
        - Findings
      x-openpsirt-requires:
        scope: product
        any_of:
          - public-read
          - public-triage
          - private-read
          - private-triage
        note: Answers only the words on findings you may see.
  /v1/products/{product}/triage-floor:
    put:
      description: |-
        Sets the least severity worth triaging for one product, overriding what the deployment says. Below the line a finding is still recorded, still counted and still reportable, and it carries no deadline — it is out of the working list, not out of the system.

        An empty value clears the override, so the product follows the deployment again. Clearing is not the same as stating the deployment's current line: a product that stated it would stop following when the deployment changed.

        Deadlines are rewritten afterwards, away from the request, because moving the line moves what is on a clock at all. The response returns before that has finished.

        Requires: administrator
      operationId: set-product-triage-floor
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriageFloorBody"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set what a product considers worth triaging
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/variants:
    get:
      description: |-
        Every variant declared under this product, with how much is open in each.

        A variant is a way one release is built — a hardware platform, a flavor — and it is independent of the branch: every stream may be built as any of them. One variant is the ordinary case, and the interface hides the dimension where there is only one.

        Each says whether it is customer-facing, which feeds how urgent a finding in it is, and defaults to customer-facing where nobody has said.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-variants
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
          explode: false
          in: query
          name: counts
          schema:
            description: "Count what is open against each row. Off by default: it is counted over the findings and is the expensive half of this read"
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyVariantBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List a product's build variants
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
    post:
      description: |-
        Records one of the parallel builds of a product — a chip variant, an architecture, an operating system. Declared once for the product, not once per release: a release is filed against it the first time a scan arrives, so nobody restates the list and no release ends up with the name spelled differently.

        Requires: administrator
      operationId: declare-variant
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VariantBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeclaredVariantBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a build variant
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/variants/{variant}:
    delete:
      description: |-
        Takes a variant out of use. It is offered nowhere and no scan may be filed against it. Everything already filed against it stays: its findings are still open, its decisions still stand, the documents published for it still name it, and the releases it was built as still list it.

        The name stays spoken for. Declaring it again brings this variant back rather than making a second one.

        Requires: administrator
      operationId: retire-variant
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retire a build variant
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
    patch:
      description: |-
        Corrects what a variant is called and whether it reaches customers. Both are left alone where the request omits them.

        A name is refused once an OpenVEX document has been published for any release built as this variant. Retire the variant and declare the intended one instead.

        A name another variant of this product holds is refused, including a retired one. Whether it reaches customers feeds how its findings rank and may be corrected at any time.

        Requires: administrator
      operationId: amend-variant
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - in: path
          name: variant
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Amend-variantRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Amend a build variant
      tags:
        - Catalog
      x-openpsirt-requires:
        scope: deployment
  /v1/products/{product}/vex-statements:
    post:
      description: |-
        Takes one OpenVEX document of what a distribution or an upstream security team has published about components this product ships.

        Nothing is applied. What arrives is a third layer beside the build's own claims and our decisions: shown as evidence, offered as a prefill, and never standing as our judgment by itself.

        What a document adds over what the scanner already reports is the reasoning. The status is in the fix state already.

        Uploading again from the same publisher sets aside what they said before rather than deleting it, so what an approval was granted on the strength of stays readable.

        OpenVEX and CSAF-VEX both read. The two say the same thing in different shapes — one puts the status on a statement, the other in which list a product identifier appears in — and both become the same claim here, because what a publisher is saying does not depend on which file they wrote it in. Which of the two a document is decides itself; anything else is refused with a sentence rather than half-read.

        A CSAF *advisory* is refused as well, and deliberately: it is a document about somebody's own flaws, and reading one as claims about what a build ships would take their advisory as this build's argument and every product it names as a suppression.

        Requires: administrator
      operationId: upload-vex-statements
      parameters:
        - in: path
          name: product
          required: true
          schema:
            type: string
        - description: The publisher, where the document does not name itself. At most 191 characters
          explode: false
          in: query
          name: publisher
          schema:
            description: The publisher, where the document does not name itself. At most 191 characters
            maxLength: 191
            type: string
      requestBody:
        content:
          multipart/form-data:
            encoding:
              statements:
                contentType: application/json,application/octet-stream
            schema:
              properties:
                statements:
                  contentEncoding: binary
                  contentMediaType: application/octet-stream
                  format: binary
                  type: string
              required:
                - statements
              type: object
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatementsTakenBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Upload a VEX document
      tags:
        - Ingest
      x-openpsirt-requires:
        scope: deployment
  /v1/releases/out-of-support:
    get:
      description: |-
        Every release you can see whose end-of-life date has passed, with how long ago that was and how many issues are still open against it.

        Past end-of-life the deadline is removed from every open finding on a release, so nothing here is overdue or due soon and none of it reaches a figure built on either. Nothing is deleted or hidden: the findings and the history stay, and stay reportable.

        `inherited` means the date came from the product rather than from the release itself. `open` counts issues at components, not one per place — the same unit every release-level count here uses.

        `within` asks what is about to go, in days ahead. Those come back under `ending`, soonest first and never mixed into what has already gone: the day a release crosses, the deadline comes off every open finding on it and that work leaves every overdue count at once, so a warning and an exposure are two lists rather than one. `ended_days` is negative on those, which is how many days are left.

        Narrowed by what you may see, and ordered by what is open.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-out-of-support
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: Also list releases whose date falls inside this many days ahead. They come back under ending, never mixed into what has already gone
          explode: false
          in: query
          name: within
          schema:
            description: Also list releases whose date falls inside this many days ahead. They come back under ending, never mixed into what has already gone
            format: int64
            maximum: 3650
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetiredOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List releases that are out of support
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/releases/out-of-support.{format}:
    get:
      description: |-
        The same answer as a file: every release you can see whose end-of-life date has passed, how long ago that was, and how many issues are still open against it.

        `open` counts issues at components rather than one per place, the same unit every release-level count here uses. `inherited` means the date came from the product rather than from the release itself.

        `within` writes out what is about to go as well, marked `ending` in the state column, with `ended_days` negative for how many days are left.

        The day the file was taken is stated in it, because how long ago a release ended is only readable against a date.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-out-of-support
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: Also write out releases whose date falls inside this many days ahead, marked as not yet ended
          explode: false
          in: query
          name: within
          schema:
            description: Also write out releases whose date falls inside this many days ahead, marked as not yet ended
            format: int64
            maximum: 3650
            minimum: 0
            type: integer
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export releases that are out of support
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/remediation:
    get:
      description: |-
        Fix velocity, average time to remediate by severity, and what is aging, over a period and narrowed by the scope picker.

        A period or a rolling window. `from` and `to` name a stretch — a quarter, a financial year — and `days` is the rolling window ending now. They are two ways of saying when, so only one may be sent. What is aging is a statement about now whatever period was asked for: how long something has been open is answered by the clock.

        A closure only counts as a fix if the issue actually went away. An upgrade that carried the issue into the next version, and a finding a scanner silently stopped reporting, are not fixes — counting them measures churn and reports it as progress, so the figure moves in the right direction while nothing improves.

        Counted in issues, not in places. One kernel flaw across sixty modules is one thing that was fixed; an average weighted by how far a component fans out measures the dependency graph rather than anybody's work.

        Asked for neither a period nor a window, this is the last 30 days.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-remediation
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
          explode: false
          in: query
          name: from
          schema:
            description: The first day of the period, as YYYY-MM-DD. Without an end the period runs to now
            type: string
        - description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
          explode: false
          in: query
          name: to
          schema:
            description: The day the period ends, as YYYY-MM-DD, and not itself in it. Without a start the period runs from the beginning
            type: string
        - description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
          explode: false
          in: query
          name: days
          schema:
            description: A rolling window of this many days ending now. An alternative to a period, not an addition to one
            format: int64
            maximum: 3650
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RemediationOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Report how fast findings are being fixed
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/review-queue:
    get:
      description: |-
        Returns the claims waiting for a second person, newest first, limited to what you may approve every row of. One entry is one claim — one proposer's action, however many decisions it wrote — with a representative decision and place, how many rows, issues and places it covers, and every build it currently reaches.

        Each entry carries the full reasoning, whether it was previously approved and came back, how long the finding has been deferred in total, and how old the claim is. A claim over many issues also carries `outliers`: the rows that do not look like the rest, which is what to read instead of all of them.

        Approve, send back or set rows aside with `POST /v1/claims/{id}/approval` and `POST /v1/claims/{id}/send-back`.

        Your own claims are not here. Approving your own is refused, so a queue containing them is a list of work you cannot do. Ask for `mine=true` to see what you proposed and nobody has agreed to yet, which is a different question.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-review-queue
      parameters:
        - description: Return what you proposed and nobody has agreed to, instead of what is waiting on you
          explode: false
          in: query
          name: mine
          schema:
            description: Return what you proposed and nobody has agreed to, instead of what is waiting on you
            type: boolean
        - description: Limit to claims made in one product, by name. Empty means every product you can see; a name you cannot see is refused rather than answered empty
          explode: false
          in: query
          name: product
          schema:
            description: Limit to claims made in one product, by name. Empty means every product you can see; a name you cannot see is refused rather than answered empty
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List claims awaiting approval
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/review-queue.{format}:
    get:
      description: |-
        What is waiting for a second person, as a file: every claim, not one page of them.

        One row per claim, the way the screen counts them — one proposer's action, however many decisions it wrote — with how much it covers and how old it is. A backlog is reported in claims because that is the unit somebody works through.

        Limited to what you may approve every row of, as the screen is, and your own claims are not in it. `mine=true` writes out what you proposed and nobody has agreed to, which is a different question, and `product` narrows it the way the screen does.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-review-queue
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Write out what you proposed and nobody has agreed to, instead of what is waiting on you
          explode: false
          in: query
          name: mine
          schema:
            description: Write out what you proposed and nobody has agreed to, instead of what is waiting on you
            type: boolean
        - description: Limit to claims made in one product, by name. Empty means every product you can see; a name you cannot see is refused rather than answered empty
          explode: false
          in: query
          name: product
          schema:
            description: Limit to claims made in one product, by name. Empty means every product you can see; a name you cannot see is refused rather than answered empty
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export the review queue
      tags:
        - Triage
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/roles/bindings:
    delete:
      description: |-
        Removes one group-to-role mapping.

        It takes effect at each member's next sign-in, because group membership is read at sign-in and never again. To cut somebody off now, end their sessions.

        Requires: administrator
      operationId: unbind-group
      parameters:
        - explode: false
          in: query
          name: group
          required: true
          schema:
            type: string
        - explode: false
          in: query
          name: product
          schema:
            type: string
        - explode: false
          in: query
          name: role
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Remove a group-to-role binding
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
    get:
      description: |-
        Lists every group-to-role mapping, and the groups that administer or audit.

        In group-bound mode a mapping is the advance authorization: somebody arriving for the first time in a mapped group is admitted, and somebody in none is refused.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: list-bindings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyBindingBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List group-to-role bindings
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
    post:
      description: |-
        Maps one identity-provider group to one role, so that everybody in that group holds it from their next sign-in.

        Every role names the product it applies to. Administration and the audit permission are bound without one, because they are held over the deployment rather than against a product.

        The group is matched exactly, including its capitals. It is an identity the provider hands over rather than a name anybody here types, so it is stored as given and compared as given — `Security` and `security` are two bindings, and a binding whose capitals do not match what the provider sends grants nothing. The refusal somebody then meets says only that they are not authorized, so check the spelling against the provider rather than against what looks right.

        Requires: administrator
      operationId: bind-group
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BindingBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BindingBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Bind an identity-provider group to a role
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/roles/mode:
    get:
      description: |-
        Says where roles come from here: assigned by an administrator, or derived from the groups an identity provider reports.

        One mode for the whole deployment, never both. A hybrid would need a precedence rule for somebody holding one role from a team and another directly, which is how a stale assignment outlives somebody's removal from the team it was shadowing.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: get-role-mode
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModeBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get the role assignment mode
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
    put:
      description: |-
        Turning group binding on sets assignments aside rather than deleting them, and turning it off restores them — so trying it is not a one-way door. Refused if it would leave nobody able to administer this deployment.

        Requires: administrator
      operationId: set-role-mode
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ModeBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModeBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set the role assignment mode
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/running-out:
    get:
      description: |-
        Returns open findings whose deadline falls within `days`, where nobody has recorded a decision, across every product you can see.

        A deadline that has been answered is not on this list. A dismissal takes a finding off the clock, because the claim is that it will not be fixed, and a deferral replaces the deadline with its own date. What is left is time passing with nothing said.

        The window comes from how urgent a finding is. Being known-exploited has its own and it is the shortest, whatever the severity says. Anything the reports did not rate takes the medium window.

        One row per issue at a component, however many places it sits at. `days_left` is negative once something is overdue.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-running-out
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The distance ahead to look
          explode: false
          in: query
          name: days
          schema:
            default: 14
            description: The distance ahead to look
            format: int64
            maximum: 365
            minimum: 0
            type: integer
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The offset into the list
          explode: false
          in: query
          name: offset
          schema:
            description: The offset into the list
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyLateBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List findings running out of time that nobody has decided about
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/running-out.{format}:
    get:
      description: |-
        Open findings whose deadline falls within `days` and which nobody has decided about, as a file: every row, not the page the screen shows.

        The same list the screen reads, under the same rule — a deadline that has been answered is not on it, because a dismissal takes a finding off the clock and a deferral replaces the deadline with its own date. What is left is time passing with nothing said.

        `days_left` is negative once something is overdue.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-running-out
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The distance ahead to look
          explode: false
          in: query
          name: days
          schema:
            default: 14
            description: The distance ahead to look
            format: int64
            maximum: 365
            minimum: 0
            type: integer
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export what is running out of time
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/scanning:
    get:
      description: |-
        Returns every build you can see, longest-silent first, with when a scan last arrived for it and whether that is longer ago than this deployment allows.

        A build that stops being scanned reports no new findings and fails nothing, so it looks healthier than one that is still being scanned. A build nothing has ever been filed against is measured from when it was declared.

        How long counts as quiet is the `scanning.quiet-after` setting.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-scanning
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The number returned. Quietest first, so the default is the answer for any estate somebody reads by hand
          explode: false
          in: query
          name: limit
          schema:
            default: 200
            description: The number returned. Quietest first, so the default is the answer for any estate somebody reads by hand
            format: int64
            maximum: 500
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoverageOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List when each build was last scanned
      tags:
        - Scans
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/scanning.{format}:
    get:
      description: |-
        Every build you can see, longest-silent first, as a file: when a scan last arrived, how long it has been, and whether that is longer than this deployment allows.

        `quiet_days` is measured from the last arrival, or from when the build was declared where nothing has ever been filed against it — `last_received_at` is empty in that case, which is a different situation from a scan that failed.

        A build whose release is out of support is in the file, marked `retired`, and is never reported as quiet: silence there is expected. Leaving it out and saying nothing would be a different answer.

        The threshold `quiet` was computed against is stated in the file.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-scanning
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export when each build was last scanned
      tags:
        - Scans
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/score:
    get:
      description: |-
        Returns the base score and the severity band a vector works out to. It reads nothing and records nothing.

        An empty vector is refused rather than answered with empty fields.

        CVSS 3.0, 3.1 and 4.0. A vector on any other scheme is refused, including version 2. Metrics outside the base set are read and ignored, so a vector carrying threat or environmental metrics scores as the base vector in it.

        Scores from two schemes are not comparable as numbers. The severity band is, and it is the same five words over the same five ranges under both.

        Requires: any signed-in person, and not a pipeline key. Answers a calculation, and reads nothing.
      operationId: score-vector
      parameters:
        - description: A CVSS 3.0, 3.1 or 4.0 base vector
          explode: false
          in: query
          name: vector
          required: true
          schema:
            description: A CVSS 3.0, 3.1 or 4.0 base vector
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Score-vectorResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Score a CVSS vector
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers a calculation, and reads nothing.
  /v1/session:
    delete:
      description: |-
        Ends the session the request arrived on, everywhere rather than in this browser alone — the session is stored, so it stops working whichever copy of the application answers next.

        Requires: your own credential
      operationId: sign-out
      responses:
        "204":
          description: No Content
          headers:
            Set-Cookie:
              schema:
                type: string
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Sign out
      tags:
        - Access
      x-openpsirt-requires:
        scope: self
  /v1/session/me:
    get:
      description: |-
        Returns the caller, the products they can reach, and what they may do in each one.

        It answers what a screen has to know before it draws: whether to offer an action at all. Without it a client either hides nothing and lets people find the refusal, or re-implements the mapping from roles to capabilities and drifts from the one the server enforces.

        Capabilities, not roles, for that reason. Which roles produce which capability is the server's rule and stays there.

        Requires: your own credential
      operationId: get-current-subject
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WhoBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Describe the current subject
      tags:
        - Access
      x-openpsirt-requires:
        scope: self
  /v1/session/me/digest:
    put:
      description: |-
        Turns the daily digest on or off, and says whether it lists findings nobody owns as well as your own outstanding work.

        Both are off until asked for. The digest carries what nothing else told you: work that became yours without a message, and — where asked for — findings that opened since the last one and nobody has picked up.

        Asking for the second without the first is refused: a digest listing what nobody owns is still a digest.

        Nothing is sent anywhere without an address recorded against you, which `GET /v1/session/me` reports as `reachable`.

        Requires: your own credential
      operationId: set-digest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Set-digestRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Choose what is sent to you daily
      tags:
        - Notifications
      x-openpsirt-requires:
        scope: self
  /v1/settings:
    get:
      description: |-
        Returns every setting an operator may change, its value, and what it decides. `default` means nobody has set it and the shipped value is in use.

        The shipped numbers are a starting point rather than a recommendation. What a deployment can hold to is a question about that deployment, and a deadline nobody agreed to produces an estate that is permanently late and a signal everybody ignores.

        Requires: administrator, or the audit permission over this deployment's own records
      operationId: list-settings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodySettingBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List this deployment's settings
      tags:
        - Administration
      x-openpsirt-requires:
        scope: records
  /v1/settings/{name}:
    put:
      description: |-
        Sets one value for everybody here. Durations are written the way Go writes them — `72h`, `30m` — and a value that cannot be read is refused rather than stored, since a setting nothing can parse is a policy silently reverting to the shipped one.

        Only the settings this deployment recognizes may be set. A name it does not know is refused, because storing it would create something nothing ever reads.

        Requires: administrator
      operationId: set-setting
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Set-settingRequest"
        required: true
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Change one setting
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/sign-in:
    get:
      description: |-
        Returns the sign-in providers this deployment has configured, so a sign-in page can offer them.

        Answered without a credential, because it is what somebody sees before they have one. It is the only reading endpoint that is, and it reports names an operator configured and nothing else — no account exists or does not exist as far as this is concerned, which is the disclosure that would matter.

        Requires: nothing: this is answered before anybody has a credential
      operationId: list-sign-in-providers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyProviderBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List sign-in providers
      tags:
        - Access
      x-openpsirt-requires:
        scope: none
  /v1/teams:
    get:
      description: |-
        Lists the teams work can be routed to.

        A team holds work and grants nothing: no role, no visibility, no capability. One team therefore carries mixed clearance as a matter of course, and what each member sees of the work routed to it is what they could see anyway.

        Names to anybody, membership to an administrator. Routing work to a team means naming one, so anybody who may hand work around has to be able to see the names; who is on it is the same question as who is here, and that is answered where the rest of the record is.

        Requires: any signed-in person, and not a pipeline key. Membership is listed for an administrator; anybody else sees the names.
      operationId: list-teams
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyTeamBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List teams
      tags:
        - Administration
      x-openpsirt-requires:
        scope: person
        note: Membership is listed for an administrator; anybody else sees the names.
    post:
      description: |-
        Records a team so that work can be routed to it. Recording the same name again returns the team and adds any members named, so a script that sets a deployment up stays runnable.

        Everybody named must already have been recorded: a team cannot bring somebody into the deployment, because it grants nothing and an account that exists is access somebody decided to give.

        Requires: administrator
      operationId: record-team
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TeamRecordBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeclaredTeamBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a team
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/teams/{team}:
    delete:
      description: |-
        Takes a team out of use. Nothing new is routed to it and its membership goes, while work already routed to it still names it — the row stays for the same reason an account is deactivated rather than deleted.

        Requires: administrator
      operationId: retire-team
      parameters:
        - in: path
          name: team
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retire a team
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/teams/{team}/assignments:
    get:
      description: |-
        The open findings routed to a team, most urgent first, in the same units as everywhere else: one item per issue in a component in a product.

        Work goes to a team by standing rule and by an assignment naming one, so a team holds work the way a person does — and the totals list says so. This is the list behind that number.

        A team nobody declared answers with an empty list rather than a 404, which is also what a team whose work is not yours to see answers. The two are deliberately the same, for the reason a person's is: refusing would answer "does this team exist" for any credential at all.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see. A team nobody declared answers as one whose work you cannot see.
      operationId: list-team-assigned
      parameters:
        - description: The team's name
          in: path
          name: team
          required: true
          schema:
            description: The team's name
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-team-assignedResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List what one team is dealing with
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see. A team nobody declared answers as one whose work you cannot see.
  /v1/teams/{team}/members/{identity}:
    delete:
      description: |-
        Removes somebody from a team. Work they have already taken stays theirs: membership says where work arrives, not who holds what has arrived.

        Requires: administrator
      operationId: remove-from-team
      parameters:
        - in: path
          name: team
          required: true
          schema:
            type: string
        - in: path
          name: identity
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Take somebody off a team
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
    put:
      description: |-
        Adds somebody to a team. Saying it twice asserts the same thing.

        It grants them nothing. What they see of the team's work is what they could see anyway, so a team with one member who may read undisclosed work and four who may not is an ordinary arrangement rather than a misconfiguration.

        Requires: administrator
      operationId: add-to-team
      parameters:
        - in: path
          name: team
          required: true
          schema:
            type: string
        - in: path
          name: identity
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Put somebody on a team
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/tokens:
    get:
      description: |-
        Lists your own personal tokens: what each is called, when it expires and when it was last used. Never anybody else's, and never the secrets.

        A token is a live reference to you rather than a copy of what you could do when it was made, so what one reaches shrinks the moment your roles do.

        Requires: your own credential
      operationId: list-my-tokens
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyTokenBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List your API tokens
      tags:
        - Access
      x-openpsirt-requires:
        scope: self
    post:
      description: |-
        Creates a personal token and returns its secret. The secret is shown once and never again — what is stored is a digest.

        Expiry is not optional, and `lifetime` may not exceed the ceiling an administrator has set. A credential that never runs out is one nobody ever revokes, and those are found when somebody leaves and nobody knows what breaks if it is turned off.

        Requires: your own credential. Signed in, not through a token: a token cannot mint another.
      operationId: mint-token
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TokenBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create an API token
      tags:
        - Access
      x-openpsirt-requires:
        scope: self
        note: "Signed in, not through a token: a token cannot mint another."
  /v1/tokens/{name}:
    delete:
      description: |-
        Revokes one of your own tokens by name. It stops working immediately.

        Yours alone. An administrator withdraws anybody else's through the administration paths.

        Requires: your own credential. Signed in, not through a token: a token cannot withdraw another.
      operationId: revoke-my-token
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Withdraw one of your API tokens
      tags:
        - Access
      x-openpsirt-requires:
        scope: self
        note: "Signed in, not through a token: a token cannot withdraw another."
  /v1/trend:
    get:
      description: |-
        Returns the three counts per step, with open split by severity, across every product you can see.

        Narrowable to part of a tree. `component` keeps one package at any version; `beneath` keeps a component and everything under it, which needs a branch and a variant naming exactly one build. A team that owns one area asks for its own three lines this way.

        Three series rather than one, because separately they are three numbers and together they say whether the team is keeping pace: new consistently outrunning resolved is a growing backlog.

        Split by severity because a total that barely moves while its critical share rises is getting worse, and a single line hides exactly that.

        Worked out when it is asked for. Nothing is precomputed or refreshed on a schedule until a measurement says it has to be.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-trend
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - explode: false
          in: query
          name: weeks
          schema:
            default: 12
            format: int64
            maximum: 104
            minimum: 1
            type: integer
        - description: Keep only what is open against components of this name, whatever version
          explode: false
          in: query
          name: component
          schema:
            description: Keep only what is open against components of this name, whatever version
            type: string
        - description: Keep only what sits at this component or anywhere under it. A subtree is a walk over one build's edges, so this needs a branch and a variant naming exactly one build
          explode: false
          in: query
          name: beneath
          schema:
            description: Keep only what sits at this component or anywhere under it. A subtree is a walk over one build's edges, so this needs a branch and a variant naming exactly one build
            type: string
        - description: The version, where the build holds that name at several
          explode: false
          in: query
          name: beneath_version
          schema:
            description: The version, where the build holds that name at several
            type: string
        - description: The ecosystem, for the few names a build holds at one version as two components
          explode: false
          in: query
          name: beneath_ecosystem
          schema:
            description: The ecosystem, for the few names a build holds at one version as two components
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBodyPointBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show new, resolved and open over time
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/trend.{format}:
    get:
      description: |-
        One row per step, with what arrived, what was answered and what stood open at the end of it — each split by severity.

        The two flows are what the backlog is read for. Ten arriving and ten answered is a team keeping pace where both are low, and a team losing ground where what arrives is critical and what leaves is not.

        Takes the window and the narrowings the trend takes. Read with your own visibility, like the chart it comes from.

        Requires: any signed-in person, and not a pipeline key. Exports only what you may see.
      operationId: export-trend
      parameters:
        - in: path
          name: format
          required: true
          schema:
            enum:
              - csv
              - json
            type: string
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - explode: false
          in: query
          name: weeks
          schema:
            default: 12
            format: int64
            maximum: 104
            minimum: 1
            type: integer
        - description: Keep only what is open against components of this name, whatever version
          explode: false
          in: query
          name: component
          schema:
            description: Keep only what is open against components of this name, whatever version
            type: string
        - description: Keep only what sits at this component or anywhere under it. A subtree is a walk over one build's edges, so this needs a branch and a variant naming exactly one build
          explode: false
          in: query
          name: beneath
          schema:
            description: Keep only what sits at this component or anywhere under it. A subtree is a walk over one build's edges, so this needs a branch and a variant naming exactly one build
            type: string
        - description: The version, where the build holds that name at several
          explode: false
          in: query
          name: beneath_version
          schema:
            description: The version, where the build holds that name at several
            type: string
        - description: The ecosystem, for the few names a build holds at one version as two components
          explode: false
          in: query
          name: beneath_ecosystem
          schema:
            description: The ecosystem, for the few names a build holds at one version as two components
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Export new, resolved and open over time
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Exports only what you may see.
  /v1/trend/releases:
    get:
      description: |-
        One point per tagged release of one product, oldest first, with what is open against it now.

        The axis follows what is being viewed. A branch is scanned nightly and has continuous data, so a calendar reads correctly on it. A tag never moves again, and releases months apart make a calendar count read as slow drift rather than the step change it was — the gaps are the chart's whole shape and they are gaps in nothing.

        Answered against today's vulnerability data, not as of the day each was cut. That is what re-scanning a shipped release is for.

        No rates here. How many appeared and were resolved between two releases is an artifact of how far apart somebody cut them; rates always plot on calendar. And a product must be named: two products' tags interleave by date and mean nothing side by side.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: get-release-trend
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - description: The number of releases, most recent kept
          explode: false
          in: query
          name: limit
          schema:
            default: 12
            description: The number of releases, most recent kept
            format: int64
            maximum: 50
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Get-release-trendResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show what each release shipped with
      tags:
        - Reports
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/unassigned:
    get:
      description: |-
        Returns open findings with no assignee, across every product you can see, most urgent first.

        Deliberately not scoped to one product: work falling between people is exactly what hides when every screen shows one product and nobody looks at the others.

        One item per issue in a component in a product, not one per build. The same code built as several variants is one piece of work — a judgment is keyed on the product and the code rather than on the build, so answering it once answers every build holding the same versions. `builds` says how many that is. Where two builds ship different versions of the component they are different work and appear separately.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-unassigned
      parameters:
        - description: Limit to one product, by name. Empty means every product you can see
          explode: false
          in: query
          name: product
          schema:
            description: Limit to one product, by name. Empty means every product you can see
            type: string
        - description: Limit to one branch or tag. Only meaningful with a product
          explode: false
          in: query
          name: stream
          schema:
            description: Limit to one branch or tag. Only meaningful with a product
            type: string
        - description: Limit to one variant. Only meaningful with a product, and independent of the branch
          explode: false
          in: query
          name: variant
          schema:
            description: Limit to one variant. Only meaningful with a product, and independent of the branch
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 50
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: offset
          schema:
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-unassignedResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List findings nobody is dealing with
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/upstream/unanswered:
    get:
      description: |-
        What asking public package indexes did not answer, and why of each.

        Asking sends a component's name to that ecosystem's index, so names this deployment calls its own are never sent. `ours` is what they were matched against, narrowed to the products you may read.

        `unknown` is a component no public index has heard of — a private module or a vendored fork — and is the candidate list for `OPENPSIRT_UPSTREAM_INTERNAL`. `unreadable` is an identifier nothing can turn into a request.

        A component the pass has not reached is not here. It is waiting rather than unanswered.

        Answers only components in products you may read.

        Requires: any signed-in person, and not a pipeline key. Answers only what you may see.
      operationId: list-unanswered-upstream
      parameters:
        - description: The number returned
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: The number returned
            format: int64
            maximum: 200
            minimum: 1
            type: integer
        - description: The number skipped
          explode: false
          in: query
          name: offset
          schema:
            description: The number skipped
            format: int64
            minimum: 0
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnansweredOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List components with no upstream answer
      tags:
        - Findings
      x-openpsirt-requires:
        scope: person
        note: Answers only what you may see.
  /v1/version:
    get:
      description: |-
        Identifies the build that is answering, so an operator can tell which version they are looking at.

        Requires: any signed-in person, and not a pipeline key. A person rather than a pipeline: a build server has no business asking what version is running.
      operationId: get-version
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Info"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get the server version
      tags:
        - Meta
      x-openpsirt-requires:
        scope: person
        note: "A person rather than a pipeline: a build server has no business asking what version is running."
  /v1/vulnerability-data:
    get:
      description: |-
        The vulnerability data version this deployment's scans are running against, and when it last moved.

        Nothing here is a version anybody can order. What a scanner reports is an opaque string — a date for one, a schema revision and a build stamp for another — so the only question that can be asked of it is whether it changed. That is enough: what matters is that it moved, not which is newer.

        `moved_at` is the most recent time any version was seen for the first time. A version that comes back was not a change the second time, which is what an air-gapped deployment re-importing an older bundle looks like.

        Absent everywhere means nothing has finished a scan and stated a version, which is a deployment nobody has pointed at anything yet rather than data that has gone stale.

        Requires: administrator
      operationId: get-vulnerability-data
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VulnerabilityDataBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Show what the scans are answering against
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/work/set-aside:
    get:
      description: |-
        Returns background work the queue has set aside, newest first, with why each stopped where anything reported a reason. A job is set aside once it has been tried as many times as it is allowed to be, whether it reported a failure or its worker stopped answering.

        At most 200 are returned. `total` is how many are set aside in all, so a clipped page can be told from a complete one.

        `waiting` is what has not stopped: how much of each kind is queued, against the bound that refuses more of it. A queue filling up and a queue that has given up are different faults and only one of them leaves rows here.

        Requires: administrator
      operationId: list-set-aside-work
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/List-set-aside-workResponse"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List work that stopped being retried
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
  /v1/work/set-aside/{id}/retry:
    post:
      description: |-
        Puts one set-aside job back in the queue for whichever worker takes it next, with its attempts reset to zero and its last error kept.

        A job that is not set aside is refused rather than moved.

        Requires: administrator
      operationId: retry-set-aside-work
      parameters:
        - description: The job to put back
          in: path
          name: id
          required: true
          schema:
            description: The job to put back
            format: int64
            type: integer
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Retry work that was set aside
      tags:
        - Administration
      x-openpsirt-requires:
        scope: deployment
