apiVersion: v1
kind: Service
metadata:
  name: {{ include "openpsirt.fullname" . }}
  labels: {{- include "openpsirt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "openpsirt.selectorLabels" . | nindent 4 }}
