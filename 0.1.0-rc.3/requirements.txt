# Pinned to what was verified locally. Update by installing, building the site,
# and copying the resolved versions back here — never by hand.
mkdocs-material==9.7.7
mkdocs-swagger-ui-tag==0.8.1
mike==2.2.0
